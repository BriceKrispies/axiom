# The runner resolves each `target` against the world's tags and executes the
# closed verb vocabulary (goto / look_at / capture / wait).

[[directive]]
verb = "goto"
target = "mountaintop"

[[directive]]
verb = "look_at"
target = "ground"

[[directive]]
verb = "capture"
label = "summit_view"
