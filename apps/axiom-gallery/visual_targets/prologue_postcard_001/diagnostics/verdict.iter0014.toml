attacked_axis = "artifact_level"
note = "the dominant remaining artifact is the faceted, floating low-poly canopies; that needs a canopy MESH change (resolution is hardcoded, inexpressible in the manifest), so redirect the attack there."
