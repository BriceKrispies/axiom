# CRITIC RECALIBRATION (convergence critic pass, 2026-07-06) — champion.png md5
# 091563c28722b5548a3836e900abaaaa (the ACTUAL artifact on disk / the convergence
# champion branch tip at e6030b7), re-scored fresh against reference.png. Harness
# unchanged: axiom-shot --app soccer-penalty --backend gpu --features offscreen,
# 960x600, tick 0. 0..5, harsh; a proxy = 1. final_score = lowest*0.7 + avg*0.3.
#
# CONSISTENCY NOTE (app-tier mirror): this file is kept BYTE-IDENTICAL (below this
# note) to the primary visual_targets/soccer_penalty_gameplay/scorecard.champion.toml.
# Both target dirs carry the same authoritative card and the same abstractions/
# (0001 humanoid kit + 0002 hemisphere-ambient channel).
#
# WHY THIS REWRITE (stale-record fix, AGAIN): the prior card described md5 be46c518
# (the live-skinned render). But the champion.png on disk is now md5 091563c2 — the
# e6030b7 "7-lens board" promote render (camera dolly-back + braced-keeper pose +
# lighting/crowd-albedo + lifted backdrop/haze). be46c518 has been demoted to
# candidate.png. The champion image swapped WITHOUT a scorecard/ledger rescore —
# exactly the unrecorded-swap drift every prior critic pass flagged as forbidden.
# This rewrite scores the artifact that ACTUALLY EXISTS (091563c2).
#
# NUMBERS: my fresh harsh pixel read of 091563c2 lands on the SAME eleven values the
# prior be46c518 card carried — the e6030b7 swap was a LATERAL move, not progress:
# the lifted backdrop/haze made the frame brighter (was near-black) but the pitch
# gained pronounced horizontal streak/scanline tearing and the ball stayed
# over-scaled; the braced-keeper pose is a different WRONG pose (arms-up star vs the
# reference's lowered ready-crouch), still a proxy. No axis crosses a band. So this
# is a RE-DESCRIPTION to the current artifact, NOT a numeric change and NOT inflation.
# final_score stays 1.27; next attack stays goalkeeper_silhouette.

composition_and_framing   = 3  # held. Kicker centre-left (from behind) / ball centre-low / goal + braced keeper upper-mid tracks the reference layout; the ball is over-large in the foreground and the goal reads small, but the essential relationships hold = 3.
camera_lens_and_scale     = 2  # held. The e6030b7 dolly-back was meant to hold the goal size and give a fuller kicker; in 091563c2 the ball still dominates the foreground and the kicker is cropped ~at the knees while the goal reads small — NOT a shot-class match to the reference's elevated, pulled-back view (whole kicker + full goal in frame). An over-scaled proxy shot = 2. "When unsure, take the lower."
kicker_silhouette         = 2  # held. A washed light-blue body from behind, blocky, with NO visible jersey "10" and legs merging into the ball. "Merely reads as a blue player from behind" = proxy+ = 2.
goalkeeper_silhouette     = 1  # held. Small yellow box-man in a braced arms-UP star/Y, legs spread — NOT the reference keeper's lowered, bent, ready-to-dive crouch; featureless, wrong pose, wrong glove read. A thumbnail proxy = 1. Still the LOWEST axis and the correct NEXT ATTACK.
ball_fidelity             = 3  # held. Black/white panelled ball, close and centred; blocky low-res panels cap it at 3. Strongest-matching element.
goal_and_net_fidelity     = 2  # held. White posts + a coarse hanging grid net read as goal+net (here the net grid is red/pink-tinted); no cloth droop / crossbar depth vs the reference's fine white mesh = 2.
field_markings_and_turf   = 1  # held. Bright green turf with pronounced horizontal streak banding but NO penalty spot/box/arc and no crisp mow-stripes = proxy = 1.
stadium_and_ad_boards     = 1  # held. The lifted "crowd-albedo" backdrop is a blank tan/brown blocky wall with NO crowd figures and NO "AXIOM SPORTS" text; a solid dark-red ad band with no legible text. Empty + textless = 1.
lighting_shadow_and_depth = 2  # held. The lifted backdrop/haze brightened the frame (was markedly DARK in be46c518) toward the reference's daylight — a REAL but SUB-BAND tone gain: still flat Lambert, no AO/depth, washed, and now streak-torn. Held at 2 (harsh-borderline).
gameplay_readability      = 2  # held. Reads as a penalty setup but the offscreen render carries NO reticle, NO HUD (SCORE/ROUND/BEST), NO SHOT POWER bar = 2.
artifact_level            = 2  # held. Pronounced horizontal streak/scanline tearing across the pitch (and faintly the actors) plus blocky low-res edges and the red-tinted net; not clean flat colour = 2. "When unsure, take the lower" — borderline 1.

# lowest = 1, tied across goalkeeper_silhouette (axis 4), field_markings_and_turf
# (axis 7) and stadium_and_ad_boards (axis 8). By FIXED axis order the FIRST axis at
# the lowest value is goalkeeper_silhouette.
# sum = 3+2+2+1+3+2+1+1+2+2+2 = 21. avg = 21/11 = 1.909.
# final_score = 1*0.7 + 1.909*0.3 = 1.27.
# Next axis (lowest, ties by fixed order): goalkeeper_silhouette (=1).
