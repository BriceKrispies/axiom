# Convergence ledger — treasure-chest-pick
# Append-only. Iteration 0 is the CRITIC's baseline calibration: it fixes the
# axis list + order for the campaign and records the harsh baseline scores.

[[iteration]]
iteration = 0
attacked_axis = "none"           # baseline calibration, no render change
changed_files = [
  "apps/casino-games/visual_targets/treasure-chest-pick/scorecard.champion.toml",
  "apps/casino-games/visual_targets/treasure-chest-pick/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_baseline"
abstraction_introduced = false
next_attacked_axis = "scene_props_completeness"
reason = """
CRITIC baseline calibration (first pass). Scored the champion against
reference.png at the parity bar, harshly. final_score = 0.5; lowest axis =
scene_props_completeness = 0, so it is the next axis to attack.

Root-cause finding (verified in apps/casino-games/web/src/games/
treasure-chest-pick/scene.ts): the app renders ONLY nine chests on a turquoise
platform disc plus a stage-room backdrop and reveal lights. The reference's
entire beach diorama — warm sand ground, palm tree (with a long soft shadow),
red crab, sand castle + flag, starfish, seashells, scattered gold nuggets — does
NOT exist in the game. scene_props_completeness = 0 is therefore 'not built',
not merely 'cropped off-frame'. This is the dominant identity gap.

COUPLING NOTE for whoever attacks scene_props_completeness: it is entangled with
composition_and_framing = 1. The champion is captured at 960x600 (capture.md),
which frames only the pool; the reference (1568x968) frames the pool as ~60% of
a wide beach with sand margins holding the props. Building the beach diorama will
not read unless the camera framing also widens to include those margins.
Whoever takes this axis must (a) add the beach ground + props as real geometry
AND (b) widen the framing so they are on-screen — otherwise props will be built
but stay outside the captured frame, moving no number honestly.

ABSTRACTION-GATE NOTE: adding a palm tree / crab / castle / starfish / shells is
new geometry that the config/manifest layer genuinely CANNOT express (there is
no beach-prop manifest surface today). Per the skill, that is the 'inexpressible'
branch of the gate — the structural fix is legitimate on the FIRST attempt (do
NOT burn three ceremonial config nudges first), but it MUST ship with an
abstractions/NNNN.toml recording inexpressible = true, failed_attempts = [], and
the smallest new surface at the lowest correct layer.

No abstraction record is owed at iteration 0: the baseline champion is the app's
pre-existing render, not a convergence structural change.
"""

[iteration.scorecard_before]
composition_and_framing = 1
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[iteration.scorecard_after]
composition_and_framing = 1
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[[iteration]]
iteration = 1
attacked_axis = "none"           # critic re-score of the pass-1 promoted champion
changed_files = [
  "apps/casino-games/visual_targets/treasure-chest-pick/scorecard.champion.toml",
  "apps/casino-games/visual_targets/treasure-chest-pick/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
abstraction_introduced = false
next_attacked_axis = "scene_props_completeness"
reason = """
CRITIC pass-2 re-score. Commit 0837038a promoted the pass-1 render (it changed
ONLY champion.png, 106545 -> 91398 bytes) with NO re-score and NO ledger entry.
This iteration re-verifies the scorecard against the actual pass-1 champion and
records the finding. NO number changed (no inflation was found; nothing was
lowered): pass-1 moved no axis TOWARD identity, so the honest re-score equals the
baseline. This entry exists so the record stops silently carrying a swapped image
under unre-scored numbers, and to refresh the scorecard prose (which still
described the pre-pass-1 render).

WHAT PASS-1 ACTUALLY DID (pre-pass-1 champion @0d21679a vs pass-1 champion
@0837038a, both vs reference.png):
- Camera dollied BACK: chests smaller, more lagoon rings + a thin sky strip
  visible. A hair less "tight crop", but the surround is still WATER, not the
  reference's sand-and-props margins -> composition_and_framing stays 1.
- Lagoon saturated toward the reference's cyan (colorist a6949233) -> good for
  the water hue.
- Chest wood drifted PALER with faded gold bands (surfacing b2e77bc8 wood ladder
  + lighting a40138de key) -> AWAY from the reference's saturated brown+brass.
  The water gain and the chest loss roughly cancel, so color_palette stays 2 and
  subject_fidelity holds at 2 (near the 1/2 line on color; held on form). Logged
  as a WATCH-ITEM: if a later pass pushes the wood paler again, both drop.

PROCESS FINDINGS (the honesty flags this pass owes):
1. RIGHT-AXIS: the next axis is correctly scene_props_completeness = 0 — it is
   still the lowest and still absent. But pass-1 spent its effort on higher axes
   (color/material/lighting/framing) and left the lowest axis untouched. Because
   final_score = lowest*0.7 + avg*0.3, an untouched 0 pins final_score at 0.5 no
   matter how much the secondary axes move — so pass-1 could not, and did not,
   improve final_score. Attacking anything but scene_props_completeness next is
   churning secondary numbers while the dominant identity gap stays at 0.
2. PROMOTION: per the skill's decision table, when the attacked axis does not
   improve the outcome is start_new_candidate_branch (keep old champion), not a
   promotion. Pass-1 was promoted anyway. It is not a regression (no non-attacked
   axis dropped >= 2), so the pass-1 image is left standing as champion — but the
   record now states plainly that it did NOT advance convergence.

ABSTRACTION-GATE: honored. Pass-1 made no structural change (no new primitive /
manifest field / shader) — only camera + color/material config feeding the same
render — so no abstractions/NNNN.toml is owed this pass. The beach-prop geometry
that scene_props_completeness needs is still un-built; the iteration-0 note
already records that its abstraction record comes due WHEN it is built.

PARITY-CLAIM: clean. No "done / matched / parity" language in the commit,
scorecard, or ledger.
"""

[iteration.scorecard_before]
composition_and_framing = 1
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[iteration.scorecard_after]
composition_and_framing = 1
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[[iteration]]
iteration = 2
attacked_axis = "none"           # critic re-score of the pass-2 promoted champion
changed_files = [
  "apps/casino-games/visual_targets/treasure-chest-pick/scorecard.champion.toml",
  "apps/casino-games/visual_targets/treasure-chest-pick/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
abstraction_introduced = false
next_attacked_axis = "scene_props_completeness"
reason = """
CRITIC pass-3 re-score of the PASS-2 champion. Commit b18b6d4a promoted the
pass-2 render (image-only swap, 91398 -> 91567 bytes). The underlying source is
the pass-2 line: modeler be85ddfa (shrink turquoise disc to an inset pool),
art-director 29e34b79 (tighten span to 2.5) which foreman a919ab7a REVERTED back
to the wide span 5.0 to keep the beach reveal on-screen, surfacing 44de0bdb
(deepen wood to saddle-brown), lighting c593eb0b (rake sun lower). This iteration
re-verifies the scorecard against the actual pass-2 champion.

ONE number moved, honestly and driven by real geometry (not a nudge):
- composition_and_framing 1 -> 2. The water-shrink added NO new geometry; it
  reparameterized the EXISTING stageRoom floor slab (a new default-valued
  `accentRadius` arg on stageRoom + a `WATER_RADIUS = 5.0` const) so the pre-
  existing sandy StageFloor now reads as a wide beach MARGIN around a central
  inset pool. That central-pool-in-sand skeleton is the reference's core
  composition, which pass-1's all-water flood entirely lacked -> off the floor.
  Held at 2 (not 3) by two real deviations: a pastel blue sky/ocean band now
  intrudes at the top + side wedges (the reference's sand fills to the frame
  edge, no horizon), and the margins are empty + symmetric vs the reference's
  asymmetric staged props. WATCH-ITEM: the art-director crop that would have
  removed the sky band was reverted to preserve the beach reveal; a pass that
  keeps the beach AND crops the band earns composition -> 3.

WHAT ELSE PASS-2 DID, and why no other number moved:
- Deepened the chest wood to saturated saddle-brown (surfacing 44de0bdb). This
  REVERSES the pass-1 watch-item (wood drifting paler toward plain pine); the
  threatened subject_fidelity / color_palette drop therefore did NOT happen.
  Both HOLD at 2 — the wood read is back toward reference, but the chests are
  still flat canvas2d (no texture/AO), so no rise to 3 either.
- The revealed sandy apron finally supplies the reference's dominant WARM-SAND
  mass that color_palette flagged as "wholly absent" — a genuine palette gain.
  color_palette HELD at 2 anyway (take the lower): the apron is a pale cream,
  not the reference's saturated golden tan, and the added blue sky/ocean band is
  an off-reference element. Balance closer; hues still off.
- Raked the sun lower (lighting c593eb0b): a key-direction tweak with no cast /
  grounding shadow produced -> lighting_and_shadow HOLDS at 1.

scene_props_completeness STAYS 0. Re-verified this pass: grep of scene.ts finds
no palm / crab / castle / starfish / shell. The pass-2 "beach" is the revealed
StageFloor slab, NOT built props; the four gold coins are the pre-existing
scatter. The dominant identity gap is untouched.

final_score: 0.50 -> 0.53 (avg 1.667 -> 1.75; lowest axis still 0, so 0*0.7 +
1.75*0.3 = 0.525 ~= 0.53). The bar barely moved because the pinning 0 is
untouched.

PROCESS FINDINGS (honesty flags this pass owes):
1. RIGHT-AXIS: next axis is still correctly scene_props_completeness = 0 — still
   the lowest, still absent. Pass-2, like pass-1, spent its effort on secondary
   axes (composition/color via the water-shrink, subject via wood, lighting via
   sun) and left the pinning 0 untouched -> final_score is still ~floored. BUT
   this pass is NOT pure secondary churn: iteration-0's coupling note said the
   props axis will not read unless the stage first widens to expose margins.
   Pass-2 delivered exactly that STAGE-widening half (the beach apron). The PROPS
   half must now actually be built next — do not spend a third pass polishing
   secondaries while props stays 0.
2. PROMOTION: defensible this time. Unlike pass-1 (which moved no axis toward
   identity), pass-2 moved composition 1 -> 2 on a real geometry change and no
   non-attacked axis regressed >= 2 — so promoting the pass-2 image is honest.
   The intruding sky/ocean band is a minor off-reference deviation, not a >= 2
   regression.

ABSTRACTION-GATE: HONORED. The inset-pool / beach-margin was achieved by
reparameterizing an EXISTING presentation helper (stageRoom's `accentRadius`
default arg + a `WATER_RADIUS` const), reusing the existing StageFloor slab and
floor-ring meshes/materials — no new primitive / mesh / material / shader / field.
That is config-level, not a structural abstraction, so NO abstractions/NNNN.toml
is owed this pass. The beach PROPS (palm / crab / castle / starfish / shells)
remain un-built; per iteration 0, their abstraction record (inexpressible = true,
failed_attempts = [], smallest new prop-manifest surface) comes due WHEN they are
built, on the first attempt — do not burn ceremonial config nudges first.

PARITY-CLAIM: clean. No "done / matched / parity" language in the pass-2 commits,
scorecard, or ledger.
"""

[iteration.scorecard_before]
composition_and_framing = 1
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[[iteration]]
iteration = 3
attacked_axis = "none"           # reference RESET + branding capability build (not a scored pass)
changed_files = [
  "apps/casino-games/visual_targets/treasure-chest-pick/reference.png",
  "apps/casino-games/visual_targets/treasure-chest-pick/champion.png",
  "apps/casino-games/visual_targets/treasure-chest-pick/capture.md",
  "apps/casino-games/visual_targets/treasure-chest-pick/ledger.toml",
  "apps/casino-games/web/src/presentation/branding/*",
  "apps/casino-games/web/src/games/treasure-chest-pick/*",
  "apps/casino-games/web/src/workbench/workbench.ts",
  "apps/casino-games/web/browser/agent_capture.py",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "reference_reset"
abstraction_introduced = true
next_attacked_axis = "recalibrate"
reason = """
REFERENCE RESET. The campaign's reference.png was replaced: the old reference was
the UNBRANDED beach diorama; the new reference (1546x1017) is the same diorama
dressed in one white-label BRAND — "ACME" red across a top ribbon banner, a left
pennant, a right signboard, the sandcastle pennant, the crab's flag, and a label
on every chest. This is a NEW visual target, so every scorecard number below is
now STALE — the numbers are copied forward unchanged only to keep the append-only
record valid; they are NOT a re-score of the new reference. The next
/visual-convergence-propose CRITIC pass owes a fresh harsh calibration against
the branded reference (that is why next_attacked_axis = "recalibrate").

WHAT WAS BUILT (the champion now wears the ACME livery to match the new reference):
- A configurable BRAND (name + primary/onPrimary/ink colors) in the game's
  gameSpecific config: presentation/branding/brand.ts (DEFAULT_BRAND = ACME),
  validated in definition.ts, editable in the SET UP panel ("Brand livery").
- Brand lettering as real welded GEOMETRY — @axiom/web-engine has no textures, so
  a 5x7 box-run font (presentation/branding/glyphs.ts) is stamped onto surfaces by
  presentation/branding/label.ts THROUGH each surface's own transform frame. A
  chest's label therefore squashes, grows, tilts and spirals welded to the chest
  (it is part of the chest, not an overlay), and long names shrink uniformly to fit.
- Branded props: top ribbon banner, left pennant, right signboard, plus the
  recolored sandcastle pennant and a flag in the crab's claw — all brand-colored
  and carrying the brand name, placed on the sand OUTSIDE the lagoon and BEFORE the
  veil so a hero reveal dims them with the rest of the stage.
- Chest labels moved onto the LID TOP (not the foreshortened front): the tabletop
  camera looks down on the lids, so the top is where the name reads.

CAPTURE DETERMINISM: the chests-ready preset now runs phase:ready -> frozen -> shot.
The new `frozen` verb (agent_capture.py) blocks until the canvas backing store stops
changing (the sim reaching the shot=90 freeze), because shooting at phase:ready
(~tick 24) caught the still-animating idle dance / palm sway and made consecutive
captures differ by ~1% of pixels. With `frozen`, two consecutive captures are
byte-identical.

ABSTRACTION-GATE: abstraction_introduced = true. The branding capability
(brand value + geometry font + surface-weld label builder) is a genuinely NEW,
reusable presentation surface that did not exist — the engine cannot express text
at all (no textures), so lettering-as-geometry is the honest lowest-layer fix, and
the brand is plain serializable config traveling the existing import/export/validate
path. Built on the FIRST attempt (no ceremonial config nudges), at the app's
presentation tier where scene dressing belongs.

PARITY-CLAIM: clean. No "done / matched / parity" language — this entry only resets
the target and records the build; scoring is deferred to the next critic pass.
"""

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[[iteration]]
iteration = 4
attacked_axis = "none"           # reference RESET #2 + champion recapture (not a scored pass)
changed_files = [
  "apps/casino-games/visual_targets/treasure-chest-pick/reference.png",
  "apps/casino-games/visual_targets/treasure-chest-pick/champion.png",
  "apps/casino-games/visual_targets/treasure-chest-pick/capture.md",
  "apps/casino-games/visual_targets/treasure-chest-pick/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "reference_reset"
abstraction_introduced = false
next_attacked_axis = "recalibrate"
reason = """
REFERENCE RESET #2 (human-supplied, 2026-07-24). The campaign target was replaced
again. The retired reference was the FULLY BRANDED diorama (ACME on a top ribbon
banner, a left pennant, a right signboard, the sandcastle pennant, the crab's
flag, and a label on EVERY chest). The new reference (1614x974) is the CLEAN
diorama: warm tan sand filling the frame edge-to-edge, a rounded turquoise pool
with a hex-honeycomb surface and a paler shallow rim, nine chests staged 3x3 on
the water, and margin props (palm with a long soft shadow, red crab, sand castle
with a red pennant, starfish, seashells, pebbles) — with the brand appearing
EXACTLY ONCE, as a red rounded nameplate hovering above the CENTRE CHEST ONLY.

The direction of travel therefore inverted on the branding axis: the previous
target wanted MORE brand surfaces, the new one wants brand restraint. The app has
already moved that way independently on main (64b33ab6 center-only nameplate,
b364ddd5 removed the freestanding brand billboard), so the new champion is much
closer to the new target than the pass-3 champion was to the old one.

EVERY SCORECARD NUMBER BELOW IS STALE. They are copied forward unchanged only to
keep the append-only record valid; they are NOT a re-score against the new
reference. The next /visual-convergence-propose CRITIC pass owes a fresh harsh
calibration (next_attacked_axis = "recalibrate"). In particular the old
scene_props_completeness = 0 is certainly wrong now — the beach props were built
on the pass-3 line and have since landed on main.

CHAMPION RECAPTURED, not carried over. The prior champion.png was rendered from
commit 64b33ab6; main has since advanced to dc16f3b2 with four further
casino-games changes the old image does not show (9ebf3ba3 + 2b3dd5b3 + a8d65030
stylized hex-honeycomb water, e433bd47 richer water, 19b404d3 centre chest no
longer reads as permanently highlighted, b364ddd5 billboard removed, dc16f3b2
"orb" ring fix). Scoring a stale image against a new reference would have made
every number a fiction, so the champion was re-shot from the CURRENT app through
the unchanged `chests-ready` preset (same seed 470573198, same shot=90, same
canvas2d, same frozen gate), keeping the capture recipe — and therefore the
before/after comparability from here on — intact.

CAPTURE-RECIPE NOTE (cost a real debugging detour, now recorded in capture.md):
the app imports @axiom/web-engine's BUILT `dist/`, not its sources. A worktree
synced past a new engine export builds RED ("Module '@axiom/web-engine' has no
exported member 'drawStylizedWaterSurface'") until
`npm --prefix packages/axiom-web-engine run build` is run. That is a checkout
prerequisite, not an app bug.

ABSTRACTION-GATE: abstraction_introduced = false. This iteration changed no
render code whatsoever — only the target image, the recaptured champion, and the
bookkeeping. No new primitive / manifest field / shader is owed.

PARITY-CLAIM: clean. No "done / matched / parity" language; this entry only
resets the target and re-shoots the champion, and defers all scoring.
"""

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[[iteration]]
iteration = 5
attacked_axis = "none"           # CRITIC scoring recalibration — the one owed since iteration 3
changed_files = [
  "apps/casino-games/visual_targets/treasure-chest-pick/scorecard.champion.toml",
  "apps/casino-games/visual_targets/treasure-chest-pick/abstractions/0001.toml",
  "apps/casino-games/visual_targets/treasure-chest-pick/abstractions/0002.toml",
  "apps/casino-games/visual_targets/treasure-chest-pick/abstractions/0003.toml",
  "apps/casino-games/visual_targets/treasure-chest-pick/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
abstraction_introduced = false   # this entry BACKFILLS records; it introduces nothing
next_attacked_axis = "scene_props_completeness"
reason = """
CRITIC pass 5. Iterations 3 and 4 each reset reference.png and each deferred
scoring with next_attacked_axis = "recalibrate"; the scorecard has carried a
"STALE — DO NOT TRUST THE NUMBERS BELOW" banner for two passes. That debt is paid
here: all twelve axes are re-derived from scratch against the CURRENT reference
(the clean beach diorama, 1614x974) and the CURRENT champion (960x600, re-shot at
f233fe43 from the app at dc16f3b2). No number was carried over unexamined.

RESULT: final_score 1.2 (lowest 1 * 0.7 + average 1.667 * 0.3). Next axis =
scene_props_completeness (1) — lowest, and earliest in the fixed axis order among
the five axes tied at 1 (props, silhouette, material, lighting, detail_density).

THREE NUMBERS MOVED, in both directions — recalibration is not a synonym for
"score lower":
1. scene_props_completeness 0 -> 1 (RAISED). The standing 0 was a factual error
   against today's app. The palm, crab, castle, shells and pebbles were built on
   the pass-3 line, are on main, and are plainly visible in champion.png. They
   are crude proxies (plank-frond palm, pillar-cluster castle, blob crab, dot
   starfish) so this is a 1, not a 2 — but recording built geometry as absent
   corrupts the loop exactly as badly as inflation does.
2. silhouette_readability 2 -> 1 (LOWERED). The old 2 was written when the props
   did not exist and only the chest outlines were being graded. Now that they
   render, the frame's silhouettes are actively WRONG rather than merely missing.
   A wrong silhouette scores below a generous grade on the chests alone.
3. artifact_level 4 -> 3 (LOWERED). "Clean" no longer holds: the pool boundary is
   a hard DECAGON where the reference is a smooth circle (tessellation artifact on
   a shape meant to read round), and a hard-edged glow disc sits under the centre
   chest with no reference counterpart.

MEASURED, not eyeballed, where it mattered: sand 222,180,106 (champion, IDENTICAL
at both frame corners — no gradient, no grain) vs 224,169,90 -> 232,177,98
(reference); water 31,150,143 vs 49,158,158 (champion greener and darker, and
missing the reference's paler shallow rim ring entirely); luminance p2/median/p98
79/184/210 vs 80/163/199 (comparable RANGE, so contrast_and_exposure is not an
exposure blowout — it stays 2 because the champion's contrast comes from hard flat
steps rather than modelling plus real contact shadows).

ABSTRACTION-GATE: BROKEN, and now backfilled. The abstractions/ directory did not
exist. Three records were owed and none had been written:
 - 0001 beach-prop geometry (the scene_props_completeness attack). Ledger
   iteration 0 explicitly warned this record would come due when the props were
   built. They were built (4921ba29, 0685b30c, 4f9ea3e3, 24f535ef); the record
   was not.
 - 0002 the branding capability. Iteration 3 DECLARED abstraction_introduced =
   true and shipped with no NNNN.toml — the gate's one hard requirement, skipped
   in the same entry that invoked it. The record also has to cover what happened
   after: the lettering was PROMOTED into the shared spine
   (packages/axiom-web-engine/src/glyph-font.ts, text.ts) with no ledger entry at
   all.
 - 0003 the stylized water primitive. A NEW ENGINE-SPINE EXPORT
   (packages/axiom-web-engine/src/canvas-water.ts, drawStylizedWaterSurface) was
   added to serve this target across four commits (9ebf3ba3, a8d65030, 2b3dd5b3,
   e433bd47) with no ledger iteration and no record. It is the largest surface
   this campaign has added and it had the least paperwork.
On the MERITS all three are defensible: @axiom/web-engine has no textures and had
no text, and there is no beach-prop config surface, so all three are genuine
"cannot express" cases where the skill sanctions going structural on the first
attempt. None is too-early and none is ceremonial. What failed is the record, not
the judgement — so the backfills state plainly that they are backfills and do not
retroactively bless anything.

PROGRESS HONESTY:
 - No number-gaming found in this pass, and no silent >= 2 regression on a
   non-attacked axis.
 - But note what the numbers now show: three engine/app structural abstractions
   have landed, and the two axes they exist to serve are STILL 1
   (material_and_texture_detail, and scene_props_completeness). Going structural
   is correct against a parity target; going structural three times without
   scoring the result is how a campaign convinces itself it is converging. Every
   future structural change on this target ships with its NNNN.toml and a scored
   ledger entry in the SAME commit.
 - The standing procedural finding from iterations 1 and 2 still holds and is
   re-stated: passes keep spending on secondary axes while the lowest axis is
   left where it is. The next pass must attack scene_props_completeness — remodel
   the palm (tapered drooping fronds, coconuts, grass tuft, long cast shadow), the
   castle (crenellated walls, corner turrets, arched doorway) and the crab, and
   give the starfish actual arms. Nothing else.

PARITY-CLAIM: clean. No "done / matched / parity" language anywhere in the
scorecard, the ledger, the backfilled abstraction records, or the campaign
commits. The champion is at final_score 1.2 out of 5; it is a legible flat proxy
of the reference, and nothing in this record says otherwise.
"""

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 2
scene_props_completeness = 0
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 4

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 2
scene_props_completeness = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 3
