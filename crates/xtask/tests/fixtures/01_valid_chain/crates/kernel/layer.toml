# Fixture base layer (index 0). Reused, with the same shape, across fixtures.
[layer]
name = "kernel"
crate_name = "kernel"
depends_on = []
meaningful_dependency = "Fixture base substrate layer."
introduced_capabilities = ["KernelClock", "KernelResult"]
consumed_capabilities = []
