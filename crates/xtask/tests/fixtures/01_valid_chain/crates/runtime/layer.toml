# A valid layer 1: imports the previous layer's public export and proves it.
[layer]
name = "runtime"
crate_name = "runtime"
depends_on = ["kernel"]
meaningful_dependency = "Runtime adapts the deterministic kernel clock into engine stepping."
introduced_capabilities = ["Runtime"]
consumed_capabilities = ["KernelClock"]

[[proof_exports]]
export = "Runtime"
must_reference = ["KernelClock"]
