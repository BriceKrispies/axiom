# Axiom State Law — current-repository audit

Generated by `uv run scripts/retained_state_audit.py`, which runs the engine dylint rulebook twice and keeps the `engine_no_retained_state` findings:

* **native** — `cargo dylint --all -- --all-targets`
* **wasm32** — `cargo dylint --all -- --target wasm32-unknown-unknown --lib`

The second pass exists because a lint only sees what the compiler compiles: the platform-facing modules' browser arms are `#[cfg(target_arch = "wasm32")]` and are entirely invisible to the canonical native invocation. The **Arm** column records which pass saw each finding.

**This is an inventory for a later migration task, not an allowed baseline.** `engine_no_retained_state` has deliberately no entry in `tools/lints/dylint-baseline.txt`: the only acceptable count for a zero-tolerance architectural law is zero, so the dylint gate fails on this lint until the engine is migrated. Nothing below is exempted, grandfathered, or `#[allow]`-ed.

Counts are **distinct source sites**, deduplicated on (file, line, column, message): `--all-targets` compiles each crate's lib, tests, examples and benches, so one line is linted several times. This is not the number `scripts/dylint-gate.sh` counts (that one counts compilation units emitting the lint).

**Total: 787 findings across 49 spine packages.**

## Totals by category

| Category | Findings |
| --- | ---: |
| `mutable-engine-api` | 693 |
| `shared-state-ownership` | 42 |
| `generic-behavior-state` | 25 |
| `retained-execution-state` | 13 |
| `interior-mutability` | 5 |
| `opaque-behavior-state` | 5 |
| `stateful-callback-boundary` | 2 |
| `static-storage` | 1 |
| `thread-local-storage` | 1 |
| **total** | **787** |

## Totals by compilation arm

| Arm | Findings |
| --- | ---: |
| `both` | 709 |
| `wasm32` | 78 |
| **total** | **787** |

## Totals by package

| Package | Findings |
| --- | ---: |
| `modules/axiom-sim-core` | 100 |
| `modules/axiom-windowing` | 67 |
| `crates/axiom-ecs` | 62 |
| `crates/axiom-kernel` | 46 |
| `modules/axiom` | 41 |
| `modules/axiom-scene` | 40 |
| `modules/axiom-text` | 33 |
| `crates/axiom-runtime` | 32 |
| `modules/axiom-animation-authoring` | 32 |
| `crates/axiom-interface` | 31 |
| `modules/axiom-render` | 21 |
| `modules/axiom-draw2d` | 19 |
| `crates/axiom-math` | 18 |
| `modules/axiom-physics` | 17 |
| `modules/axiom-webgpu` | 17 |
| `crates/axiom-host` | 14 |
| `modules/axiom-agent` | 14 |
| `modules/axiom-tick` | 14 |
| `modules/axiom-gpu-backend` | 13 |
| `modules/axiom-audio` | 12 |
| `modules/axiom-debug-overlay` | 12 |
| `modules/axiom-resources` | 12 |
| `modules/axiom-netcode` | 10 |
| `modules/axiom-physical-animation` | 10 |
| `crates/axiom-entropy` | 9 |
| `modules/axiom-animation` | 9 |
| `modules/axiom-canvas2d-backend` | 9 |
| `modules/axiom-client-core` | 7 |
| `modules/axiom-grid` | 7 |
| `modules/axiom-render-pipeline` | 7 |
| `modules/axiom-recording` | 6 |
| `crates/axiom-frame` | 5 |
| `crates/axiom-proc` | 5 |
| `crates/axiom-crypto` | 4 |
| `crates/axiom-introspect` | 4 |
| `crates/axiom-proc-core` | 3 |
| `crates/axiom-recipe` | 3 |
| `modules/axiom-input` | 3 |
| `modules/axiom-tween` | 3 |
| `crates/axiom-layout` | 2 |
| `crates/axiom-proc-validate` | 2 |
| `modules/axiom-assets` | 2 |
| `modules/axiom-streaming` | 2 |
| `modules/axiom-terrain-mesh` | 2 |
| `modules/axiom-world` | 2 |
| `crates/axiom-hydrology` | 1 |
| `crates/axiom-proc-texture` | 1 |
| `modules/axiom-forest` | 1 |
| `modules/axiom-perception` | 1 |
| **total** | **787** |

## Findings

### `crates/axiom-crypto` — 4 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-crypto/src/signature.rs` | 31 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-crypto/src/signature.rs` | 41 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-crypto/src/verifying_key.rs` | 47 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-crypto/src/verifying_key.rs` | 58 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-ecs` — 62 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-ecs/src/column_set.rs` | 17 | both | `opaque-behavior-state` | return type exposes `dyn erased_column::ErasedColumn` on a public engine boundary | this hands executable behavior across the boundary, and that behavior may capture and retain state the engine cannot see |
| `crates/axiom-ecs/src/column_set.rs` | 20 | both | `mutable-engine-api` | parameter type takes or returns `&mut Self` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/column_set.rs` | 20 | both | `opaque-behavior-state` | return type exposes `dyn erased_column::ErasedColumn` on a public engine boundary | this hands executable behavior across the boundary, and that behavior may capture and retain state the engine cannot see |
| `crates/axiom-ecs/src/column_set.rs` | 26 | both | `mutable-engine-api` | parameter type takes or returns `&mut Self` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/command_buffer.rs` | 135 | both | `mutable-engine-api` | parameter type takes or returns `&mut command_buffer::CommandBuffer` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/command_buffer.rs` | 145 | both | `mutable-engine-api` | parameter type takes or returns `&mut command_buffer::CommandBuffer` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/command_buffer.rs` | 166 | both | `mutable-engine-api` | parameter type takes or returns `&mut command_buffer::CommandBuffer` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/command_buffer.rs` | 166 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_column.rs` | 28 | both | `mutable-engine-api` | parameter type takes or returns `&mut component_column::ComponentColumn<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_column.rs` | 38 | both | `mutable-engine-api` | parameter type takes or returns `&mut component_column::ComponentColumn<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_column.rs` | 38 | both | `mutable-engine-api` | return type takes or returns `&mut T` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_column.rs` | 48 | both | `mutable-engine-api` | parameter type takes or returns `&mut component_column::ComponentColumn<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_column.rs` | 58 | both | `mutable-engine-api` | parameter type takes or returns `&mut component_column::ComponentColumn<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_column.rs` | 87 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_column.rs` | 96 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_command_buffer.rs` | 46 | both | `mutable-engine-api` | parameter type takes or returns `&mut component_command_buffer::ComponentCommandBuffer<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_command_buffer.rs` | 69 | both | `mutable-engine-api` | parameter type takes or returns `&mut component_command_buffer::ComponentCommandBuffer<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_command_buffer.rs` | 96 | both | `mutable-engine-api` | parameter type takes or returns `&mut component_command_buffer::ComponentCommandBuffer<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/component_command_buffer.rs` | 96 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/dynamic_components.rs` | 94 | both | `mutable-engine-api` | parameter type takes or returns `&mut dynamic_components::DynamicComponents` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/dynamic_components.rs` | 118 | both | `mutable-engine-api` | parameter type takes or returns `&mut dynamic_components::DynamicComponents` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/dynamic_components.rs` | 173 | both | `mutable-engine-api` | parameter type takes or returns `&mut dynamic_components::DynamicComponents` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/entity_registry.rs` | 43 | both | `mutable-engine-api` | parameter type takes or returns `&mut entity_registry::EntityRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/entity_registry.rs` | 54 | both | `mutable-engine-api` | parameter type takes or returns `&mut entity_registry::EntityRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/entity_registry.rs` | 62 | both | `mutable-engine-api` | parameter type takes or returns `&mut entity_registry::EntityRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/entity_registry.rs` | 73 | both | `mutable-engine-api` | parameter type takes or returns `&mut entity_registry::EntityRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/entity_registry.rs` | 126 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/entity_registry.rs` | 140 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/erased_column.rs` | 23 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/erased_column.rs` | 26 | both | `mutable-engine-api` | parameter type takes or returns `&mut Self` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/erased_column.rs` | 26 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/erased_column.rs` | 30 | both | `mutable-engine-api` | parameter type takes or returns `&mut Self` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/event_buffer.rs` | 23 | both | `mutable-engine-api` | parameter type takes or returns `&mut event_buffer::EventBuffer<E>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/event_buffer.rs` | 28 | both | `mutable-engine-api` | parameter type takes or returns `&mut event_buffer::EventBuffer<E>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/event_buffer.rs` | 33 | both | `mutable-engine-api` | parameter type takes or returns `&mut event_buffer::EventBuffer<E>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/query.rs` | 100 | both | `mutable-engine-api` | parameter type takes or returns `&'a mut component_column::ComponentColumn<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/replay_log.rs` | 36 | both | `mutable-engine-api` | parameter type takes or returns `&mut replay_log::ReplayLog` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/replay_log.rs` | 44 | both | `mutable-engine-api` | parameter type takes or returns `&mut replay_log::ReplayLog` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/replay_log.rs` | 62 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/tracked_column.rs` | 48 | both | `mutable-engine-api` | parameter type takes or returns `&mut tracked_column::TrackedColumn<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/tracked_column.rs` | 62 | both | `mutable-engine-api` | parameter type takes or returns `&mut tracked_column::TrackedColumn<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/tracked_column.rs` | 62 | both | `mutable-engine-api` | return type takes or returns `&mut T` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/tracked_column.rs` | 70 | both | `mutable-engine-api` | parameter type takes or returns `&mut tracked_column::TrackedColumn<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/tracked_column.rs` | 92 | both | `mutable-engine-api` | parameter type takes or returns `&mut tracked_column::TrackedColumn<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 67 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 67 | both | `opaque-behavior-state` | parameter type exposes `(dyn world_system::WorldSystem<S> + 'static)` on a public engine boundary | this hands executable behavior across the boundary, and that behavior may capture and retain state the engine cannot see |
| `crates/axiom-ecs/src/world.rs` | 75 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 75 | both | `opaque-behavior-state` | parameter type exposes `(dyn world_system::WorldSystem<S> + 'static)` on a public engine boundary | this hands executable behavior across the boundary, and that behavior may capture and retain state the engine cannot see |
| `crates/axiom-ecs/src/world.rs` | 90 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 95 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 110 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 110 | both | `mutable-engine-api` | return type takes or returns `&mut S` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 133 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 169 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 179 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 190 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 214 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 214 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 259 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 271 | both | `mutable-engine-api` | parameter type takes or returns `&mut world::World<S>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world.rs` | 271 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-ecs/src/world_system.rs` | 16 | both | `mutable-engine-api` | parameter type takes or returns `&mut S` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-entropy` — 9 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-entropy/src/entropy_stream.rs` | 35 | both | `mutable-engine-api` | parameter type takes or returns `&mut entropy_stream::EntropyStream` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-entropy/src/entropy_stream.rs` | 40 | both | `mutable-engine-api` | parameter type takes or returns `&mut entropy_stream::EntropyStream` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-entropy/src/entropy_stream.rs` | 55 | both | `mutable-engine-api` | parameter type takes or returns `&mut entropy_stream::EntropyStream` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-entropy/src/entropy_stream.rs` | 63 | both | `mutable-engine-api` | parameter type takes or returns `&mut entropy_stream::EntropyStream` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-entropy/src/entropy_stream.rs` | 70 | both | `mutable-engine-api` | parameter type takes or returns `&mut entropy_stream::EntropyStream` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-entropy/src/entropy_stream.rs` | 77 | both | `mutable-engine-api` | parameter type takes or returns `&mut entropy_stream::EntropyStream` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-entropy/src/entropy_stream.rs` | 85 | both | `mutable-engine-api` | parameter type takes or returns `&mut entropy_stream::EntropyStream` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-entropy/src/entropy_stream.rs` | 101 | both | `mutable-engine-api` | parameter type takes or returns `&mut entropy_stream::EntropyStream` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-entropy/src/entropy_stream.rs` | 101 | both | `mutable-engine-api` | parameter type takes or returns `&mut [T]` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-frame` — 5 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-frame/src/frame_accumulator.rs` | 51 | both | `mutable-engine-api` | parameter type takes or returns `&mut frame_accumulator::FrameAccumulator` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-frame/src/frame_builder.rs` | 71 | both | `mutable-engine-api` | parameter type takes or returns `&mut frame_builder::FrameBuilder` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-frame/src/frame_command_queue.rs` | 37 | both | `mutable-engine-api` | parameter type takes or returns `&mut frame_command_queue::FrameCommandQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-frame/src/frame_command_queue.rs` | 49 | both | `mutable-engine-api` | parameter type takes or returns `&mut frame_command_queue::FrameCommandQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-frame/src/frame_command_queue.rs` | 55 | both | `mutable-engine-api` | parameter type takes or returns `&mut frame_command_queue::FrameCommandQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-host` — 14 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-host/src/draw2d_list.rs` | 44 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_list::Draw2dList` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/draw2d_list.rs` | 49 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_list::Draw2dList` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/draw2d_list.rs` | 55 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_list::Draw2dList` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/draw2d_list.rs` | 64 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_list::Draw2dList` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/draw2d_list.rs` | 72 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_list::Draw2dList` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/draw2d_list.rs` | 86 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_list::Draw2dList` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/draw2d_list.rs` | 100 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_list::Draw2dList` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/frame_postprocess.rs` | 249 | both | `mutable-engine-api` | parameter type takes or returns `&mut [u8]` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/frame_retro_32bit.rs` | 249 | both | `mutable-engine-api` | parameter type takes or returns `&mut [u8]` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/frame_volumetrics.rs` | 155 | both | `mutable-engine-api` | parameter type takes or returns `&mut [u8]` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/host_step_driver.rs` | 44 | both | `mutable-engine-api` | parameter type takes or returns `&mut host_step_driver::HostStepDriver` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/host_step_driver.rs` | 78 | both | `mutable-engine-api` | parameter type takes or returns `&mut host_step_driver::HostStepDriver` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/host_step_driver.rs` | 79 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_runtime::Runtime` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-host/src/render_scale.rs` | 209 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_scale::RenderScaleController` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-hydrology` — 1 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-hydrology/src/pit_fill.rs` | 34 | both | `mutable-engine-api` | parameter type takes or returns `&mut [axiom_kernel::Meters]` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-interface` — 31 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-interface/src/command_table.rs` | 50 | both | `mutable-engine-api` | parameter type takes or returns `&mut C` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 39 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 52 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 59 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 66 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 80 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 84 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 91 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 99 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 107 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 129 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 137 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 150 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 157 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 167 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 176 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 198 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 204 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 213 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 218 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 231 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 258 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 265 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 282 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 298 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/interface_api.rs` | 305 | both | `mutable-engine-api` | parameter type takes or returns `&mut interface_api::InterfaceApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/ui_surface.rs` | 80 | both | `mutable-engine-api` | parameter type takes or returns `&mut ui_surface::UiSurface` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/ui_surface.rs` | 87 | both | `mutable-engine-api` | parameter type takes or returns `&mut ui_surface::UiSurface` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/ui_surface.rs` | 92 | both | `mutable-engine-api` | parameter type takes or returns `&mut ui_surface::UiSurface` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/ui_surface.rs` | 100 | both | `mutable-engine-api` | parameter type takes or returns `&mut ui_surface::UiSurface` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-interface/src/ui_surface.rs` | 107 | both | `mutable-engine-api` | parameter type takes or returns `&mut ui_surface::UiSurface` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-introspect` — 4 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-introspect/src/frame_history.rs` | 37 | both | `mutable-engine-api` | parameter type takes or returns `&mut frame_history::FrameHistory` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-introspect/src/introspect_api.rs` | 40 | both | `mutable-engine-api` | parameter type takes or returns `&mut introspect_api::IntrospectApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-introspect/src/introspect_api.rs` | 47 | both | `mutable-engine-api` | parameter type takes or returns `&mut introspect_api::IntrospectApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-introspect/src/introspect_api.rs` | 126 | both | `mutable-engine-api` | parameter type takes or returns `&mut introspect_api::IntrospectApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-kernel` — 46 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-kernel/src/binary_reader.rs` | 53 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 58 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 63 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 69 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 75 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 81 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 87 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 94 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 103 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 112 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_reader.rs` | 142 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'a>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_writer.rs` | 22 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_writer.rs` | 27 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_writer.rs` | 32 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_writer.rs` | 37 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_writer.rs` | 42 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_writer.rs` | 47 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_writer.rs` | 52 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_writer.rs` | 57 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/binary_writer.rs` | 63 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/deterministic_rng.rs` | 45 | both | `mutable-engine-api` | parameter type takes or returns `&mut deterministic_rng::DeterministicRng` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/deterministic_rng.rs` | 55 | both | `mutable-engine-api` | parameter type takes or returns `&mut deterministic_rng::DeterministicRng` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/deterministic_rng.rs` | 63 | both | `mutable-engine-api` | parameter type takes or returns `&mut deterministic_rng::DeterministicRng` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/facade.rs` | 67 | both | `mutable-engine-api` | parameter type takes or returns `&mut impl LogSink` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/facade.rs` | 73 | both | `mutable-engine-api` | parameter type takes or returns `&mut impl TelemetrySink` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/in_memory_log_sink.rs` | 40 | both | `mutable-engine-api` | parameter type takes or returns `&mut in_memory_log_sink::InMemoryLogSink` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/in_memory_telemetry_sink.rs` | 50 | both | `mutable-engine-api` | parameter type takes or returns `&mut in_memory_telemetry_sink::InMemoryTelemetrySink` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/log_sink.rs` | 13 | both | `mutable-engine-api` | parameter type takes or returns `&mut Self` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/message_queue.rs` | 25 | both | `mutable-engine-api` | parameter type takes or returns `&mut message_queue::MessageQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/message_queue.rs` | 30 | both | `mutable-engine-api` | parameter type takes or returns `&mut message_queue::MessageQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/message_queue.rs` | 50 | both | `mutable-engine-api` | parameter type takes or returns `&mut message_queue::MessageQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/reflect.rs` | 23 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/reflect.rs` | 26 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/replay_timeline.rs` | 42 | both | `mutable-engine-api` | parameter type takes or returns `&mut replay_timeline::ReplayTimeline<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/replay_timeline.rs` | 49 | both | `mutable-engine-api` | parameter type takes or returns `&mut replay_timeline::ReplayTimeline<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/replay_timeline.rs` | 86 | both | `mutable-engine-api` | parameter type takes or returns `&mut replay_timeline::ReplayTimeline<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/replay_timeline.rs` | 91 | both | `mutable-engine-api` | parameter type takes or returns `&mut replay_timeline::ReplayTimeline<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/schema_version.rs` | 41 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_writer::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/schema_version.rs` | 47 | both | `mutable-engine-api` | parameter type takes or returns `&mut binary_reader::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/simulation_clock.rs` | 41 | both | `mutable-engine-api` | parameter type takes or returns `&mut simulation_clock::SimulationClock` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/simulation_clock.rs` | 62 | both | `mutable-engine-api` | parameter type takes or returns `&mut simulation_clock::SimulationClock` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/telemetry_sink.rs` | 14 | both | `mutable-engine-api` | parameter type takes or returns `&mut Self` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/tick_divider.rs` | 53 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_divider::TickDivider` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/tick_schedule.rs` | 58 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_schedule::TickSchedule<Id, P>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/tick_schedule.rs` | 66 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_schedule::TickSchedule<Id, P>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-kernel/src/tick_schedule.rs` | 81 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_schedule::TickSchedule<Id, P>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-layout` — 2 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-layout/src/tree.rs` | 75 | both | `mutable-engine-api` | parameter type takes or returns `&mut tree::LayoutTreeBuilder` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-layout/src/tree.rs` | 82 | both | `mutable-engine-api` | parameter type takes or returns `&mut tree::LayoutTreeBuilder` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-math` — 18 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-math/src/aabb.rs` | 141 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/aabb.rs` | 147 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/mat4.rs` | 419 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/mat4.rs` | 426 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/math_api.rs` | 232 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_runtime::RuntimeContext<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/math_api.rs` | 244 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_runtime::RuntimeContext<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/quat.rs` | 290 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/quat.rs` | 298 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/sphere.rs` | 90 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/sphere.rs` | 96 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/transform.rs` | 181 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/transform.rs` | 188 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/vec2.rs` | 96 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/vec2.rs` | 102 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/vec3.rs` | 133 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/vec3.rs` | 140 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/vec4.rs` | 89 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-math/src/vec4.rs` | 97 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-proc` — 5 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-proc/src/evaluation.rs` | 53 | both | `mutable-engine-api` | parameter type takes or returns `&mut evaluation::Evaluation` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-proc/src/recipe.rs` | 38 | both | `mutable-engine-api` | parameter type takes or returns `&mut recipe::Recipe` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-proc/src/recipe.rs` | 43 | both | `mutable-engine-api` | parameter type takes or returns `&mut recipe::Recipe` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-proc/src/recipe.rs` | 48 | both | `mutable-engine-api` | parameter type takes or returns `&mut recipe::Recipe` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-proc/src/recipe.rs` | 53 | both | `mutable-engine-api` | parameter type takes or returns `&mut recipe::Recipe` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-proc-core` — 3 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-proc-core/src/node_eval.rs` | 52 | both | `mutable-engine-api` | parameter type takes or returns `&mut node_eval::NodeEval<'a, Out>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-proc-core/src/node_eval.rs` | 52 | both | `mutable-engine-api` | return type takes or returns `&mut axiom_entropy::EntropyStream` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-proc-core/src/proc_core.rs` | 40 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

### `crates/axiom-proc-texture` — 1 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-proc-texture/src/texture_buffer.rs` | 20 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

### `crates/axiom-proc-validate` — 2 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-proc-validate/src/sampling.rs` | 35 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `crates/axiom-proc-validate/src/sampling.rs` | 36 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

### `crates/axiom-recipe` — 3 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-recipe/src/recipe_graph.rs` | 43 | both | `mutable-engine-api` | parameter type takes or returns `&mut recipe_graph::RecipeGraph` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-recipe/src/recipe_graph.rs` | 93 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-recipe/src/recipe_graph.rs` | 103 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `crates/axiom-runtime` — 32 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `crates/axiom-runtime/src/runtime.rs` | 79 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime::Runtime` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime.rs` | 92 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime::Runtime` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime.rs` | 105 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime::Runtime` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime.rs` | 116 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime::Runtime` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime.rs` | 144 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime::Runtime` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime.rs` | 258 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime::Runtime` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime.rs` | 258 | both | `mutable-engine-api` | return type takes or returns `&mut runtime_scheduler::RuntimeScheduler` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_command_queue.rs` | 27 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_command_queue::RuntimeCommandQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_command_queue.rs` | 32 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_command_queue::RuntimeCommandQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_command_queue.rs` | 52 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_command_queue::RuntimeCommandQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_command_queue.rs` | 57 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_command_queue::RuntimeCommandQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 30 | both | `mutable-engine-api` | parameter type takes or returns `&'r mut runtime_command_queue::RuntimeCommandQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 31 | both | `mutable-engine-api` | parameter type takes or returns `&'r mut runtime_event_queue::RuntimeEventQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 33 | both | `mutable-engine-api` | parameter type takes or returns `&'r mut axiom_kernel::InMemoryLogSink` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 34 | both | `mutable-engine-api` | parameter type takes or returns `&'r mut axiom_kernel::InMemoryTelemetrySink` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 61 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_context::RuntimeContext<'r>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 61 | both | `mutable-engine-api` | return type takes or returns `&mut runtime_command_queue::RuntimeCommandQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 69 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_context::RuntimeContext<'r>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 69 | both | `mutable-engine-api` | return type takes or returns `&mut runtime_event_queue::RuntimeEventQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 76 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_context::RuntimeContext<'r>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_context.rs` | 82 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_context::RuntimeContext<'r>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_event_queue.rs` | 25 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_event_queue::RuntimeEventQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_event_queue.rs` | 29 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_event_queue::RuntimeEventQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_event_queue.rs` | 46 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_event_queue::RuntimeEventQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_event_queue.rs` | 50 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_event_queue::RuntimeEventQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_scheduler.rs` | 71 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_scheduler::RuntimeScheduler` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_scheduler.rs` | 75 | both | `opaque-behavior-state` | parameter type exposes `(dyn runtime_system::RuntimeSystem + 'static)` on a public engine boundary | this hands executable behavior across the boundary, and that behavior may capture and retain state the engine cannot see |
| `crates/axiom-runtime/src/runtime_scheduler.rs` | 134 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_scheduler::RuntimeScheduler` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_scheduler.rs` | 135 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_context::RuntimeContext<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_system.rs` | 18 | both | `mutable-engine-api` | parameter type takes or returns `&mut Self` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_system.rs` | 18 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_context::RuntimeContext<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `crates/axiom-runtime/src/runtime_timeline.rs` | 45 | both | `mutable-engine-api` | parameter type takes or returns `&mut runtime_timeline::RuntimeTimeline` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom` — 41 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom/src/app.rs` | 123 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnOnce` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom/src/app.rs` | 436 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app.rs` | 438 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnOnce` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom/src/app.rs` | 469 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app.rs` | 499 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 57 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 73 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 89 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 116 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 133 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 154 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 180 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 205 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 249 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/authoring.rs` | 263 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/components.rs` | 22 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/dynamic_world.rs` | 29 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/dynamic_world.rs` | 37 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/dynamic_world.rs` | 55 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/dynamic_world.rs` | 69 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/frame.rs` | 20 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/frame.rs` | 29 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/frame.rs` | 41 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/frame.rs` | 58 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/frame.rs` | 102 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/queries.rs` | 46 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/queries.rs` | 60 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/queries.rs` | 86 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/queries.rs` | 131 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/queries.rs` | 142 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/queries.rs` | 163 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/render_look.rs` | 14 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/render_look.rs` | 22 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/render_look.rs` | 37 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/render_look.rs` | 50 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/render_look.rs` | 67 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/app/render_look.rs` | 84 | both | `mutable-engine-api` | parameter type takes or returns `&mut app::RunningApp` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/assets.rs` | 24 | both | `mutable-engine-api` | parameter type takes or returns `&mut assets::Assets<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/bundle.rs` | 261 | both | `mutable-engine-api` | parameter type takes or returns `&mut bundle::SpawnCommand` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/component.rs` | 29 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_scene::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom/src/scene_commands.rs` | 45 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_commands::SceneCommands` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-agent` — 14 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-agent/src/action_queue.rs` | 35 | both | `mutable-engine-api` | parameter type takes or returns `&mut action_queue::ActionQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/action_queue.rs` | 47 | both | `mutable-engine-api` | parameter type takes or returns `&mut action_queue::ActionQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/agent_api.rs` | 141 | both | `mutable-engine-api` | parameter type takes or returns `&mut reaction_buffer::ReactionBuffer` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/agent_api.rs` | 357 | both | `mutable-engine-api` | parameter type takes or returns `&mut B` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/agent_api.rs` | 359 | both | `mutable-engine-api` | parameter type takes or returns `&mut agent_memory::AgentMemory` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/agent_brain.rs` | 65 | both | `mutable-engine-api` | parameter type takes or returns `&mut Self` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/agent_memory.rs` | 68 | both | `mutable-engine-api` | parameter type takes or returns `&mut agent_memory::AgentMemory` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/agent_memory.rs` | 76 | both | `mutable-engine-api` | parameter type takes or returns `&mut agent_memory::AgentMemory` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/agent_runtime.rs` | 34 | both | `mutable-engine-api` | parameter type takes or returns `&mut B` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/agent_runtime.rs` | 36 | both | `mutable-engine-api` | parameter type takes or returns `&mut agent_memory::AgentMemory` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/observation_builder.rs` | 60 | both | `mutable-engine-api` | parameter type takes or returns `&mut observation_builder::ObservationBuilder` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/observation_builder.rs` | 67 | both | `mutable-engine-api` | parameter type takes or returns `&mut observation_builder::ObservationBuilder` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/observation_builder.rs` | 74 | both | `mutable-engine-api` | parameter type takes or returns `&mut observation_builder::ObservationBuilder` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-agent/src/reaction_buffer.rs` | 81 | both | `mutable-engine-api` | parameter type takes or returns `&mut reaction_buffer::ReactionBuffer` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-animation` — 9 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-animation/src/animation_api.rs` | 39 | both | `mutable-engine-api` | parameter type takes or returns `&mut animation_api::AnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation/src/animation_api.rs` | 47 | both | `mutable-engine-api` | parameter type takes or returns `&mut animation_api::AnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation/src/animation_api.rs` | 57 | both | `mutable-engine-api` | parameter type takes or returns `&mut animation_api::AnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation/src/animation_api.rs` | 72 | both | `mutable-engine-api` | parameter type takes or returns `&mut animation_api::AnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation/src/animation_api.rs` | 81 | both | `mutable-engine-api` | parameter type takes or returns `&mut animation_api::AnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation/src/animation_api.rs` | 95 | both | `mutable-engine-api` | parameter type takes or returns `&mut animation_api::AnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation/src/animation_api.rs` | 101 | both | `mutable-engine-api` | parameter type takes or returns `&mut animation_api::AnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation/src/animation_api.rs` | 241 | both | `mutable-engine-api` | parameter type takes or returns `&mut animation_api::AnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation/src/animation_api.rs` | 265 | both | `mutable-engine-api` | parameter type takes or returns `&mut animation_api::AnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-animation-authoring` — 32 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 52 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 88 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 104 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 114 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 121 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 133 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 143 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 149 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 155 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 160 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 166 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 171 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 177 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 189 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 200 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 211 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 228 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 238 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 249 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 260 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 271 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 290 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 361 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 371 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 378 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 388 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 399 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 410 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 423 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 435 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/authoring_api.rs` | 460 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-animation-authoring/src/penalty_kick.rs` | 46 | both | `mutable-engine-api` | parameter type takes or returns `&mut authoring_api::AnimationAuthoringApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-assets` — 2 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-assets/src/assets_api.rs` | 42 | both | `mutable-engine-api` | parameter type takes or returns `&mut assets_api::AssetsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-assets/src/assets_api.rs` | 51 | both | `mutable-engine-api` | parameter type takes or returns `&mut assets_api::AssetsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-audio` — 12 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-audio/src/audio_api.rs` | 250 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 261 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 267 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 281 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 288 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 304 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 321 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 342 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 369 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 374 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api.rs` | 388 | both | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-audio/src/audio_api/web.rs` | 29 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut audio_api::AudioApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-canvas2d-backend` — 9 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-canvas2d-backend/src/canvas2d_backend_api.rs` | 99 | both | `mutable-engine-api` | parameter type takes or returns `&mut canvas2d_backend_api::Canvas2dBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-canvas2d-backend/src/canvas2d_backend_api.rs` | 107 | both | `mutable-engine-api` | parameter type takes or returns `&mut canvas2d_backend_api::Canvas2dBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-canvas2d-backend/src/canvas2d_backend_api.rs` | 139 | both | `mutable-engine-api` | parameter type takes or returns `&mut canvas2d_backend_api::Canvas2dBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-canvas2d-backend/src/canvas2d_backend_api.rs` | 150 | both | `mutable-engine-api` | parameter type takes or returns `&mut canvas2d_backend_api::Canvas2dBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-canvas2d-backend/src/canvas2d_backend_api.rs` | 191 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut canvas2d_backend_api::Canvas2dBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-canvas2d-backend/src/canvas2d_backend_api.rs` | 205 | both | `mutable-engine-api` | parameter type takes or returns `&mut canvas2d_backend_api::Canvas2dBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-canvas2d-backend/src/canvas2d_backend_api.rs` | 213 | both | `mutable-engine-api` | parameter type takes or returns `&mut canvas2d_backend_api::Canvas2dBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-canvas2d-backend/src/frame_packet_raster.rs` | 286 | wasm32 | `thread-local-storage` | `thread_local!` declares ambient per-thread storage | a thread-local slot is read and written by code that never names it in a signature, so the same call can observe different values on different threads |
| `modules/axiom-canvas2d-backend/src/frame_telemetry.rs` | 42 | wasm32 | `static-storage` | `static` item declares process-wide storage | a static outlives every call, so information written into it is remembered across invocations without appearing in any signature |

### `modules/axiom-client-core` — 7 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-client-core/src/client_core_api.rs` | 61 | both | `mutable-engine-api` | parameter type takes or returns `&mut client_core_api::ClientCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-client-core/src/client_core_api.rs` | 70 | both | `mutable-engine-api` | parameter type takes or returns `&mut client_core_api::ClientCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-client-core/src/client_core_api.rs` | 85 | both | `mutable-engine-api` | parameter type takes or returns `&mut client_core_api::ClientCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-client-core/src/client_core_api.rs` | 106 | both | `mutable-engine-api` | parameter type takes or returns `&mut client_core_api::ClientCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-client-core/src/client_core_api.rs` | 124 | both | `mutable-engine-api` | parameter type takes or returns `&mut client_core_api::ClientCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-client-core/src/client_core_api.rs` | 202 | both | `mutable-engine-api` | parameter type takes or returns `&mut client_core_api::ClientCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-client-core/src/client_core_api.rs` | 229 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

### `modules/axiom-debug-overlay` — 12 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-debug-overlay/src/dom_binding.rs` | 158 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::RefCell<overlay_state::OverlayState>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-debug-overlay/src/dom_binding.rs` | 178 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::RefCell<overlay_state::OverlayState>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-debug-overlay/src/dom_binding.rs` | 217 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::RefCell<overlay_state::OverlayState>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-debug-overlay/src/dom_binding.rs` | 504 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::RefCell<overlay_state::OverlayState>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-debug-overlay/src/dom_binding.rs` | 583 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::RefCell<overlay_state::OverlayState>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-debug-overlay/src/dom_binding.rs` | 608 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::RefCell<overlay_state::OverlayState>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-debug-overlay/src/dom_binding.rs` | 620 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::RefCell<overlay_state::OverlayState>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-debug-overlay/src/measured_diagnostics.rs` | 26 | wasm32 | `shared-state-ownership` | type alias contains `std::rc::Rc<std::cell::RefCell<std::option::Option<wasm_bindgen::prelude::ScopedClosure<'static, (dyn std::ops::FnMut() + 'static)>>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-debug-overlay/src/overlay_api.rs` | 25 | both | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::RefCell<overlay_state::OverlayState>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-debug-overlay/src/overlay_api.rs` | 277 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut overlay_api::DebugOverlayApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-debug-overlay/src/overlay_api.rs` | 287 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut overlay_api::DebugOverlayApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-debug-overlay/src/overlay_api.rs` | 315 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut overlay_api::DebugOverlayApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-draw2d` — 19 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 80 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 86 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 95 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 100 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 106 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 113 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 126 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 132 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 139 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 146 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 159 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 165 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 175 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 181 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 190 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 207 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 212 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 217 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-draw2d/src/draw2d_api.rs` | 258 | both | `mutable-engine-api` | parameter type takes or returns `&mut draw2d_api::Draw2dApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-forest` — 1 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-forest/src/forest_api.rs` | 26 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

### `modules/axiom-gpu-backend` — 13 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-gpu-backend/src/gpu_backend_api.rs` | 73 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_backend_api::GpuBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/gpu_backend_api.rs` | 93 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_backend_api::GpuBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/gpu_backend_api.rs` | 123 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_backend_api::GpuBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/gpu_backend_api.rs` | 440 | wasm32 | `retained-execution-state` | `async fn` compiles to a state machine | the generated future holds its locals and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-gpu-backend/src/gpu_backend_api.rs` | 441 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut gpu_backend_api::GpuBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/gpu_backend_api.rs` | 475 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut gpu_backend_api::GpuBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/gpu_backend_api.rs` | 486 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut gpu_backend_api::GpuBackendApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/live_gpu_binding.rs` | 134 | wasm32 | `retained-execution-state` | `async fn` compiles to a state machine | the generated future holds its locals and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-gpu-backend/src/live_gpu_binding.rs` | 474 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut live_gpu_binding::LiveGpuBinding` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/live_gpu_binding.rs` | 630 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut live_gpu_binding::LiveGpuBinding` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/live_gpu_binding.rs` | 675 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut live_gpu_binding::LiveGpuBinding` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/live_gpu_binding.rs` | 682 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut live_gpu_binding::LiveGpuBinding` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-gpu-backend/src/live_gpu_binding.rs` | 710 | wasm32 | `retained-execution-state` | `async fn` compiles to a state machine | the generated future holds its locals and resume point across every `.await`, which is retained execution state by construction |

### `modules/axiom-grid` — 7 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-grid/src/grid.rs` | 61 | both | `mutable-engine-api` | parameter type takes or returns `&mut grid::Grid<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-grid/src/grid.rs` | 67 | both | `mutable-engine-api` | parameter type takes or returns `&mut grid::Grid<T>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-grid/src/grid.rs` | 72 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-grid/src/grid_api.rs` | 52 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-grid/src/grid_api.rs` | 105 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-grid/src/grid_api.rs` | 118 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-grid/src/grid_api.rs` | 132 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

### `modules/axiom-input` — 3 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-input/src/input_state.rs` | 92 | both | `mutable-engine-api` | parameter type takes or returns `&mut input_state::InputState` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-input/src/input_state.rs` | 104 | both | `mutable-engine-api` | parameter type takes or returns `&mut input_state::InputState` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-input/src/swipe_synth.rs` | 43 | both | `mutable-engine-api` | parameter type takes or returns `&mut swipe_synth::SwipeSynth` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-netcode` — 10 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-netcode/src/input_timeline.rs` | 29 | both | `mutable-engine-api` | parameter type takes or returns `&mut input_timeline::InputTimeline` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-netcode/src/input_timeline.rs` | 50 | both | `mutable-engine-api` | parameter type takes or returns `&mut input_timeline::InputTimeline` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-netcode/src/net_command.rs` | 34 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-netcode/src/net_command.rs` | 40 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-netcode/src/netcode_api.rs` | 55 | both | `mutable-engine-api` | parameter type takes or returns `&mut netcode_api::NetcodeApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-netcode/src/netcode_api.rs` | 64 | both | `mutable-engine-api` | parameter type takes or returns `&mut netcode_api::NetcodeApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-netcode/src/netcode_api.rs` | 82 | both | `mutable-engine-api` | parameter type takes or returns `&mut netcode_api::NetcodeApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-netcode/src/netcode_api.rs` | 92 | both | `mutable-engine-api` | parameter type takes or returns `&mut netcode_api::NetcodeApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-netcode/src/peer_id.rs` | 24 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryWriter` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-netcode/src/peer_id.rs` | 29 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_kernel::BinaryReader<'_>` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-perception` — 1 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-perception/src/perception_api.rs` | 187 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

### `modules/axiom-physical-animation` — 10 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 74 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 88 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 108 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 124 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 200 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 242 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 267 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 277 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 293 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physical-animation/src/physical_animation_api.rs` | 321 | both | `mutable-engine-api` | parameter type takes or returns `&mut physical_animation_api::PhysicalAnimationApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-physics` — 17 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-physics/src/physics_api.rs` | 75 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 81 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 90 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 110 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 124 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 138 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 153 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 173 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 206 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 211 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 220 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 225 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 230 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 239 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 250 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 260 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-physics/src/physics_api.rs` | 295 | both | `mutable-engine-api` | parameter type takes or returns `&mut physics_api::PhysicsApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-recording` — 6 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-recording/src/recording_api.rs` | 74 | both | `mutable-engine-api` | parameter type takes or returns `&mut recording_api::RecordingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-recording/src/recording_api.rs` | 150 | both | `mutable-engine-api` | parameter type takes or returns `&mut recording_api::RecordingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-recording/src/recording_api.rs` | 159 | both | `mutable-engine-api` | parameter type takes or returns `&mut recording_api::RecordingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-recording/src/recording_api.rs` | 166 | both | `mutable-engine-api` | parameter type takes or returns `&mut recording_api::RecordingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-recording/src/recording_api.rs` | 172 | both | `mutable-engine-api` | parameter type takes or returns `&mut recording_api::RecordingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-recording/src/recording_api.rs` | 185 | both | `mutable-engine-api` | parameter type takes or returns `&mut recording_api::RecordingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-render` — 21 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-render/src/render_api.rs` | 68 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 72 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 78 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 93 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 109 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 115 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 126 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 144 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 164 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 186 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 293 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_api::RenderApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 293 | both | `mutable-engine-api` | return type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 301 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_api::RenderApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_api.rs` | 568 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_command_list.rs` | 31 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_command_list::RenderCommandList` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_input.rs` | 53 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_input.rs` | 65 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_input.rs` | 73 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_input.rs` | 80 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_input.rs` | 85 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render/src/render_input.rs` | 98 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_input::RenderInput` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-render-pipeline` — 7 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-render-pipeline/src/render_pipeline_api.rs` | 187 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_pipeline_api::RenderFrame` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render-pipeline/src/render_pipeline_api.rs` | 193 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_pipeline_api::RenderFrame` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render-pipeline/src/render_pipeline_api.rs` | 202 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_pipeline_api::RenderFrame` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render-pipeline/src/render_pipeline_api.rs` | 220 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_pipeline_api::RenderFrame` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render-pipeline/src/render_pipeline_api.rs` | 243 | both | `mutable-engine-api` | parameter type takes or returns `&mut render_pipeline_api::RenderPipelineApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render-pipeline/src/render_pipeline_api.rs` | 245 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_scene::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-render-pipeline/src/render_pipeline_api.rs` | 246 | both | `mutable-engine-api` | parameter type takes or returns `&mut axiom_webgpu::WebGpuApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-resources` — 12 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-resources/src/resources_api.rs` | 53 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 76 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 83 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 90 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 97 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 106 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 117 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 131 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 147 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 159 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 171 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-resources/src/resources_api.rs` | 181 | both | `mutable-engine-api` | parameter type takes or returns `&mut resource_table::ResourceTable` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-scene` — 40 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-scene/src/scene_api.rs` | 79 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 84 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 90 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 129 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 134 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 141 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 154 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 168 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 180 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 190 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 206 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 216 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 223 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 231 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 240 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 253 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 259 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 296 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 301 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 336 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 342 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 377 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 418 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 446 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 471 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 481 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 497 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 514 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api.rs` | 546 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/cameras.rs` | 23 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/dynamic.rs` | 21 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/dynamic.rs` | 37 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/players.rs` | 17 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/players.rs` | 39 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/players.rs` | 51 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/sdf.rs` | 18 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/sdf.rs` | 32 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/sdf.rs` | 46 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_api/sdf.rs` | 55 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_api::SceneApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-scene/src/scene_snapshot.rs` | 47 | both | `mutable-engine-api` | parameter type takes or returns `&mut scene_snapshot::SceneSnapshot` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-sim-core` — 100 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-sim-core/src/body.rs` | 163 | both | `mutable-engine-api` | parameter type takes or returns `&mut body::BodyStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/body.rs` | 258 | both | `mutable-engine-api` | parameter type takes or returns `&mut body::BodyStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/body.rs` | 267 | both | `mutable-engine-api` | parameter type takes or returns `&mut body::BodyStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/body_plan.rs` | 331 | both | `mutable-engine-api` | parameter type takes or returns `&mut body_plan::BodyPlanRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/body_plan.rs` | 340 | both | `mutable-engine-api` | parameter type takes or returns `&mut body_plan::BodyPlanRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/body_plan.rs` | 345 | both | `mutable-engine-api` | parameter type takes or returns `&mut body_plan::BodyPlanRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/body_plan.rs` | 354 | both | `mutable-engine-api` | parameter type takes or returns `&mut body_plan::BodyPlanRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/causal.rs` | 109 | both | `mutable-engine-api` | parameter type takes or returns `&mut causal::CausalJournal` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/definition.rs` | 199 | both | `mutable-engine-api` | parameter type takes or returns `&mut definition::DefinitionRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/dirty_set.rs` | 155 | both | `mutable-engine-api` | parameter type takes or returns `&mut dirty_set::DirtySet` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/dirty_set.rs` | 167 | both | `mutable-engine-api` | parameter type takes or returns `&mut dirty_set::DirtySet` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/dirty_set.rs` | 181 | both | `mutable-engine-api` | parameter type takes or returns `&mut dirty_set::DirtySet` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/dirty_set.rs` | 221 | both | `mutable-engine-api` | parameter type takes or returns `&mut dirty_set::DirtySet` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/effect.rs` | 163 | both | `mutable-engine-api` | parameter type takes or returns `&mut effect::EffectBatch` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/effect.rs` | 181 | both | `mutable-engine-api` | parameter type takes or returns `&mut effect::EffectBatch` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/effect.rs` | 191 | both | `mutable-engine-api` | parameter type takes or returns `&mut effect::EffectBatch` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/effect.rs` | 200 | both | `mutable-engine-api` | parameter type takes or returns `&mut effect::EffectBatch` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/effect.rs` | 216 | both | `mutable-engine-api` | parameter type takes or returns `&mut effect::EffectBatch` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/effect.rs` | 225 | both | `mutable-engine-api` | parameter type takes or returns `&mut effect::EffectBatch` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/effect.rs` | 243 | both | `mutable-engine-api` | parameter type takes or returns `&mut effect::EffectBatch` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/effect.rs` | 253 | both | `mutable-engine-api` | parameter type takes or returns `&mut effect::EffectBatch` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 130 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 191 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 212 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 217 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 249 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 272 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 304 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 324 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 329 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 336 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 363 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade.rs` | 370 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 72 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 165 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 173 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 217 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 223 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 277 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 479 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 546 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 627 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 643 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 688 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 716 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/anatomy.rs` | 723 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 107 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 132 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 207 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 231 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 265 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 328 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 342 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 358 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 372 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 387 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 434 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 484 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/materials.rs` | 539 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 103 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 114 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 130 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 145 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 158 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 173 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 199 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 207 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 221 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 227 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 237 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 255 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 263 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 277 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 285 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 327 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/facade/scheduling.rs` | 338 | both | `mutable-engine-api` | parameter type takes or returns `&mut facade::SimCoreApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/fact.rs` | 114 | both | `mutable-engine-api` | parameter type takes or returns `&mut fact::FactStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/fact.rs` | 143 | both | `mutable-engine-api` | parameter type takes or returns `&mut fact::FactStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/fact.rs` | 149 | both | `mutable-engine-api` | parameter type takes or returns `&mut fact::FactStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/interaction.rs` | 181 | both | `mutable-engine-api` | parameter type takes or returns `&mut interaction::InteractionStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/material.rs` | 71 | both | `mutable-engine-api` | parameter type takes or returns `&mut material::MaterialCatalog` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/material.rs` | 98 | both | `mutable-engine-api` | parameter type takes or returns `&mut material::MaterialCatalog` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/material_effect.rs` | 213 | both | `mutable-engine-api` | parameter type takes or returns `&mut material_effect::MaterialEffectRuleStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/material_effect.rs` | 232 | both | `mutable-engine-api` | parameter type takes or returns `&mut effect::EffectBatch` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/process.rs` | 131 | both | `mutable-engine-api` | parameter type takes or returns `&mut process::ProcessQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/process.rs` | 157 | both | `mutable-engine-api` | parameter type takes or returns `&mut process::ProcessQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/process.rs` | 166 | both | `mutable-engine-api` | parameter type takes or returns `&mut process::ProcessQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/process.rs` | 183 | both | `mutable-engine-api` | parameter type takes or returns `&mut process::ProcessQueue` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/process_dependency.rs` | 122 | both | `mutable-engine-api` | parameter type takes or returns `&mut process_dependency::DependencySet` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/process_lifecycle.rs` | 103 | both | `mutable-engine-api` | parameter type takes or returns `&mut process_lifecycle::ProcessLifecycle` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/relation.rs` | 136 | both | `mutable-engine-api` | parameter type takes or returns `&mut relation::RelationStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/relation.rs` | 163 | both | `mutable-engine-api` | parameter type takes or returns `&mut relation::RelationStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/residue.rs` | 138 | both | `mutable-engine-api` | parameter type takes or returns `&mut residue::ResidueStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/residue.rs` | 169 | both | `mutable-engine-api` | parameter type takes or returns `&mut residue::ResidueStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/residue.rs` | 174 | both | `mutable-engine-api` | parameter type takes or returns `&mut residue::ResidueStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/tissue.rs` | 168 | both | `mutable-engine-api` | parameter type takes or returns `&mut tissue::TissueRegistry` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/transfer.rs` | 167 | both | `mutable-engine-api` | parameter type takes or returns `&mut transfer::TransferRuleStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/transfer.rs` | 194 | both | `mutable-engine-api` | parameter type takes or returns `&mut transfer::TransferRuleStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/wound.rs` | 219 | both | `mutable-engine-api` | parameter type takes or returns `&mut wound::WoundStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-sim-core/src/wound.rs` | 248 | both | `mutable-engine-api` | parameter type takes or returns `&mut wound::WoundStore` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-streaming` — 2 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-streaming/src/residency.rs` | 66 | both | `mutable-engine-api` | parameter type takes or returns `&mut residency::Residency` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-streaming/src/residency.rs` | 70 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

### `modules/axiom-terrain-mesh` — 2 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-terrain-mesh/src/terrain_mesh_api.rs` | 42 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-terrain-mesh/src/terrain_mesh_api.rs` | 111 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

### `modules/axiom-text` — 33 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-text/src/text_api.rs` | 81 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 97 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 109 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 114 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 119 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 144 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 149 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 155 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 162 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 170 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 181 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 189 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 199 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 205 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 219 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 225 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 262 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 267 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 273 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 288 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 293 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 304 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_api.rs` | 310 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_api::TextApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 129 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 135 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 143 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 149 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 156 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 163 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 169 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 176 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 183 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-text/src/text_record.rs` | 189 | both | `mutable-engine-api` | parameter type takes or returns `&mut text_record::TextRecord` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-tick` — 14 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-tick/src/machines.rs` | 32 | both | `mutable-engine-api` | parameter type takes or returns `&mut machines::Machines` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/machines.rs` | 41 | both | `mutable-engine-api` | parameter type takes or returns `&mut machines::Machines` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/state_machine.rs` | 45 | both | `mutable-engine-api` | parameter type takes or returns `&mut state_machine::StateMachine` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/tick_api.rs` | 35 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_api::TickApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/tick_api.rs` | 40 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_api::TickApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/tick_api.rs` | 45 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_api::TickApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/tick_api.rs` | 51 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_api::TickApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/tick_api.rs` | 57 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_api::TickApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/tick_api.rs` | 62 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_api::TickApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/tick_api.rs` | 78 | both | `mutable-engine-api` | parameter type takes or returns `&mut tick_api::TickApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/timers.rs` | 41 | both | `mutable-engine-api` | parameter type takes or returns `&mut timers::Timers` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/timers.rs` | 49 | both | `mutable-engine-api` | parameter type takes or returns `&mut timers::Timers` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/timers.rs` | 59 | both | `mutable-engine-api` | parameter type takes or returns `&mut timers::Timers` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tick/src/timers.rs` | 67 | both | `mutable-engine-api` | parameter type takes or returns `&mut timers::Timers` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-tween` — 3 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-tween/src/tween_api.rs` | 65 | both | `mutable-engine-api` | parameter type takes or returns `&mut tween_api::TweenApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tween/src/tween_api.rs` | 78 | both | `mutable-engine-api` | parameter type takes or returns `&mut tween_api::TweenApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-tween/src/tween_api.rs` | 86 | both | `mutable-engine-api` | parameter type takes or returns `&mut tween_api::TweenApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-webgpu` — 17 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-webgpu/src/gpu_submission.rs` | 33 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/gpu_submission.rs` | 42 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/gpu_submission.rs` | 45 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/gpu_submission.rs` | 48 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/gpu_submission.rs` | 51 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/gpu_submission.rs` | 54 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/gpu_submission.rs` | 57 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/gpu_submission.rs` | 60 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/webgpu_api.rs` | 133 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/webgpu_api.rs` | 137 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/webgpu_api.rs` | 141 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/webgpu_api.rs` | 145 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/webgpu_api.rs` | 151 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/webgpu_api.rs` | 158 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/webgpu_api.rs` | 162 | both | `mutable-engine-api` | parameter type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/webgpu_api.rs` | 267 | both | `mutable-engine-api` | parameter type takes or returns `&mut webgpu_api::WebGpuApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-webgpu/src/webgpu_api.rs` | 270 | both | `mutable-engine-api` | return type takes or returns `&mut gpu_submission::GpuSubmission` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |

### `modules/axiom-windowing` — 67 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 85 | wasm32 | `shared-state-ownership` | type alias contains `std::rc::Rc<(dyn std::ops::Fn() -> std::vec::Vec<u8> + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 87 | wasm32 | `shared-state-ownership` | type alias contains `std::rc::Rc<(dyn for<'a> std::ops::Fn(&'a [u8]) + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 124 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::RefCell<std::vec::Vec<(web_sys::Element, web_sys::Element)>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 125 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::RefCell<std::vec::Vec<web_sys::Element>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 133 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::RefCell<axiom_recording::RecordingApi>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 134 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::Cell<bool>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 135 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::Cell<bool>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 140 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::Cell<bool>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 141 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::RefCell<axiom_interface::InterfaceApi>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 153 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<(dyn std::ops::Fn() -> std::vec::Vec<u8> + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 163 | wasm32 | `interior-mutability` | field type contains `std::cell::RefCell<std::collections::VecDeque<(u64, [f32; 4], axiom_host::Draw2dList)>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 173 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<(dyn std::ops::Fn() -> std::vec::Vec<u8> + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 174 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<(dyn for<'a> std::ops::Fn(&'a [u8]) + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 597 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<(dyn for<'a> std::ops::Fn(&'a [u8]) + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 618 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<(dyn for<'a> std::ops::Fn(&'a [u8]) + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/frame_scrubber.rs` | 651 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<(dyn for<'a> std::ops::Fn(&'a [u8]) + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/pointer_capture.rs` | 24 | wasm32 | `shared-state-ownership` | type alias contains `std::rc::Rc<std::cell::RefCell<std::collections::BTreeMap<i32, (f32, f32)>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/pointer_capture.rs` | 29 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::RefCell<std::collections::BTreeMap<i32, (f32, f32)>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api.rs` | 84 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::RefCell<std::option::Option<windowing_api::web::LivePresenter>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api.rs` | 87 | both | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::Cell<std::option::Option<axiom_host::BackendKind>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api.rs` | 93 | both | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::Cell<axiom_host::RenderScale>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api.rs` | 104 | both | `shared-state-ownership` | type alias contains `std::rc::Rc<std::cell::Cell<std::option::Option<axiom_host::BackendKind>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api.rs` | 129 | both | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api.rs` | 152 | both | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api.rs` | 191 | both | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api.rs` | 246 | both | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api.rs` | 260 | both | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api.rs` | 275 | both | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api.rs` | 289 | both | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api.rs` | 305 | both | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api.rs` | 384 | both | `stateful-callback-boundary` | return type exposes `impl Fn() -> std::option::Option<axiom_host::BackendKind> + 'static` on a public engine boundary | this hands executable behavior across the boundary, and that behavior may capture and retain state the engine cannot see |
| `modules/axiom-windowing/src/windowing_api.rs` | 403 | both | `stateful-callback-boundary` | return type exposes `impl Fn(axiom_host::RenderScale) + 'static` on a public engine boundary | this hands executable behavior across the boundary, and that behavior may capture and retain state the engine cannot see |
| `modules/axiom-windowing/src/windowing_api.rs` | 420 | both | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 42 | wasm32 | `mutable-engine-api` | parameter type takes or returns `&mut windowing_api::WindowingApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 153 | wasm32 | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 210 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::RefCell<std::vec::Vec<(u64, u64, [f32; 16], [f32; 16], [f32; 4], std::vec::Vec<[f32; 16]>)>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 214 | wasm32 | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 264 | wasm32 | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 316 | wasm32 | `retained-execution-state` | `async` block compiles to a state machine | the generated future holds its captured environment and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 411 | wasm32 | `retained-execution-state` | `async` block compiles to a state machine | the generated future holds its captured environment and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 541 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<(dyn std::ops::Fn() -> std::vec::Vec<u8> + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 542 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<(dyn for<'a> std::ops::Fn(&'a [u8]) + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 547 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::RefCell<std::vec::Vec<(u64, u64, [f32; 16], [f32; 16], [f32; 4], std::vec::Vec<[f32; 16]>)>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 590 | wasm32 | `retained-execution-state` | `async` block compiles to a state machine | the generated future holds its captured environment and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 748 | wasm32 | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 796 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<(dyn std::ops::Fn() -> std::vec::Vec<u8> + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 797 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<(dyn for<'a> std::ops::Fn(&'a [u8]) + 'static)>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 805 | wasm32 | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 872 | wasm32 | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 894 | wasm32 | `retained-execution-state` | `async` block compiles to a state machine | the generated future holds its captured environment and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1041 | wasm32 | `generic-behavior-state` | public generic parameter is bound by `std::ops::FnMut` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1063 | wasm32 | `retained-execution-state` | `async` block compiles to a state machine | the generated future holds its captured environment and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1145 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::RefCell<windowing_api::web::LiveBackend>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1153 | wasm32 | `interior-mutability` | field type contains `std::cell::RefCell<std::rc::Rc<std::vec::Vec<(u64, std::vec::Vec<f32>, std::vec::Vec<u32>)>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1154 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::vec::Vec<axiom_host::MaterialTexture>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1164 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::Cell<bool>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1176 | wasm32 | `interior-mutability` | field type contains `std::cell::Cell<u32>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1183 | wasm32 | `interior-mutability` | field type contains `std::cell::Cell<u32>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1188 | wasm32 | `interior-mutability` | field type contains `std::cell::RefCell<std::vec::Vec<(u64, u64, [f32; 16], [f32; 16], [f32; 4], std::vec::Vec<[f32; 16]>)>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1203 | wasm32 | `shared-state-ownership` | field type contains `std::rc::Rc<std::cell::Cell<std::option::Option<axiom_host::BackendKind>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1249 | wasm32 | `retained-execution-state` | `async fn` compiles to a state machine | the generated future holds its locals and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1257 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::Cell<std::option::Option<axiom_host::BackendKind>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1284 | wasm32 | `retained-execution-state` | `async fn` compiles to a state machine | the generated future holds its locals and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1294 | wasm32 | `shared-state-ownership` | parameter type contains `std::rc::Rc<std::cell::Cell<std::option::Option<axiom_host::BackendKind>>>` | this type exists to hold information that outlives, or mutates outside of, the call that touches it |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1478 | wasm32 | `retained-execution-state` | `async` block compiles to a state machine | the generated future holds its captured environment and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1879 | wasm32 | `retained-execution-state` | `async fn` compiles to a state machine | the generated future holds its locals and resume point across every `.await`, which is retained execution state by construction |
| `modules/axiom-windowing/src/windowing_api/web.rs` | 1939 | wasm32 | `retained-execution-state` | `async fn` compiles to a state machine | the generated future holds its locals and resume point across every `.await`, which is retained execution state by construction |

### `modules/axiom-world` — 2 findings

| File | Line | Arm | Category | Offending construct | Why it retains state |
| --- | ---: | --- | --- | --- | --- |
| `modules/axiom-world/src/world_api.rs` | 47 | both | `mutable-engine-api` | parameter type takes or returns `&mut world_api::WorldApi` | a public API that writes through a mutable reference mutates caller-owned state in place, so the new state never appears as a value |
| `modules/axiom-world/src/world_api.rs` | 50 | both | `generic-behavior-state` | public generic parameter is bound by `std::ops::Fn` | a caller-supplied closure can capture and mutate arbitrary hidden state inside engine computation |

