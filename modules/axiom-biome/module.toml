# Domain rules (elevation/moisture thresholds) live here, not in the generic
# proc-validate layer, which only knows neutral words.

[module]
name = "biome"
crate_name = "axiom-biome"
kind = "engine-module"

allowed_layers = ["kernel", "space", "entropy"]

allowed_modules = []

introduced_capabilities = [
    "biome-classification",
]
