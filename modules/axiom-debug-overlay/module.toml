[module]
name = "debug-overlay"
crate_name = "axiom-debug-overlay"
kind = "engine-module"

allowed_layers = ["interface"]

allowed_modules = []

introduced_capabilities = [
    "browser-debug-overlay",
]
