[module]
name = "grid"
crate_name = "axiom-grid"
kind = "engine-module"

allowed_layers = [
    "kernel",
    "math",
]

allowed_modules = []

introduced_capabilities = [
    "integer-grid",
    "tile-space",
    "grid-pathfinding",
    "grid-steering",
]
