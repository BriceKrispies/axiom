[module]
name = "levelgen"
crate_name = "axiom-levelgen"
kind = "feature-module"

allowed_layers = ["kernel", "space"]

allowed_modules = ["terrain", "biome", "placement"]

introduced_capabilities = [
    "world-recipe",
]
