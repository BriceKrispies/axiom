[module]
name = "render"
crate_name = "axiom-render"
kind = "engine-module"

allowed_layers = [
    "kernel",
    "runtime",
    "math",
    "frame",
    # build_frame_packet returns host::FramePacket, consumed by both the GPU
    # and (later) Canvas 2D backends.
    "host",
    # RenderMaterial names the LightingModel of the surface it describes; a
    # draw's RenderPipelineKind (BASIC_LIT / UNLIT) is derived from it rather
    # than hand-set, which is what finally puts something behind UNLIT.
    "surface",
]

allowed_modules = []

introduced_capabilities = [
    "render-input-construction",
    "backend-neutral-render-commands",
    "basic-lit-pipeline-marker",
    "deterministic-command-list",
    "backend-neutral-frame-packet",
]
