# Axiom module manifest — axiom-terrain.
#
# Coherent value-noise heightfields, keyed by lattice Address (space) + an
# entropy stream, with the kernel supplying canonical bytes + digest. Does not
# depend on proc: noise is a spatial field, not a sequential recipe, so that
# dependency would be ceremonial.

[module]
name = "terrain"
crate_name = "axiom-terrain"
kind = "engine-module"

allowed_layers = ["kernel", "space", "entropy"]

allowed_modules = []

introduced_capabilities = [
    "heightfield-noise",
]
