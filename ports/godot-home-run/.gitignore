# Godot 4 editor/import cache — regenerated on open, never committed.
.godot/
# Exported builds (web/wasm export output).
/build/
/export/
/dist/
*.tmp
# Self-signed dev TLS certs (generate locally; never commit).
/.certs/
