# Runs wasm tests under node via wasm-bindgen-test-runner; scripts/wasm-test.*
# sets the equivalent env var when invoked from the repo root instead.
[target.wasm32-unknown-unknown]
runner = "wasm-bindgen-test-runner"
