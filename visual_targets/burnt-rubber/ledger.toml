# Convergence ledger — burnt-rubber
#
# Campaign: converge the LIVE browser render of apps/burnt-rubber toward the
# night opening-straight reference the user supplied.
#
# Harness (the capture recipe every pass must reproduce — the reference is a
# live phone screenshot, so the champion must be one too; axiom-shot's native
# slices frame a different aspect and carry no DOM HUD):
#
#   uv run scripts/localhost_servers.py start-app burnt-rubber --port <PORT>
#   AXIOM_PW_VIEWPORT=470x836 AXIOM_PW_DPR=2 AXIOM_PW_TOUCH=1 \
#     uv run scripts/playwright_controller.py goto http://localhost:<PORT>/
#   uv run scripts/playwright_controller.py wait 6500
#   uv run scripts/playwright_controller.py screenshot <name>
#
# The moment: car stationary on the opening straight, ~5.9 s elapsed, 0%
# progress, 0 near misses — the same game STATE the reference shows.
#
# Correction (critic pass, iteration 1): this line originally read "matching the
# reference's HUD exactly". It does not. The state matches; the HUD does not —
# different digit font (slashed vs plain zero) at roughly half the reference's
# cap height, a control-hint line that wraps to two rows and collides with the
# BRAKE button, different boost/progress bar cell counts, and a timer reading
# 0:05.933 against the reference's 0:05.817. "Matches exactly" is a parity claim
# and no part of this campaign may carry one.
#
# Correction (critic pass, iteration 2): the promoted champion reads 0:06.300,
# not the 0:05.933 recorded above and not the reference's 0:05.817. The `wait
# 6500` recipe does not pin the timer, so the "moment" this campaign compares
# is reproducible in game STATE (stationary, 0%, 0 near misses) but not in the
# frame the HUD prints. Anything scored off the timer digits is scoring noise.
#
# Note the browser falls back to WebGL2 on this machine (WebGPU device creation
# fails on dxil.dll), so the champion is the GL path. That is the path the
# reference was shot on too.

[[iteration]]
iteration = 0
attacked_axis = "none"
changed_files = []
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = "Campaign seeded. Reference supplied by the user; champion captured from the real live browser build at the same moment (0 KM/H, OPENING STRAIGHT, 0%, 0:05.9). Initial harsh scorecard authored. final_score stated as '0*0.7 + 1.42*0.3 = 0.43' — that arithmetic is WRONG for this iteration's own scores (they sum to 18, average 1.50, final 0.45); corrected in iteration 1."
next_attacked_axis = "atmosphere"
abstraction_introduced = false

[iteration.scorecard_after]
composition_and_framing = 3
subject_fidelity = 1
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 1
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 2
artifact_level = 2

# ---------------------------------------------------------------------------
# Iteration 1 — SCORING RECALIBRATION. No render changed; no candidate was
# captured. `decision` is deliberately NOT one of the loop's four values,
# because none of them describes "the champion image is untouched and only the
# record moved" — calling this a keep_candidate would fake a promotion.
# ---------------------------------------------------------------------------
[[iteration]]
iteration = 1
attacked_axis = "none"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of the seeded record found inflation on five axes and one false
parity claim. Recalibrated DOWN, no render touched:
composition_and_framing 3->2 (the road mesh ends in a hard edge and the bottom
third of the frame is bare void where the reference carries asphalt and dashes
to the bottom edge; plus the HUD layout differs);
silhouette_readability 2->1 (the hero silhouette, the car, is a plain
rectangle);
color_palette 2->1 (right hue family, wrong value+saturation on every major
surface, which is precisely what makes it fail to read as night);
detail_density 2->1 (~half the reference's conifers/buildings and an entirely
missing mountain layer);
artifact_level 2->1 (the road-mesh cut is a defect occupying ~30% of the frame,
and the HUD hint line wraps into the BRAKE button).
Also struck the seed's claim that the HUD "already matches" / "matches the
reference's HUD exactly" — it does not (font, cap height, line wrap, bar cell
counts), and that claim had quietly scoped ~25% of the frame out of scoring.
Also corrected iteration 0's arithmetic: its own scores average 1.50, not 1.42,
so its final_score was 0.45, not 0.43.
New final_score = 0*0.7 + 1.0833*0.3 = 0.325 (was, correctly computed, 0.45).
The lowest axis is UNCHANGED at atmosphere = 0, so the next attack is unaffected
by this recalibration — the inflation was hiding severity, not the target.
"""
next_attacked_axis = "atmosphere"
abstraction_introduced = false

[iteration.scorecard_before]
composition_and_framing = 3
subject_fidelity = 1
silhouette_readability = 2
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 1
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 2
artifact_level = 2

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 1
artifact_level = 1

# ---------------------------------------------------------------------------
# Iteration 2 — SCORING RECALIBRATION + GATE BACKFILL. No render changed; no
# candidate was captured. `decision` is deliberately NOT one of the loop's four
# values, for the same reason as iteration 1: none of them describes "the
# champion image is untouched and only the record moved".
#
# THE UNRECORDED PROMOTION. Between iteration 1 and this entry, FIVE source
# commits changed the render and a sixth promoted the result:
#   59e4cc57 art-director  composition_and_framing  (car starts 30 m in)
#   d62e3a04 modeler       subject_fidelity         (fastback rear)
#   a7321618 surfacing     material_and_texture_detail (tail lights)
#   c85e483d lighting      lighting_and_shadow      (night key/fill rebalance)
#   5f09965c architect     depth_and_separation     (FrameDepthFog + render look)
#   fc940ada              champion.png replaced, and NOTHING else
# fc940ada is a one-file commit. It moved the champion image without touching
# scorecard.champion.toml, without appending a single [[iteration]], and
# without an abstractions/ record for the spine surface 5f09965c introduced.
# The loop's whole guarantee — that a champion is promoted only after the
# candidate is scored against the reference and the decision is written down —
# was skipped. This entry is the reconstruction, not a re-litigation of whether
# the render improved (it did).
# ---------------------------------------------------------------------------
[[iteration]]
iteration = 2
attacked_axis = "none"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/abstractions/0001.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of the promoted pass-1 champion. Three defects in the RECORD, none
in the decision to promote:

1. STALE SCORECARD (the serious one). scorecard.champion.toml still described
the PREVIOUS champion — an image no longer on disk. It asserted "critically NO
tail lights at all" and "a flat blue-grey box cabin" of a car that now has a
fastback cabin and two tail-light rectangles, and it made the ~30%-of-frame
road-mesh void the load-bearing reason for composition_and_framing = 2 and
artifact_level = 1 when the road now runs to the bottom edge. A scorecard
describing a deleted render is not measuring the champion at all. Every axis
note is rewritten against the image actually at champion.png.

2. SCORE MOVEMENT. Re-scoring the real champion moves exactly ONE number:
artifact_level 1 -> 2, because the defect that pinned it at 1 (bare void across
a third of the frame) is genuinely gone. Eleven axes hold their value for
materially different reasons. Notably subject_fidelity, material_and_texture_
detail and lighting_and_shadow all HOLD AT 1 despite being the declared targets
of three of the five pass-1 commits: the tail lights are flat salmon decals
with no emission, the car is still an untextured box with no wheels, and there
is still no shadow under it and no directional key anywhere in frame. Real
structural work that lands inside the same rubric band is not a score move —
recording it as one would be the number-gaming this ledger exists to prevent.
final_score 0.325 -> 0.35 (0*0.7 + (14/12)*0.3).

3. MISSING ABSTRACTION RECORD, now backfilled as abstractions/0001.toml.
5f09965c introduced new engine-spine surface — axiom_host::FrameDepthFog, a new
FramePacket field, read hooks in the GPU and Canvas 2D backend modules, and
WindowingApi::set_ambient/set_depth_fog — with no abstractions/NNNN.toml and no
abstractions/ directory. The change itself CLEARS the gate on the "genuinely
cannot express" branch (the live binder discarded the app's authored ambient
outright, and the two backends disagreed about fog with no neutral contract to
reconcile them) — an app author could not reach either defect. The ruling is
missing-record, not too-early. The backfill also records two obligations the
promotion left open: no determinism/repeat-capture proof for the champion, and
no spine gate run (coverage / check-architecture / dylint) recorded for a
change that touches crates/* and modules/*.

RIGHT-AXIS BREACH, recorded for the next pass. The lowest axis has been
atmosphere = 0 since iteration 0. Pass 1 attacked composition_and_framing,
subject_fidelity, material_and_texture_detail, lighting_and_shadow and
depth_and_separation — five axes, none of them the lowest. The skill's rule is
ONE bounded change on the LOWEST axis per iteration. A parallel proposal board
will always tempt four axes at once; that is a reason to be explicit in the
record, not a licence. atmosphere is still uniquely 0 after re-scoring, so this
recalibration does not change the target — but it is now the second consecutive
pass in which the lowest axis is named as next and then not attacked.

And the axis is not moving by accident: pass 1 shipped an atmosphere-adjacent
spine primitive (FrameDepthFog) and atmosphere stayed at 0, because fog
attenuates GEOMETRY and this axis is about what fills the EMPTY sky — stars, a
horizon glow at the vanishing point, a night gradient, a mountain layer. The
champion's sky is one flat fill. The next attack must put content in the sky.
"""
next_attacked_axis = "atmosphere"
abstraction_introduced = false

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 1
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 1
artifact_level = 2

# ---------------------------------------------------------------------------
# Iteration 3 — SCORING RECALIBRATION + GATE BACKFILL + THE FIRST TARGET
# CHANGE. No render changed; no candidate was captured. `decision` is
# deliberately NOT one of the loop's four values, for the same reason as
# iterations 1 and 2.
#
# THE UNRECORDED PROMOTION, AGAIN. Between iteration 2 and this entry, FIVE
# source commits changed the render and a sixth promoted the result:
#   045cbbdf architect     spine: FrameDrawItem gains `emissive`
#   ac4ad576 modeler       subject_fidelity            (greenhouse chop)
#   86822146 art-director  scale_and_proportion        (chase rig 15% closer)
#   02ae8da2 lighting      lighting_and_shadow         (pool light)
#   9412025c surfacing     material_and_texture_detail (asphalt aggregate grain)
#   8261e072              champion.png replaced, and NOTHING else
# 8261e072 is a one-file commit. It moved the champion image without touching
# scorecard.champion.toml, without appending a single [[iteration]], and
# without an abstractions/ record for the spine surface 045cbbdf introduced.
# That is the identical defect iteration 2 wrote up for fc940ada, one pass
# later, in the same shape, after being told. Twice is a process, not a slip.
# ---------------------------------------------------------------------------
[[iteration]]
iteration = 3
attacked_axis = "none"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/abstractions/0002.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of the promoted pass-2 champion. This pass MEASURED before it
scored, and that discipline cut both ways.

1. THE TARGET MOVES. contrast_and_exposure 1 -> 0, on measurement, not
impression. Over the scene band (rows 430..1672) the reference puts 43.8% of
its pixels below L=16 with a median of 18; the champion puts 0.0% below L=16
with a median of 60. Not one pixel of the champion's scene reaches the tone
that is the reference's single most common value. The same asphalt at the same
place in frame reads 68.3 against the reference's 8.0. Three consecutive
scorecards held this at 1 on the prose "the image still sits in a narrow band".
The band is not merely narrow - it is placed entirely off the reference's, with
the black end absent outright, and the frame reads as overcast dusk rather than
night. That is the rubric's 0.

Consequence: TWO axes now sit at 0, and the fixed axis order breaks the tie.
contrast_and_exposure is 7th, atmosphere is 9th, so the next flaw to attack is
CONTRAST_AND_EXPOSURE. This is the first time the campaign's target has moved,
and it moved because an inflated score was hiding an EARLIER tie - precisely
the failure mode the rubric's "when unsure, take the lower" exists to prevent.
The two are the same disease anyway (a night that holds no blacks and a sky
that is one flat fill), and contrast is the one that dominates every other
axis's read: no palette, material or lighting judgement can converge while the
whole frame sits 3.3x too bright.

2. THREE REGRESSIONS I DID NOT RECORD, because they did not survive
measurement. Listed because a critic pass that invents regressions to justify
its existence is the same corruption as a lens that invents progress:
  - "the camera move oversized the car": red body mass is 229x330 px against
    the reference's 216x287 - about 15% larger, not double. 86822146 converged.
  - "the grass shoulders ballooned": grass is 7.6% of the scene band against
    the reference's 10.6% - less, not more.
  - "the new asphalt grain is facet mosaic": on a clean asphalt patch the
    champion's neighbour-to-neighbour steps are SMALLER than the reference's
    (mean |dx| 2.0 vs 4.0; 3.7% of steps exceed 3 units vs 8.0%).
composition_and_framing, scale_and_proportion and artifact_level therefore all
HOLD. scale_and_proportion is the one pass-2 axis whose attack demonstrably
moved toward the reference, and it is credited as such.

3. STALE SCORECARD, AGAIN. scorecard.champion.toml still described the pass-1
image. Its notes asserted "the asphalt is uniform matte with no lit pool around
the car" and "the tail lights are a lighter flat fill, not an emissive
material"; 9412025c and 045cbbdf made both false. Every axis note is rewritten
against the image actually at champion.png.

4. MISSING ABSTRACTION RECORD, now backfilled as abstractions/0002.toml.
045cbbdf introduced new engine-spine surface - FrameDrawItem::emissive, a
36-to-40-float instance layout, a retained material_emissive map in the render
pipeline, and read paths in both backends - with no abstractions/NNNN.toml. The
change CLEARS the gate on the "genuinely cannot express" branch, more cleanly
than 0001 did: with_emissive was carried to RenderPipelineApi::submit and
dropped there, so no value an app could set ever reached a backend, and the app
was reduced to faking lamps with white-hot BASE colours (a reflectance, so they
went darker as the car turned away from the key). Ruling: missing-record, not
too-early. The backfill also records a real divergence the commit message
frames as parity - the skinned pipeline is at its 16-attribute ceiling, carries
no emissive lane, and silently shades a skinned emissive black.

5. THE NUMBERS THE ABSTRACTION DID NOT BUY. material_and_texture_detail,
subject_fidelity and lighting_and_shadow all HOLD AT 1 despite being the
declared targets of three pass-2 commits. The emissive term reaches the pixels
but the tail lights render as pale salmon fills with no spill; the pool light
produces no measurable lateral falloff (62.3 next to the car vs 61.9 at the
frame's left edge, at the car's own depth); the asphalt has grain but no sheen.
Real structural work landing inside the same rubric band is not a score move.

final_score 0.35 -> 0.325 (0*0.7 + (13/12)*0.3).

RIGHT-AXIS BREACH, THIRD CONSECUTIVE. The lowest axis was atmosphere = 0 from
iteration 0 through iteration 2 and was named next_attacked_axis every time.
Pass 1 attacked five axes, none of them atmosphere. Pass 2 attacked five axes,
none of them atmosphere. Three passes, one axis named, zero attacks on it. A
parallel proposal board will always tempt N axes at once; the skill's rule is
ONE bounded change on the LOWEST axis per iteration, and a board that never
attacks its own stated target is not running the loop - it is running five
unscored art passes and promoting the bundle. The target has now moved to
contrast_and_exposure by tie-break; atmosphere remains at 0 behind it.

TWO DEBTS STILL UNPAID from iteration 2, now owed twice: no determinism /
repeat-capture proof exists for either promoted champion, and no spine gate run
(coverage / check-architecture / dylint) is recorded for either 5f09965c or
045cbbdf, both of which touch crates/* and modules/*.

No parity language appears anywhere in the record; none is added here.
"""
next_attacked_axis = "contrast_and_exposure"
abstraction_introduced = false

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 1
artifact_level = 2

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 1
artifact_level = 2

# ---------------------------------------------------------------------------
# Iteration 4 — the pass-3 candidate, scored BEFORE promotion.
#
# Written by the foreman to close the defect the critic recorded twice (at
# iterations 2 and 3): passes 1 and 2 were promoted as one-file commits with no
# candidate scoring and no decision. This entry restores the loop's guarantee.
#
# SELF-SCORED, and therefore suspect. These are the foreman's numbers for a pass
# the foreman stacked — the exact conflict of interest the critic lens exists to
# audit. Treat every value here as provisional until the next critic pass
# re-measures it. Where a score was borderline it was taken LOW, and three axes
# that saw real work this pass (subject_fidelity, color_palette,
# contrast_and_exposure) are deliberately HELD, per the standing rule that
# structural work landing inside the same rubric band is not a score move.
# ---------------------------------------------------------------------------
[[iteration]]
iteration = 4
attacked_axis = "detail_density"
changed_files = [
  "crates/axiom-host/tests/architecture.rs",
  "apps/burnt-rubber/src/render/scenery_pool.rs",
  "apps/burnt-rubber/src/tuning.rs",
  "apps/burnt-rubber/src/render/mod.rs",
  "apps/burnt-rubber/src/render/surface_builder.rs",
  "apps/burnt-rubber/src/render/road_mesh.rs",
  "apps/burnt-rubber/src/render/asphalt_texture.rs",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
Six lens commits stacked; the colorist's grade was DROPPED for the third
consecutive pass because `axiom-windowing` exposes no `set_postprocess`, so an
authored grade cannot reach the live browser arm this campaign captures from.
Four defects fixed rather than tuned: a curated-export gate left red by the
wave-1 spine commit (axiom-host was failing `cargo test` on this line); two
disagreeing scenery culls that made every per-kind draw distance dead code; a
UV mapping that stretched the new asphalt grain 9:1 into lateral camo bands; and
a key light aimed down-track so every shadow fell hidden under its own caster.
Verified green before promotion: axiom-host 309 + 17 pass, burnt-rubber 427 pass.
No non-attacked axis dropped. final_score 0.325 -> 0.525 (self-scored).
"""
next_attacked_axis = "contrast_and_exposure"
abstraction_introduced = false

# Known-outstanding debts, recorded rather than quietly carried:
#  - No determinism/repeat-capture proof for any promoted champion (owed 3x).
#  - No spine gate run (coverage / check-architecture / dylint) recorded for the
#    three spine commits on this line (5f09965c, 045cbbdf, 7b913a78).
#  - `atmosphere` has been named next_attacked_axis at iterations 0,1,2 and
#    attacked zero times; it is no longer the target only because
#    contrast_and_exposure tied it at 0 and wins the fixed-order tie-break.

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 1
artifact_level = 2

[iteration.scorecard_after]
composition_and_framing = 3
subject_fidelity = 1
silhouette_readability = 2
material_and_texture_detail = 2
lighting_and_shadow = 2
color_palette = 1
contrast_and_exposure = 0
depth_and_separation = 2
atmosphere = 0
scale_and_proportion = 3
detail_density = 2
artifact_level = 3

# ============================================================================
# REFERENCE ERA B BEGINS HERE (2026-08-04)
# ============================================================================
# The user supplied a new reference and scoped the campaign to the WebGPU/WebGL
# arm. Everything above this line was scored against the era-A reference
# (retired; recoverable at b8010795:visual_targets/burnt-rubber/reference.png)
# and does not compare to anything below it.
#
# Two structural changes come in with the era:
#
#  1. `campaign.toml` now holds the machine-readable half of the campaign —
#     the judged arm (`gpu`), the guarded arm (`canvas2d`, legibility only,
#     never scored, never a target), both capture recipes, and the fixed axis
#     order. The foreman reads it instead of re-deriving the recipe from these
#     header comments.
#  2. `champions/` archives every champion that has landed on `main`, with an
#     INDEX.md and a contact sheet, so progress survives champion.png being
#     overwritten each pass. Backfilled with all four era-A champions.
#
# The capture recipe gained two steps since era A and both are load-bearing:
# the controller daemon must be restarted for AXIOM_PW_VIEWPORT to apply (an
# already-running daemon silently keeps its old desktop viewport), and the race
# must be started with Enter/Space/tap or every frame is the start screen
# 2f5ad14b added.

[[iteration]]
iteration = 5
attacked_axis = "none"
changed_files = [
  "visual_targets/burnt-rubber/reference.png",
  "visual_targets/burnt-rubber/champion.png",
  "visual_targets/burnt-rubber/canvas-guard.png",
  "visual_targets/burnt-rubber/campaign.toml",
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/champions/",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
Reference swap + recalibration, not a convergence pass. No lens ran and no app \
source changed. The reference was replaced with the user's new era-B image \
(925x1700: near-black night, black muscle car with stripes/badge/plate, wet \
glossy asphalt, unbloomed posts) and the campaign was scoped to the judged GPU \
arm. The era-A champion on disk was four commits stale — 8c3f408f, 67093433, \
89560b17, 2f5ad14b had moved the render out from under it — so it was archived \
to champions/0003 and main was re-captured at the same moment (0 KM/H, OPENING \
STRAIGHT, 0%, 0 near misses, 0:09.200 against the reference's 0:09.117). The \
Canvas 2D arm was captured alongside it as the first guard image. \
scorecard_before/after below are NOT before-and-after of a change: before is \
the last era-A scorecard, after is the era-B scoring of a re-captured render. \
They are different exams and no movement between them is progress. \
final_score 0.325 (era A) -> 0.30 (era B), lowest axis contrast_and_exposure 0 \
in both.
"""
next_attacked_axis = "contrast_and_exposure"
abstraction_introduced = false

[iteration.scorecard_before]   # era A — the reference this scored is retired
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 1
artifact_level = 2

[iteration.scorecard_after]    # era B — a different exam, not an improvement
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 1
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

# ---------------------------------------------------------------------------
# Iteration 6 — SCORING RECALIBRATION + GATE BACKFILL. No render changed; no
# candidate was captured; no app source was touched. `decision` is deliberately
# NOT one of the loop's four values, for the same reason as iterations 1, 2
# and 3: none of them describes "the champion image is untouched and only the
# record moved", and calling this a keep_candidate would fake a promotion.
# ---------------------------------------------------------------------------
[[iteration]]
iteration = 6
attacked_axis = "none"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/abstractions/0003.toml",
  "visual_targets/burnt-rubber/abstractions/0004.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of the era-B baseline. Three findings. The champion itself is
CLEAN — it postdates every commit on the line (its HUD carries the `GHOST` line
ece53937 added) and its md5 matches champions/0004-2026-08-04-main.png byte for
byte, so unlike passes 1, 2 and 3 this record is not scoring a deleted image.

1. ONE AXIS INFLATED: atmosphere 1 -> 0. It rested on two arguments and both
fail. (a) "the champion's flat sky is now wrong only in value" — it is not flat
(a navy gradient at ~7x the reference's (1,2,4)) and it carries a green/teal
haze band across the whole horizon that the reference has no counterpart for.
Atmosphere present and plainly INCORRECT is the rubric's 0; atmosphere merely
crude is its 1. (b) "add the moon, which is very nearly right" — the moon is
already the load-bearing credit at composition_and_framing ("the closest match
anywhere in this frame"). Spending one matched feature twice to hold two axes up
is double-counting. Removing the double credit leaves this axis holding nothing
but additive error: the 7x sky, the green band, and a bloom halo on every
reflector post (1.88% bright-yellow band area against the reference's 0.98%)
where the reference's posts are flat.

THE TARGET DOES NOT MOVE. This creates a tie at 0 with contrast_and_exposure,
and campaign.toml's fixed axis order puts contrast_and_exposure 7th against
atmosphere's 9th. Next attack is UNCHANGED at contrast_and_exposure — the
inflation was hiding severity, not the target. final_score 0.30 -> 0.275
(0*0.7 + (11/12)*0.3).

2. THE ERA-B DRIFT ACCOUNTING WAS WRONG, and wrong in the direction that hides
work. Iteration 5 and the scorecard header both said the era-A champion was
"four commits stale" and named 8c3f408f, 67093433, 89560b17 and 2f5ad14b.
`git log fac33fc4..51db97d6 -- apps/burnt-rubber/ crates/axiom-host/
modules/axiom-gpu-backend/ modules/axiom-windowing/` returns NINE, and the two
omitted are the two largest:

  08731280  FrameSky + FrameBloom + FrameRenderLook + a GPU post chain + a
            specular term + three new RenderCapability bits, AND a burnt-rubber
            sky/bloom/roughness look authored on top of them.
  ece53937  axiom_host::MaterialTexture + CPU mip chains + per-material
            anisotropic sampling + the tangent-frame NaN fix.

Also unnamed: 32ced933 and 631ca158 (which moved the Canvas 2D GUARD arm from
the Low to the High quality tier — noted only, never scored; the guard arm is
not this campaign's exam).

This is not pedantry about a count. The scorecard blames 08731280's bloom halos
at TWO axes and its sky at a third, and blames a GHOST line on "the four
commits" when the GHOST line is ece53937's. Blaming the wrong commit is how the
next pass fixes the wrong thing. Both attributions are corrected in place; no
score moved for either.

3. TWO MISSING ABSTRACTION RECORDS, now backfilled as 0003.toml (08731280) and
0004.toml (ece53937). Both changes CLEAR the gate on the "genuinely cannot
express" branch, and 0003 clears it more cleanly than any abstraction on this
campaign so far: ledger iteration 4 had ALREADY written down, one pass before
the commit landed, that "the colorist's grade was DROPPED for the third
consecutive pass because axiom-windowing exposes no set_postprocess". Three
passes of a lens unable to mount ANY bounded attempt is the inexpressibility
branch stated in advance. Ruling for both: missing-record, not too-early, not
ceremonial.

That makes FOUR consecutive spine changes on this campaign's line that landed
without their gate record (0001, 0002, 0003, 0004), each one backfilled by a
critic pass after the fact. Iteration 2 wrote this up. Iteration 3 wrote it up
again, calling it "a process, not a slip". It is now four for four.

WHAT 0003 IS AND IS NOT. It is capability-gated richness — Sky, Specular and
Bloom land on the judged GPU arm and the Canvas 2D guard arm drops them by
declaration — which is exactly the shape the skill asks for, and the guard arm
was not lowered to carry it. It is also a worked example of a lesson the record
should keep: new spine capability aimed at one axis moved a DIFFERENT axis
backwards. It was reached for contrast_and_exposure, which is still 0, and its
bloom and sky are two of the three reasons atmosphere drops to 0 in this same
entry. A record that only lists what a change was FOR will never show that.

DEBTS, now owed four times over and unchanged: no determinism / repeat-capture
tolerance proof exists for ANY champion this campaign has promoted, and no spine
gate run (coverage / check-architecture / dylint) is recorded for any of the
four spine commits. 0004 adds a harness limit worth keeping: the defect
ece53937 fixed was mobile-GPU-only and no capture this campaign takes could
reproduce it, so one desktop-class WebGL2 frame cannot see a whole class of
defect in the arm it judges.

RIGHT-AXIS CHECK: contrast_and_exposure is confirmed correct as the next attack,
now by tie-break rather than uniquely. `atmosphere` has now been at 0 for
essentially the whole campaign and attacked zero times; it sits directly behind
contrast_and_exposure in the order and the two are the same disease (a night
that holds no blacks, and air that adds light the reference does not have).

GUARDED-ARM CHECK: clean. No scorecard note, ledger reason or axis score in this
campaign credits or blames the Canvas 2D arm. campaign.toml scopes it correctly
and scorecard.champion.toml says so at the top.

PARITY-CLAIM CHECK: clean. No "done", "matched" or "parity" language anywhere in
the record; none is added here. The moon note's "very nearly right" is an axis
detail about one feature, not a campaign claim, and it is now scored once rather
than twice.
"""
next_attacked_axis = "contrast_and_exposure"
abstraction_introduced = false

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 1
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

[[iteration]]
iteration = 7
attacked_axis = "contrast_and_exposure"
changed_files = [
  "apps/burnt-rubber/src/tuning.rs",
  "apps/burnt-rubber/src/render/car_model.rs",
  "apps/burnt-rubber/src/render/palette.rs",
  "apps/burnt-rubber/src/render/mod.rs",
  "apps/burnt-rubber/src/render/asphalt_texture.rs",
  "apps/burnt-rubber/src/web.rs",
  "crates/axiom-host/src/frame_postprocess.rs",
  "crates/axiom-host/src/frame_render_look.rs",
  "modules/axiom-windowing/src/windowing_api.rs",
  "modules/axiom-gpu-backend/src/post_chain.rs",
  "modules/axiom-gpu-backend/src/live_gpu_binding.rs",
  "modules/axiom-gpu-backend/src/offscreen.rs",
  "modules/axiom/src/prelude.rs",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
Era-B pass 1. Seven lenses ran with no steering; all seven committed and all seven \
cherry-picked without a conflict. Rendered once on the judged GPU arm (WebGL2) at the \
campaign moment (0 KM/H, OPENING STRAIGHT, 0%, 0:09.233 against the reference's \
0:09.117), and once on the guarded canvas2d arm. Attacked axis contrast_and_exposure \
0 -> 2: the band went from 2.4% of pixels below L=16 to 83.7% against the reference's \
80.2%, so the histogram shape is now the reference's where it was a night scene with no \
blacks at all. No non-attacked axis dropped, so this is a keep by the table; six axes \
rose and none fell. final_score 0.275 -> 1.20, lowest axis 0 -> 1. \
\
THE OVERSHOOT IS RECORDED, NOT SMOOTHED. The frame now sits BELOW the reference and \
clipped: median 0.0 against 3.9, sky (0,0,0) against (1,2,4), shoulder (0,0,0) against \
(3,5,4). Three commits pushed the same direction in one pass — colorist dropped SKY \
~13x, lighting cut ambient ~4x and the key 0.85->0.30, architect's low_key() subtracts \
a 0.16 black point on top — and the architect's own proposal predicted this exact \
interaction, having derived 0.16 from the pre-pass 0.18 floor. The next pass retunes \
that one constant; it does not revert the three. \
\
Two axes are HIDDEN rather than measured at this exposure and are flagged as such in \
the scorecard: material_and_texture_detail (the new 1.2 cm asphalt grain is invisible \
under a clipped floor) and part of detail_density. They must be re-scored after the \
retune, not carried. \
\
Guarded arm: canvas2d darkened in parallel (it takes the same black point through the \
CPU post path) and remains fully legible — car, lane paint, reflector posts, stripes \
and HUD all readable. No regression; not scored.
"""
next_attacked_axis = "silhouette_readability"
abstraction_introduced = true

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 2
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
atmosphere = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 1

# ---------------------------------------------------------------------------
# Iteration 8 — SCORING RECALIBRATION + GATE BACKFILL. No render changed; no
# candidate was captured; no app source was touched. `decision` is deliberately
# NOT one of the loop's four values, for the same reason as iterations 1, 2, 3
# and 6: none of them describes "the champion image is untouched and only the
# record moved", and calling this a keep_candidate would fake a promotion.
# ---------------------------------------------------------------------------
[[iteration]]
iteration = 8
attacked_axis = "none"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/abstractions/0005.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of era-B pass 1. Four findings; the target moves.

THE CHAMPION IS CLEAN, and that is checked first because three earlier passes on
this campaign were not. `champion.png` hashes byte-identical to
`champions/0005-2026-08-04-d2509552.png`, and the only two commits on `main`
since `d2509552` are `7c983c54` (promote) and `1bf3776a` (archive) — neither
touches an app or spine source file. The scorecard describes the image on disk.
Era is consistent: campaign.toml `reference_era = "B"`, INDEX.md era B current,
and iteration 7's before/after both sit inside era B. No score crosses the
boundary.

EVERY MEASUREMENT IN THE PASS-1 SCORECARD REPRODUCES. Re-measured from the two
PNGs, band rows 0.30h..0.72h: band median 0.0 (reference 3.9), 83.7% of pixels
below L=16 (reference 80.2%), sky (0,0,0) against (1,2,4), road-left (0,0,5)
against (7,8,11), tail-light span 0.312w against 0.522w. The pass reported its
own overshoot accurately and did not smooth it. That is the part of this record
that is working.

1. THREE AXES INFLATED — subject_fidelity, atmosphere, scale_and_proportion,
each 2 -> 1. Seven of twelve axes rose in a pass whose one attacked axis was
exposure, and these three are the ones credited for something other than a new
resemblance to the reference:

  (a) subject_fidelity. Raised on the arrival of the twin stripes and a plate.
  Both are real; neither makes the structure emerge, which is what the 2 anchor
  requires. New measurements: the car has no wheels and no glass at all; its
  body is 0.75 wide-to-tall against the reference's 1.52 (2.0x too tall for its
  width); its lamp bars are 0.097h against 0.032h (3.0x too deep, aspect 1.8:1
  against 9.3:1) and filled flat orange where the reference has two thin
  dark-red bars with a badge between them; the plate is grey and hangs below the
  body where the reference's is warm yellow and inset; the decklid is bright red
  where the reference is black head to toe. Six identifying features wrong, two
  stripes right. That is the 1 anchor almost verbatim — "present but crude/proxy
  … nobody would mistake it for the reference". A decal on a proxy is a proxy.

  (b) atmosphere. Raised 0 -> 2 on three DELETIONS: the 7x navy sky, the
  green/teal haze band, and post bloom from 1.88% to 0.94% of band area against
  the reference's 0.98%. All three are real and none is disputed. All three are
  the removal of atmosphere the reference does not have, and this axis scores
  the atmosphere it DOES have. What the champion now contains is a sky of
  literal (0,0,0) with no gradient and no hue, no haze, no scattering, no glow
  on the far posts, and a moon halo larger and softer than the reference's.
  Scoring a deletion as an arrival is the cleanest example on this scorecard of
  a number moving without the render moving toward identity.

  (c) scale_and_proportion. Raised on one real measurement (tail-light span
  0.220w -> 0.312w against 0.522w) covering half the axis. The proportion half
  was never measured and is grossly wrong: body aspect 0.75 against 1.52. A
  subject at 58% of the reference's width AND 2.0x its aspect error is not the
  reference's shape at a smaller size; it is a different shape.

Two more axes were examined and HELD at 2 with corrected reasoning rather than
lowered: lighting_and_shadow (the pool is real but is one-sided — road left of
the car (1,5,14) against road right (37,42,54), a 36-level asymmetry, where the
reference is symmetric within 2 levels — and neutral grey where the reference's
is warm amber (68,55,23)) and color_palette (background is a 3, the car is a 1,
the axis takes the lower). Neither correction changes a number.

THE TARGET MOVES. subject_fidelity at 1 joins the tie at 1, and campaign.toml's
fixed axis order places it 2nd against silhouette_readability's 3rd.

  NEXT FLAW = subject_fidelity   (iteration 7 recorded silhouette_readability)

Both axes indict the same car, so this is not a redirection away from the model
— it is a change of question, and the question matters to whoever attacks it.
silhouette_readability asks "does the outline read": chamfers, rolled haunch,
tucked valance, the things the modeler correctly recorded as beyond the app's
cube/plane/sphere/cylinder vocabulary. subject_fidelity asks "is that the
reference's car at all": wheels, glass, a body half as wide as it should be for
its height, lamp bars 3x too deep, a grey plate, a red decklid. The second is
reachable with the vocabulary that already exists and is, by the campaign's own
fixed order, owed first.

final_score 1.20 -> 1.125 (1*0.7 + (17/12)*0.3). The lowest axis is unchanged at
1; the inflation was hiding WHICH axis, not the severity.

2. MISSING ABSTRACTION RECORD, now backfilled as abstractions/0005.toml.
Iteration 7 sets `abstraction_introduced = true` and then names no abstraction.
The pass landed new engine-spine surface in `cc71f189` (FrameRenderLook gains
the colour grade as its fifth part; FramePostProcess gains a black_point term;
a live-arm grade path through post_chain/live_gpu_binding; a WindowingApi
forwarding entry; a widened prelude), tidied by `d2509552`. A flag that says an
abstraction was introduced with no record of WHICH one is worse than no flag: it
tells the next pass the gate was considered and gives it nothing to check.

The change CLEARS the gate on the "genuinely cannot express" branch, and clears
it well: the judged arm — the live browser present — could not receive a colour
grade at all (FramePostProcess travelled only on a FramePacket, so only the two
read-back arms were graded), and the chain had no subtract, so no composition of
exposure/contrast/saturation/white-balance expresses "remove 0.16 of lift". Both
are spine facts no app-tier value reaches. Ledger iterations 4, 5 and 7 each
record a colorist grade authored and not reaching the captured frame — three
passes unable to mount any bounded attempt, stated in advance.

This is the FIFTH consecutive spine change on this campaign's line to land
without its gate record (0001, 0002, 0003, 0004, now 0005). Four of the five
cleared the gate on the merits. The repetition is the finding: the records are
being written by the critic afterwards instead of by the lens that reached.

3. ONE ARITHMETIC SLIP, in the conservative direction. Iteration 7's prose says
"six axes rose and none fell"; its own before/after tables show SEVEN rose
(subject_fidelity, lighting_and_shadow, color_palette, contrast_and_exposure,
depth_and_separation, atmosphere, scale_and_proportion). Recorded, not because
one miscount matters, but because seven of twelve axes rising off one attacked
axis is the pattern this recalibration is about.

4. TWO THINGS THIS RECORD DOES RIGHT AND SHOULD KEEP DOING. The overshoot was
named in the same breath as the win, with the numbers that prove it and the one
constant that caused it — no smoothing. And the guarded canvas2d arm is reported
("darkened in parallel, remains fully legible, no regression") and NOWHERE
scored, credited or blamed: it appears in no axis comment on the scorecard and
in no score in this ledger. The campaign has one exam and it is the GPU arm.

PARITY LANGUAGE: clean. The only occurrence of the word on the scorecard is the
art-director's finding that "full parity is unreachable from the camera at all",
which is a stated limitation, not a claim. Nothing anywhere claims a matched,
finished or done axis, and nothing on this scorecard is above 2.

NO RENDER, NO APP SOURCE AND NO CHAMPION IMAGE WAS TOUCHED BY THIS ITERATION.
"""
next_attacked_axis = "subject_fidelity"
abstraction_introduced = false

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 2
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
atmosphere = 2
scale_and_proportion = 2
detail_density = 1
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

[[iteration]]
iteration = 9
attacked_axis = "subject_fidelity"
changed_files = [
  "apps/burnt-rubber/src/tuning.rs",
  "apps/burnt-rubber/src/render/car_model.rs",
  "apps/burnt-rubber/src/render/mod.rs",
  "apps/burnt-rubber/src/render/palette.rs",
  "apps/burnt-rubber/src/render/asphalt_texture.rs",
  "apps/burnt-rubber/src/web.rs",
  "crates/axiom-host/src/host_device_profile.rs",
  "modules/axiom-windowing/src/windowing_api.rs",
  "modules/axiom-gpu-backend/src/live_gpu_binding.rs",
  "modules/axiom-gpu-backend/src/upscale.rs",
  "modules/axiom-gpu-backend/src/gpu_backend_api.rs",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
Era-B pass 2. Seven lenses, seven commits, all cherry-picked with zero conflicts, \
one render on the judged GPU arm (WebGL2) at the campaign moment (0 KM/H, OPENING \
STRAIGHT, 0%, 0:10.983) plus the canvas2d guard. Attacked axis subject_fidelity \
1 -> 2 on the tail's arrangement: lamp clusters of twin tubes in fixed bezels, and \
a centre badge. No non-attacked axis dropped, so this is a keep by the table. Four \
axes rose, each on a measurement stated at the axis; final_score 1.125 -> 1.225. \
\
The pass-1 overshoot is repaired, and NOT by the route pass 1 predicted. Pass 1 \
named low_key()'s 0.16 black point. The lighting lens found the compounding term \
instead: the black point is a hard clip at 41/255 and pass 1 had also cut \
KEY_INTENSITY 0.85 -> 0.30, so tarmac rendered at byte 27 and the verge at 35, both \
under the subtract, both clipped to zero. It predicted a byte-35 verge and measured \
0.0. Raising the key to 0.88 restores the ground plane: road (0,2,11) -> (14,14,17) \
against the reference's (7,8,11). The grade was untouched and no other lens moved \
exposure, so nothing compounded — the lens warned that it must not, and the \
architect independently advised the same. \
\
artifact_level is HELD as unverified rather than credited: the architect's \
supersample targets it, but the edge-ramp probe (2.34 -> 4.11 against the \
reference's 1.03) cannot separate sampling from the road's simultaneous luminance \
rise. It needs a probe that holds lighting fixed. \
\
Standing finding, not acted on and not scored: the judged arm is anamorphically \
squashed 3.16x horizontally (web.rs boots surface and camera from a compile-time \
1280x720 while the live canvas is portrait 470x836 @ DPR 2; configure_surface also \
hardcodes a 1.0 scale factor so the host never learns DPR). Two lenses reached it \
independently. It is unscored because the reference carries the same squash in its \
world and only its car was redrawn true, so correcting it would diverge from the \
reference on composition — a foreman decision about the target, not a silent fix. \
\
Guarded arm: canvas2d captured, still fully legible, not scored. \
\
Also fixed on main before this pass (339d7bda): the pass-1 stripe test landed red \
and had been failing on main — it measured centre-to-centre distance where the twin \
stripes straddle the centreline by +/-0.30, so a correctly-placed stripe could never \
pass. Now measured along the glass's normal. 545 app tests green.
"""
next_attacked_axis = "silhouette_readability"
abstraction_introduced = true

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 2
silhouette_readability = 1
material_and_texture_detail = 2
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 2
detail_density = 2
artifact_level = 1

# ============================================================================
# REFERENCE ERA C BEGINS HERE (2026-08-06)
# ============================================================================
# The user supplied a daylight reference and stated explicitly that they
# understand it is sunny where eras A and B were night, and that this is the
# intent. Everything above this line was scored against a night reference and
# does not compare to anything below it. Era B's reference is recoverable at
# 493dac58:visual_targets/burnt-rubber/reference.png.
#
# Three things moved with the era, and only the first is a look:
#
#  1. Day, not night. Sun and lens flare, cumulus, turquoise sea, sand, palms
#     casting hard shadows across the road, stilt houses, mountains.
#  2. The MOMENT. Eras A and B were parked on the opening straight at ~0:09;
#     this reference is mid-race at 162 km/h. The campaign follows it to the
#     game's COASTAL SWEEPERS section rather than to the reference's stated 72%,
#     because 72% is inside CANYON RUN — walls on both sides, the one stretch
#     that cannot show a beach. The reference is a concept frame whose section
#     name ("COASTAL HIGHWAY") does not exist in this game, so its PLACE is the
#     honest thing to match and its percentage is not.
#  3. The capture method. A mid-race moment cannot be waited for reproducibly —
#     driving there takes ~40 s and the traffic decides where you end up — so it
#     is PLACED with the browser probe and then FROZEN. Recipe in campaign.toml.
#
# Also fixed here, and unrelated to the era: campaign.toml's `axes` array had sat
# under the `[capture.guard]` table header since era B, so it parsed as
# `capture.guard.axes`. Every foreman so far read that file by eye and never
# noticed; one that parsed it would have found no axis order at all — and the
# axis order is what breaks ties for "which axis do we attack next". Moved under
# [campaign], where the tie-break for this very entry was computed from it.

[[iteration]]
iteration = 10
attacked_axis = "none"
changed_files = [
  "visual_targets/burnt-rubber/reference.png",
  "visual_targets/burnt-rubber/champion.png",
  "visual_targets/burnt-rubber/canvas-guard.png",
  "visual_targets/burnt-rubber/campaign.toml",
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/champions/",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
Reference swap + recalibration, not a convergence pass. No lens ran and no app \
source changed. The reference was replaced with the user's daylight coastal \
image, the moment moved to a driving frame in COASTAL SWEEPERS, and main was \
re-captured on both arms at the new moment via the probe (place 1900 m / 45 m/s, \
then freeze). \
\
scorecard_before/after below are NOT before-and-after of a change: before is the \
last era-B scoring, after is the era-C scoring of a freshly captured render. \
They are different exams. final_score 1.225 -> 0.25, and that fall is the \
reference moving, not the render getting worse — the same app that scored 1.225 \
against a night reference scores 0.25 against a daylight one. \
\
Measured, band = rows 0.30h..0.72h: reference median luma 139.7 with 0.5% of \
pixels below L=16; champion median 0.1 with 78.1%. Mean saturation 0.522 against \
0.141. Sky (27,111,198) against (19,19,20). \
\
Four axes sit at 0 — lighting_and_shadow, color_palette, contrast_and_exposure, \
atmosphere — and the fixed order breaks the tie to lighting_and_shadow, which is \
also the causal order: the other three are downstream of the frame being lit by \
a moon. There is no grading a palette or an exposure toward daylight until there \
is a sun. \
\
Guarded arm captured at the same frozen moment. Not scored.
"""
next_attacked_axis = "lighting_and_shadow"
abstraction_introduced = false

[iteration.scorecard_before]   # era B — the reference this scored is retired
composition_and_framing = 2
subject_fidelity = 2
silhouette_readability = 1
material_and_texture_detail = 2
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 2
detail_density = 2
artifact_level = 1

[iteration.scorecard_after]    # era C — a different exam, not a regression
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 0
color_palette = 0
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 1
artifact_level = 1

[[iteration]]
iteration = 11
attacked_axis = "none"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/abstractions/0006.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
SCORING RECALIBRATION by the critic lens. No lens ran, no app source changed, \
no render was captured. The champion on disk is unchanged and remains the \
era-C baseline (md5 e6346ecf..., byte-identical to champions/0007). This entry \
corrects two inflated axes, backfills one missing abstraction record, and \
records what the era swap dropped. \
\
FIRST, WHAT THIS AUDIT FOUND HONEST, because a critic that only reports faults \
is not measuring anything. Every measurement in scorecard.champion.toml's \
header was re-derived from the two PNGs and reproduced exactly: band median \
139.7 / 0.1, 1st percentile 23.2 / 0.0, %below-L16 0.5 / 78.1, saturation 0.522 \
/ 0.141, whole-frame luma 114.4 / 23.3. Not one figure was rounded in the \
campaign's favour. The era-C boundary is handled better than the skill asks: \
the scorecard warns a reader against reading 1.225 -> 0.25 as a regression \
before anyone can misread it, and iteration 10 says plainly that the two \
numbers are different exams. The champion is fresh, not stale — it was captured \
at c8d11623 and the only commit since (edb4f4ff) touched visual_targets/ alone. \
No forbidden parity or done/matched language anywhere in the record; the two \
"parity" hits in campaign.toml are the guard-arm rule stating what must NOT be \
traded away, which is correct usage. The guarded canvas2d arm is captured, \
never scored, and never credited or blamed in any axis note. \
\
CORRECTION 1 — scale_and_proportion 2 -> 1. The judged arm renders the WORLD \
anamorphically and the campaign has known it since iteration 9, where it was \
written down as a standing finding and then dropped by the era-C swap: it \
appears nowhere in iteration 10 or in the era-C scorecard. web.rs still \
configures the surface from the compile-time WIDTH x HEIGHT (1280x720) while \
the live canvas is 470x836 @ DPR 2 = 940x1672, a 3.16x horizontal squash. \
Proved from the champion itself rather than inferred: the moon (world space) \
measures 34w x 114h, h/w 3.35, while the BRAKE ring (screen space) measures \
180w x 190h, h/w 1.06 — 3.35/1.06 = 3.16, the predicted factor to the second \
decimal. A round HUD and an elliptical moon in one image. The old note scored \
the axis a 2 on the car's share of frame and the road's width at the bottom \
edge; both readings were taken through the squash, and the car's "comparable \
share of frame" is two errors cancelling. An axis named scale_and_proportion \
cannot be "the right structure is emerging" on a frame stretched 3.16x. \
\
The era-B reason for not scoring the squash — "the reference carries the same \
squash in its world and only its car was redrawn true" — EXPIRED WITH ERA B and \
was never re-examined when the reference changed. The era-C reference is a \
painted daylight frame with a round sun and round cumulus. Nothing in it is \
anamorphic. Whether to FIX the squash remains a foreman decision about the \
target, exactly as iteration 9 said; whether to SCORE it is not, and it has been \
scored now. \
\
CORRECTION 2 — artifact_level 1 -> 0. The old note named two artifacts. \
Auditing this axis on a frame that is 78% black requires lifting the exposure, \
and at 6x the champion shows at least six: a stray green streak crossing the \
black sky from ~(132,460) to ~(152,660) at RGB ~(17,52,35) against a (0,0,0) \
sky, never named before; lane paint blooming into wide white smears where the \
reference's is clean matte white; the HUD's BOOST row drawn on top of the touch \
buttons, overlapping the D-pad and the GAS ring; a gear disc reading "R" while \
the HUD reads 156 KM/H; the aliasing already named; and the crushed floor \
already named, at 78.1% of the scene band and 99.7% of rows 0.28h..0.45h below \
L=10. Six classes against a reference with none is anchor 0. \
\
NEITHER CORRECTION MOVES THE ATTACK, and that was checked before they were \
written rather than after. scale_and_proportion goes to 1, not 0, so it joins no \
tie. artifact_level goes to 0 but is 12th in campaign.toml's fixed order, behind \
lighting_and_shadow at 5th. The zeros are now lighting_and_shadow (5), \
color_palette (6), contrast_and_exposure (7), atmosphere (9), artifact_level \
(12); the fixed order still breaks the tie to lighting_and_shadow. \
next_attacked_axis is CONFIRMED, not changed. final_score 0.25 -> 0.20 \
(average 10/12 -> 8/12, lowest still 0) — a recalibration of the same image, not \
a regression of the render, and it must not be reported as one. \
\
ABSTRACTION GATE — missing record, backfilled as abstractions/0006.toml. \
Iteration 9 sets abstraction_introduced = true and names no abstraction, the \
same defect 0005.toml recorded for iteration 7. The unnamed abstraction is \
09b5c12e's supersample tier: a new public method on axiom-host's \
HostDeviceProfile, a new public entry on the windowing module's WindowingApi, \
and a rewritten render-target clamp in gpu-backend. The change CLEARS the gate \
on the "genuinely cannot express" branch — the engine had no anti-aliasing of \
any kind and render_size only ever scaled DOWN, so no app-tier config could ask \
for more samples — so the ruling is missing-record, not too-early and not \
ceremonial. Two things are recorded there that were not recorded anywhere \
before: its screenshot_proof is UNVERIFIED and the A/B at fixed lighting that \
would prove it has still not been run, and it was spent on artifact_level while \
the pass was attacking subject_fidelity, i.e. off both the attacked axis and \
the lowest axis. \
\
THE PATTERN IS NOW THE FINDING. This is the SIXTH consecutive spine change on \
this line to land without its gate record (0001-0006), and each of the five \
previous backfills said in as many words that the repetition was the finding. \
It should stop being read as six oversights and start being read as a process \
defect: nothing in the loop makes the gate record a precondition of landing, so \
it is the step that gets dropped whenever a pass is going well. \
\
MINOR, recorded and not acted on: the era-C bookkeeping is dated 2026-08-06 \
(campaign.toml reference_supplied, the ledger's era header, the INDEX row and \
the champions/0007 filename) while both the era commit edb4f4ff and the \
captured commit c8d11623 are dated 2026-08-05. Not corrected here — the date is \
baked into an archive filename the INDEX references, and renaming it is churn \
with a real chance of desyncing the archive to fix a day. \
\
Guarded arm: canvas2d not re-captured (no render ran) and not scored, as ever.
"""
next_attacked_axis = "lighting_and_shadow"
abstraction_introduced = false

[iteration.scorecard_before]   # era C as previously written — final_score 0.25
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 0
color_palette = 0
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 2
detail_density = 1
artifact_level = 1

[iteration.scorecard_after]    # same image, recalibrated — final_score 0.20
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 0
color_palette = 0
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 1
detail_density = 1
artifact_level = 0

[[iteration]]
iteration = 12
attacked_axis = "lighting_and_shadow"
changed_files = [
  "apps/burnt-rubber/src/render/mod.rs",
  "apps/burnt-rubber/src/render/palette.rs",
  "apps/burnt-rubber/src/render/asphalt_texture.rs",
  "apps/burnt-rubber/src/render/prop_meshes.rs",
  "apps/burnt-rubber/src/render/scenery.rs",
  "apps/burnt-rubber/src/render/scenery_pool.rs",
  "crates/axiom-host/src/frame_sky.rs",
  "modules/axiom-gpu-backend/src/scene_renderer.rs",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
Era-C pass 1 — the night-to-day conversion. Seven lenses ran; SIX stacked, one \
declined by the human. Attacked lighting_and_shadow 0 -> 2. No axis fell; seven \
rose. final_score 0.20 -> 1.175. \
\
THE ART-DIRECTOR'S PROPOSAL WAS DECLINED, NOT DROPPED. It measured the car at \
20% of frame width against the reference's 30% and proposed scaling the chase \
rig 0.70 to close it. That rig was pulled back and lifted two days ago at the \
human's explicit request, for gameplay — road ahead to read a corner and the \
traffic standing in it. The human declined the revert. scale_and_proportion is \
therefore capped by a deliberate design decision and should not be re-proposed \
without them revisiting it. \
\
TWO LENSES CONFLICTED AND WERE RECONCILED, NOT DROPPED. Lighting sized \
KEY_INTENSITY through low_key()'s 0.16 black point and chose 3.6; the colorist \
retired low_key() for cinematic() in the same round, whose black point is zero. \
Dropping either would have left a daylight rig under a night sky. The lighting \
proposal had written the contingency into its own caveat — 'come back to ~2.6' — \
so the foreman resolved both hunks by hand and applied that arithmetic, in a \
separate labelled commit. Neither lens was overruled. \
\
TWO OVERSHOOTS RECORDED. The frame is now brighter than the reference rather \
than darker (band median 178.3 against 139.7) and flatter (saturation 0.204 \
against 0.522); and the zenith is blown to (228,231,232) against the reference's \
(27,111,198) while the horizon band lands nearly right at (128,202,249) against \
(145,219,239). The gradient is correct and the cloud layer is burying it — \
CLOUD_COVERAGE 0.55, authored blind, which the architect itself named as the one \
thing GPU verification had to judge. It reads as a wash, not cumulus. \
\
Still absent and now conspicuous: the sea, beach, sailboats and stilt houses \
(the reference's whole left third is water; the champion's left shoulder is a \
green hillside), and any palm shadow across the road — the reference's single \
most characteristic feature. \
\
Guarded canvas2d arm captured at the same frozen moment. Not scored. \
\
NEXT: by the rule, silhouette_readability (five axes tie at 1, fixed order breaks \
it 3rd). By human decision, pass 2 targets the anamorphic squash \
(scale_and_proportion) first, because the art-director measured that a wedge \
squashed 3.16x in plan renders as a rectangle — so silhouette work is blocked \
behind the aspect fix and would otherwise be spent twice.
"""
next_attacked_axis = "scale_and_proportion"
abstraction_introduced = true

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 0
color_palette = 0
contrast_and_exposure = 0
depth_and_separation = 1
atmosphere = 0
scale_and_proportion = 1
detail_density = 1
artifact_level = 0

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 2
silhouette_readability = 1
material_and_texture_detail = 2
lighting_and_shadow = 2
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 2
scale_and_proportion = 1
detail_density = 2
artifact_level = 1

[[iteration]]
iteration = 13
attacked_axis = "none — scoring recalibration"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/abstractions/0007.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
SCORING RECALIBRATION — no render change. The image on disk is byte-identical \
to the one iteration 12 promoted; four of its twelve scores were not honest. \
final_score 1.175 -> 0.375 on an unchanged frame. The pass still moved the \
campaign (0.20 -> 0.375 under one calibration), by about a third of what it \
claimed, and the next flaw is not where iteration 12 recorded it. \
\
FOUR AXES LOWERED, each against a measurement from the two PNGs in this \
directory: \
\
  subject_fidelity 2 -> 1. The iteration-12 note raised this and then wrote, in \
  the same paragraph, "what holds it at 2 is unchanged shape". The axis IS the \
  shape. Every reason it gave for the rise — the car is lit for the first time, \
  the black rear panel is gone under the sky fill — is the ATTACKED axis's work \
  seen on the subject, already scored at lighting_and_shadow. Counting it twice \
  is a number moving without the render moving toward identity, which is the \
  precise failure mode the skill names. \
\
  lighting_and_shadow 2 -> 1. THE ATTACKED AXIS. It improved and 0 -> 1 is \
  earned. 0 -> 2 is not. The shadow half of the axis is not weak, it is \
  INVERTED: at row 0.60h the reference's road centre is DARKER than the road \
  beside it (54 vs 92) because palm shadows lie across it; at row 0.72h the \
  champion's road centre is BRIGHTER than the road beside it (99 vs 85) because \
  a specular streak runs down it. The reference's signature feature is not just \
  absent, its photographic opposite occupies the same place. And the new key is \
  what clips 11.0% of the frame to white. \
\
  atmosphere 2 -> 1. The note scored 2 and described a milky wash; both cannot \
  be true. The architect named the falsifiable test in its own proposal — does \
  the field read as cumulus or as a wash — GPU verification answered WASH, and \
  the score followed the intent rather than the answer. \
\
  artifact_level 1 -> 0. The rise was justified as "three of six artifact \
  classes gone with the night". Three went; at least three arrived in the same \
  pass — 11.0% of the frame above L=250 against the reference's 1.4% (19.3% of \
  the sky band against 2.8%), a specular blowout streak down the road, and the \
  bottom HUD instruction line running off the LEFT frame edge (leftmost-6px mean \
  luma 83 against the reference's 37). Six classes against a reference with \
  none, which is the count the previous critic pass scored 0 for. A net swap of \
  three defects for three is not a point of progress. \
\
WHAT THE PASS-1 SCORECARD MEASURED AND WHAT IT DID NOT. It measured the floor it \
had repaired (78.1% of the scene band below L=16 -> 0.0%) and did not measure \
the ceiling it had broken (1.4% -> 11.0% above L=250). Every figure it DID \
publish reproduces to the decimal — the numbers were real, four of the scores \
drawn from them were not. \
\
ONE CLAIM I CHECKED AND DROPPED, recorded so nobody repeats it: I expected the \
1280x720 -> 940x1672 blit to leave the frame measurably soft. Scene-band \
high-frequency RMS is 8.26 (reference) against 8.24 (champion) — \
indistinguishable. The anamorphic squash is real; a global softness from it is \
not, and nothing in the recalibrated scorecard rests on one. \
\
ABSTRACTION GATE: abstractions/0007.toml BACKFILLED. Iteration 12 sets \
abstraction_introduced = true and names no abstraction — the same defect 0005 \
and 0006 were each written to repair. The unnamed change is a06678a3, \
FrameSky::with_clouds on the axiom-host LAYER plus its SKY_WGSL mirror and a new \
sky-uniform vec4 in the gpu-backend MODULE. It clears the gate cleanly on the \
"cannot express" branch (above the horizon there is no geometry to put a cloud \
on, and the app-tier alternative — alpha cloud cards — would have survived on a \
backend that declares it DROPS RenderCapability::Sky, i.e. silent backend \
divergence). Justified, but this is the SEVENTH consecutive spine change on this \
line to land without its record. Six critic passes have each written that the \
repetition is the finding rather than the paperwork. Nothing in the loop makes \
the gate record a precondition of landing, so it is always what gets dropped \
when a pass is going well. \
\
NEXT AXIS CHANGES: artifact_level, alone at 0, no tie to break. Iteration 12 \
recorded "by the rule, silhouette_readability (five axes tie at 1)" and then \
targeted scale_and_proportion by human decision. With artifact_level scored \
honestly there is no tie at the bottom at all, and silhouette was never next by \
the rule. The human's decision is NOT overruled and does not need to be: the \
3.16x anamorphic squash and the blown highlight field are the same pass's work \
— the geometry of the blit and the top of the grade — so a pass 2 that fixes the \
aspect and pulls the ceiling under 255 serves artifact_level and \
scale_and_proportion together. \
\
JUDGED ARM: gpu (WebGL2), the only arm scored. The guarded canvas2d arm is \
mentioned nowhere in this recalibration and no axis credits or blames it. \
\
BASE ERROR IN THIS PASS'S FAN-OUT, recorded because it is a process fact and I \
am the lens that keeps those. The brief named `main` (edb4f4ff) as the base for \
all seven era-C pass-1 lenses; the champion line was 8f027579, eight commits \
ahead. A lens that obeyed the brief would have authored against the NIGHT scene \
and scored against the daylight reference. The colorist detected it and rebased \
itself; the rest, this one included, were corrected mid-flight by the foreman. \
Any proposal in this pass that reads as authored against a night frame has that \
as its cause. \
\
NO PARITY LANGUAGE: nothing in this campaign claims the render matches the \
reference, and nothing should. Twelve axes, none above 2.
"""
next_attacked_axis = "artifact_level"
abstraction_introduced = false

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 2
silhouette_readability = 1
material_and_texture_detail = 2
lighting_and_shadow = 2
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 2
scale_and_proportion = 1
detail_density = 2
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 2
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 1
detail_density = 2
artifact_level = 0

[[iteration]]
iteration = 14
attacked_axis = "scale_and_proportion"
changed_files = [
  "modules/axiom-windowing/src/windowing_api.rs",
  "modules/axiom-windowing/src/windowing_api/web.rs",
  "apps/burnt-rubber/src/tuning.rs",
  "apps/burnt-rubber/src/app.rs",
  "apps/burnt-rubber/src/web.rs",
  "apps/burnt-rubber/src/camera.rs",
  "apps/burnt-rubber/src/lib.rs",
  "apps/burnt-rubber/src/render/mod.rs",
  "apps/burnt-rubber/src/render/chunks.rs",
  "apps/burnt-rubber/src/render/palette.rs",
  "apps/burnt-rubber/src/render/prop_meshes.rs",
  "apps/burnt-rubber/src/render/scenery.rs",
  "apps/burnt-rubber/src/render/scenery_pool.rs",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
Era-C pass 2 — the anamorphic projection is fixed. Attacked scale_and_proportion \
1 -> 3, verified at the source rather than inferred: the canvas drawing buffer \
(940x1672, aspect 0.5622) and its CSS box (470x836, aspect 0.5622) now agree to \
four decimals at devicePixelRatio 2, where the buffer had been declared at the \
compile-time 1280x720. No axis fell; four rose. final_score 0.375 -> 1.20. \
\
THE FIX LANDED IN axiom-windowing BECAUSE IT IS THE ONLY CRATE THAT CAN SEE A \
CANVAS — which is why nothing above it had ever caught it, and why every app in \
this repo carries the same latent bug. The rig was re-solved in the same commit \
(arm 5.60 -> 8.91 m, height 1.55 -> 1.71 m), derived by holding the neighbouring \
lane in frame and holding the pitch so the horizon does not move, and floored at \
the authored numbers so landscape displays and the native capture are \
bit-identical. \
\
A MEASUREMENT OVERTURNED A FOREMAN DERIVATION, correctly. Last pass the foreman \
reconciled KEY_INTENSITY to 2.6 by arithmetic through a black point, with no \
render to check against. Lighting this pass inverted the pipeline against both \
frames and found the key ALONE laid 0.861 on flat road where the reference's \
entire sunlit road is 0.746 — so no ambient or grade change could have fixed it. \
1.45 is measured; 2.6 was argued. The road now sits at (63,65,72) against the \
reference's (64,57,55). \
\
TWO LENSES SPLIT ONE ERROR. Surfacing found specular is live and the gloss set \
had never been re-sized for a key that tripled — peak specular is exactly \
(1-roughness)*KEY_INTENSITY, so the tarmac threw 0.83 linear against its own 0.08 \
diffuse and erased its own grain. Both fixes move that product down, so they \
composed without reconciliation. \
\
RECORDED AND NOT SMOOTHED: the exposure overshot the other way (band median 178.3 \
-> 99.1 against 139.7: 39 over became 40 under), clipping is improved but still \
five times the reference (7.50% vs 1.43%), and the zenith did not move at all \
((228,231,232) -> (227,231,232) against (27,111,198)) — which confirms the \
colorist's own stated risk and isolates CLOUD_COVERAGE = 0.55 as the single named \
cause. \
\
FOREMAN ERROR, RECORDED: this pass's fan-out brief named `Base: main` for all \
seven lenses, but main was eight commits behind the champion line and still held \
the night scene. The colorist detected it unaided and rebased itself; the other \
five were corrected mid-flight by message. No proposal was authored against the \
wrong base in the end, but that was luck plus one lens's diligence, not process. \
\
STANDING FINDING FOR THE NEXT ARCHITECT PASS: shadow_view.rs sets SHADOW_EXTENT = \
20.0 as an orthographic box anchored at Vec3::ZERO while this moment is ~1.9 km \
down the course, so every cast shadow is outside the shadow map and the \
reference's defining feature cannot appear at any key setting. Also open: the \
terrain is one flat verge quad to 46 m, so the reference's sea, beach, houses and \
headland have nothing to stand on; ExtendedLimits now costs 1.70x the fragments \
at an honest DPR; and surface resize is unhandled.
"""
next_attacked_axis = "subject_fidelity"
abstraction_introduced = true

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 2
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 1
detail_density = 2
artifact_level = 0

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 2
material_and_texture_detail = 2
lighting_and_shadow = 2
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 3
detail_density = 2
artifact_level = 1

# ---------------------------------------------------------------------------
# Iteration 15 — SCORING RECALIBRATION + GATE BACKFILL. No render changed; no
# candidate was captured; no app source was touched. `decision` is deliberately
# NOT one of the loop's four values, for the same reason as iterations 1, 2, 3,
# 6, 8 and 11: none of them describes "the champion image is untouched and only
# the record moved", and calling this a keep_candidate would fake a promotion.
# ---------------------------------------------------------------------------
[[iteration]]
iteration = 15
attacked_axis = "none — scoring recalibration"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/abstractions/0008.toml",
  "visual_targets/burnt-rubber/champions/INDEX.md",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of era-C pass 2. Four axes lowered, one abstraction backfilled, the
attacked axis CONFIRMED unchanged. final_score 1.20 -> 1.10 on an image that did
not move.

FIRST, WHAT IS HONEST, checked before anything was corrected.

  THE CHAMPION IS CLEAN. champion.png hashes 433540235183900a48c85f98dfe04de2,
  byte-identical to champions/0009-2026-08-06-b0f4e518.png and to the copy on the
  champion line. `git log b0f4e518..HEAD -- apps/burnt-rubber/ crates/ modules/`
  returns nothing, so the only commit since the capture (0245cbce) touched
  visual_targets/ alone. This scorecard describes the image on disk.

  THE ERA IS CONSISTENT. campaign.toml reference_era = "C", INDEX.md marks era C
  current, iteration 14's before/after both sit inside era C. No score crosses the
  boundary and no movement across one is reported as progress.

  EVERY MEASUREMENT IN THE PASS-2 SCORECARD REPRODUCES EXACTLY, re-derived from
  the two PNGs: band median 139.7 / 99.1, whole-frame saturation 0.522 / 0.221,
  whole-frame mean luma 114.4 / 137.1, above-L250 1.43% / 7.50%. Not one figure
  was rounded in the campaign's favour, and the pass named its own overshoot (39
  over became 40 under) in the same breath as its win. That is the part of this
  record that is working, and it is the reason the corrections below are about
  apportionment rather than about honesty.

  THE GUARDED ARM IS CLEAN. No score, no axis note and no ledger reason in this
  campaign credits or blames the canvas2d arm. The four "canvas" hits on the
  scorecard are the HTML canvas element the projection fix measures, not the
  software arm. campaign.toml scopes the guard correctly (legibility, not parity).

  PARITY LANGUAGE: clean. Nothing claims the render matches, is done, or has
  reached parity. Two per-feature phrasings sat at the drift edge — "the road is
  right" and "measured-correct" — and are rewritten in the axis notes this pass
  touches. A per-feature "is right" is one step from "matched" and the record
  should not take that step casually. Nothing on this scorecard is above 2.

1. FOUR AXES INFLATED. The pass's own header declared the risk it was taking —
"the projection fix improves what three axes measure at once … if a critic folds
silhouette back, that is a fair call and this note is the evidence for it" — and
declaring it was right. The split it chose does not survive measurement.

  (a) scale_and_proportion 3 -> 2. THE HEADLINE, and the central finding.
  Its ceiling reason was "the subject still spans 0.313 of frame width against
  the reference's 0.598". The champion figure reproduces exactly. The REFERENCE
  figure is wrong by 2x. Three independent readings agree it is ~0.30: the
  painted body's widest row measures 0.287w, a direct crop read of the rear track
  gives ~0.305w, and this campaign's OWN art-director measured "the reference's
  30%" at iteration 12. A naive full-width red bbox on the reference returns
  1.000w — it swallows the roofs, the umbrella and the sand — which is exactly
  the error shape that produces a figure like 0.598. So the champion's car is at
  109% of the reference's width and the residual this axis was scored against
  does not exist.
  The half that DOES fail was never measured: painted-body aspect 3.42 wide-to-
  tall against the reference's 1.13 (3.0x); full silhouette ~1.89 against ~0.97
  (1.95x). An axis named scale_and_proportion cannot be "clearly on-model" with
  its subject's governing ratio off by 2-3x.
  What genuinely improved is real and is why this is 2 and not 1: the world is no
  longer anamorphic, the moon-vs-BRAKE-ring test is retired, road width and palm
  spacing land, the horizon holds at 0.474 against 0.464, and the car's width now
  matches. That is 1 -> 2. It is not 1 -> 3, because 3 was taken from a source
  proof and a mis-measured residual rather than from the image.

  (b) silhouette_readability 2 -> 1. The fold-back the pass invited, made on
  measurement rather than on the invitation. "A low wide wedge instead of a
  rectangle" is accurate, and the wedge is twice as wide for its height as the
  reference's outline. Off by 2-3x in the governing ratio, no wheels, no exhaust
  tips, no visible rear deck: that is the 1 anchor almost verbatim. The 2 anchor
  asks for the right structure emerging, and the structure emerging is a
  different one — the reference is seen down onto its deck, the champion nearly
  edge-on at its tail panel.

  (c) subject_fidelity HELD AT 1 — the third leg of the split, and it SURVIVES.
  Holding it while raising the other two was the right call and this audit
  confirms it with the measurement the pass did not take: the painted body is
  3.42 w/h against 1.13, so the car is not the reference's at a different size,
  it is a different shape. Recorded because a critic pass that only reports what
  failed is not measuring anything.

  (d) lighting_and_shadow 2 -> 1, and this is the SECOND fold of the same axis.
  Iteration 13 folded it 2 -> 1 with the ruling "0 -> 1 is earned, 0 -> 2 is
  not"; pass 2 raised it back to 2 on the LEVEL half alone. The level half is
  genuinely excellent — the key solved off the reference's own sunlit tarmac,
  overturning a foreman derivation with a measurement, correctly. But the axis is
  lighting AND shadow. The shadow half is still 0 by this scorecard's own
  diagnosis (SHADOW_EXTENT = 20.0 anchored at Vec3::ZERO, this moment ~1.9 km
  down the course, so every cast shadow is outside the map): no palm shadow
  raking the road, and no contact shadow under the car at all, so the subject
  floats. NEW FINDING never before in the record: the key is on the wrong side of
  frame — the reference's sun sits upper-RIGHT with a flare and casts its palm
  shadows leftward; the champion's disc sits upper-LEFT (bright-pixel centroid
  0.262w). That may be rig azimuth or the frozen moment's heading on a curving
  road; the campaign has never established which, and either way it is a fact
  about the judged image.

  (e) material_and_texture_detail 2 -> 1. The previous note credited a road VALUE
  and then said the grain "was not separately measured this pass and is not
  credited on assumption". Measured now, on three clean asphalt patches: the
  champion's lateral micro-step is 0.32-0.47 with patch std 0.79-0.95, against
  the reference's 0.89 step and 2.96 std. The road is a flat gradient. A correct
  road value is colour and is already carried by color_palette; it is not
  material, and nothing in frame carries the reference's sand, foam, water, roof
  tile or frond detail.

  TWO AXES EXAMINED AND HELD, borderline, flagged rather than folded:
  depth_and_separation (recession reads, but four of the reference's planes do
  not exist and haze is doing the separating) and detail_density (green-dominant
  pixels 2.64% against the reference's 16.96%, a 6.4x gap, with six absent
  classes). Both are the most likely next inflations on this file. They are held
  because a critic pass that folds every borderline axis is as unreliable as one
  that folds none.

2. THE HUMAN DECISION ON THE CHASE RIG — audited specifically, and it is
recorded correctly.
  RECORDED AS A DECISION: yes. Iteration 12 states it plainly ("the human
  declined the revert … capped by a deliberate design decision and should not be
  re-proposed without them revisiting it") and the scorecard carries it at the
  axis.
  QUIETLY RE-SCORED AS A DEFECT: no. No axis note penalises the declined pull-in,
  and this recalibration does not either. The opposite is now true and is the
  useful finding: with the reference's car measured correctly at ~0.30w against
  the champion's 0.313w, the width gap the declined proposal was about is CLOSED
  — the un-squash closed it — so the human's decision has cost this axis nothing
  at all. What is folded is the ASPECT residual, a different quantity that was
  never put to them, and which is camera height/pitch rather than pull-in
  distance.
  RE-PROPOSED SINCE: not the pull-in. But disclosure is owed and was not given:
  iteration 14 DID re-solve the rig (arm 5.60 -> 8.91 m, height 1.55 -> 1.71 m)
  in the pass immediately after the decision. It moves AWAY from the declined
  pull-in — further back, holding the adjacent lane in frame — so it respects the
  ruling in direction. It is still a rig change made under a standing human
  decision about the rig, and the ledger describes it only as "the rig was
  re-solved with it". Recorded here so the human can see that their rig moved.
  THE PRINCIPLE, restated because it is the one this pass came closest to
  bending: a decision caps what may be PROPOSED. It does not cap what is
  MEASURED. This campaign set that at iteration 11 — "whether to FIX the squash
  remains a foreman decision; whether to SCORE it is not" — and it holds here.
  Using a decision as the reason an axis stops at 3 quietly converts a residual
  into a non-residual, which is how a capped axis becomes an unscored one.

3. THE TARGET DOES NOT MOVE, and that was checked before the corrections were
written rather than after. Eight axes now tie at 1 (subject_fidelity,
silhouette_readability, material_and_texture_detail, lighting_and_shadow,
color_palette, contrast_and_exposure, atmosphere, artifact_level) and
campaign.toml's fixed order breaks it 2nd.

  NEXT FLAW = subject_fidelity   — UNCHANGED from what iteration 14 recorded.

The inflation was hiding severity and apportionment, not the target. It is also
still the honest next target for the reason the pass gave: the projection fix
means the car's real shape is finally visible, so modelling work will no longer
be spent twice.

4. MISSING ABSTRACTION RECORD, now backfilled as abstractions/0008.toml.
Iteration 14 sets `abstraction_introduced = true` and names no abstraction — the
same defect 0005, 0006 and 0007 were each written to repair. The unnamed change
is 110b59a1 (`WindowingApi::configure_surface_with_scale` /
`configure_surface_with_profile` / `surface_scale_factor`, new public surface on
a spine module) plus 25d16abf typing the ratio as the kernel's `Ratio`. It
CLEARS the gate on the "genuinely cannot express" branch more strongly than any
abstraction on this campaign so far: Module Law #9 makes browser APIs legal only
in `host` and `windowing`, so an app-tier value could not have carried a canvas
measurement even in principle, and the inexpressibility was stated in advance
twice (iterations 9 and 11) across two eras. Ruling: missing-record, not
too-early, not ceremonial.

That is the EIGHTH consecutive spine change on this line to land without its gate
record (0001..0008). Seven previous critic passes each wrote that the repetition
was the finding. Eight for eight is no longer a pattern worth noting; it is the
loop's most reliably reproduced behaviour, and the cause is structural — nothing
makes the gate record a precondition of landing, so it is what gets dropped
whenever a pass is going well.

5. DEBTS, unchanged and now owed eight times: no determinism / repeat-capture
tolerance proof exists for ANY champion this campaign has promoted, and no spine
gate run (coverage / check-architecture / dylint) is recorded for any of the
eight spine commits — 110b59a1 included, whose own message asserts "native,
covered, branchless" with no run in the record that confirms it.

final_score 1.20 -> 1.10 (1*0.7 + (16/12)*0.3). A recalibration of the same
image. The pass still moved the campaign 0.375 -> 1.10 under one calibration, and
the projection fix remains the largest single win this campaign has landed — the
correction is about how it was apportioned across four axes, not about whether it
happened.

JUDGED ARM: gpu (WebGL2), the only arm scored. The guarded canvas2d arm is
mentioned nowhere in this recalibration and no axis credits or blames it.

NO RENDER, NO APP SOURCE AND NO CHAMPION IMAGE WAS TOUCHED BY THIS ITERATION.
"""
next_attacked_axis = "subject_fidelity"
abstraction_introduced = false

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 2
material_and_texture_detail = 2
lighting_and_shadow = 2
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 3
detail_density = 2
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 2
detail_density = 2
artifact_level = 1

[[iteration]]
iteration = 16
attacked_axis = "subject_fidelity"
changed_files = [
  "modules/axiom-render-pipeline/src/shadow_view.rs",
  "modules/axiom-render-pipeline/src/render_pipeline_api.rs",
  "apps/burnt-rubber/src/render/car_model.rs",
  "apps/burnt-rubber/src/render/mod.rs",
  "apps/burnt-rubber/src/render/asphalt_texture.rs",
  "apps/burnt-rubber/src/tuning.rs",
  "apps/burnt-rubber/src/camera.rs",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "keep_candidate"
reason = """
Era-C pass 3. Seven lenses, seven commits, ZERO cherry-pick conflicts — the first \
clean stack of the campaign, despite two lenses in render/mod.rs and two in \
tuning.rs. Attacked subject_fidelity 1 -> 2. No axis fell; eight rose. Every axis \
is now at 2 for the first time. final_score 1.10 -> 2.00. \
\
THE CAR CASTS A SHADOW, for the first time in fourteen iterations. The shadow map \
was never broken: it was anchored at Vec3::ZERO while the moment sits 1.9 km along \
the course, and scene_renderer.rs treats fragments outside the frustum as lit — so \
every shadow was being discarded rather than dimmed. The architect anchored the \
volume on the view instead, and notably REFUSED the fix the code's own doc \
proposed (letting the caller supply the volume, "a change to the frame contract"), \
on the grounds that it would push a derivable quantity into an authorable field \
every app must remember to set. The pipeline already resolves the camera. \
\
FOUR LENSES FOUND ONE CLASS OF BUG — a value correct in the source and destroyed \
downstream. Cloud coverage 0.55 produced a threshold of 0.329 against a field \
whose mean is 0.5, so a lid was painted over a zenith gradient that was already \
the reference's cobalt. The asphalt grain was authored isotropically and sampled \
by a 16x anisotropic filter along the road, arriving at a quarter of its \
amplitude. The hemisphere ambient spanned 0.067 across the whole normal sphere — \
a constant wearing a hemisphere's name. And the shadow box above. None were taste \
calls; all four were arithmetic. \
\
A CLAIM DECLINED. The art-director's proposal rested on "zero road paint below 60% \
of frame height"; my own detector finds paint reaching 0.95 of frame height both \
before and after, so composition_and_framing is HELD and the depression change is \
recorded as landed-but-uncredited. It may have improved the frame; this scorecard \
does not know that it did. \
\
A RESIDUAL RETIRED. The critic established that the reference car spans ~0.30 of \
frame width, not the 0.598 an earlier naive bbox returned by swallowing roofs, an \
umbrella and sand — and the champion sits at 109% of that. The width gap this \
campaign scored against for three passes did not exist, and the human's decision \
to keep the pulled-back rig has therefore cost scale_and_proportion nothing. \
\
STILL ABSENT: the reference's defining palm shadows raking the road (the car's \
shadow is present; the avenue's is not), and its entire left third — sea, beach, \
sailboats, stilt houses — which needs terrain the flat 46 m verge quad cannot \
carry. \
\
HEADROOM WARNING from the architect, unrelated to any axis: \
render_pipeline_api.rs is now 996 lines against the engine_no_large_files hard cap \
of 1000, baseline 0. This commit is safe; the next edit to that file very likely \
is not. It wants splitting.
"""
next_attacked_axis = "composition_and_framing"
abstraction_introduced = true

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 1
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 2
detail_density = 2
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 2
subject_fidelity = 2
silhouette_readability = 2
material_and_texture_detail = 2
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
atmosphere = 2
scale_and_proportion = 2
detail_density = 2
artifact_level = 2

[[iteration]]
iteration = 17
attacked_axis = "none — scoring recalibration"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/champions/INDEX.md",
  "visual_targets/burnt-rubber/champions/0011-2026-08-09-ffd4334e.png",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of the era-C re-baseline. EIGHT axes lowered, final_score 2.00 ->
1.10, on an image the scorecard had never described. The next-attack axis is
CONFIRMED unchanged.

THE FINDING IS THE ONE THE RE-BASELINE COMMIT ANNOUNCED AND LEFT OPEN.
ffd4334e replaced champion.png with a fresh WebGL2 capture of main at the era-C
moment, because the course-authoring rework had moved the road under the
recipe's placement, and stated in its own message: "The scorecard is NOT touched
here: it still describes d054860b's image ... this pass's critic re-scores
against this baseline." Until this entry, the campaign was carrying a 2.00 that
was measured on a frame no longer on disk. That is the stale-champion failure in
its cleanest form — declared honestly by the capture, and still a false record
for as long as it stood.

NINE OF TWELVE PASS-3 CREDITS DO NOT REPRODUCE on the image that is there now.
Re-derived from the two PNGs, nothing inherited:

  scene-band median   139.7 ref | 96.4 claimed | 78.4 on disk   (WORSE by 18)
  pixels above L=250  1.43% ref | 0.27% claimed | 1.54% on disk (clip returned)
  road, 7 taps        (95,82,73)..(236,211,152) ref, a 140-level warm ramp
                    | (132,97,93) claimed
                    | (68,69,74) FLAT at every tap, b > r everywhere
  sky "cloud" frac    12.1% ref | "~18 vs ~19%" claimed | 2.2% on disk
  painted-body w/h    1.14 ref (1.13 at iteration 15) | 1.79 claimed | 2.10
  sky median luma     124.1 ref | -- | 170.5   (+46)
  ground median luma  114.9 ref | -- |  69.1   (-46)

THE EXPOSURE MATCH IS TWO ERRORS CANCELLING. Whole-frame mean luma 115.6 against
114.4, and clipping 1.54% against 1.43%, both read like convergence. They are a
sky 46 levels too bright sitting over a ground 46 levels too dark: a hot pale sky
over a dark road, where the reference is a deep cobalt sky over a sun-struck one.
A global single-number match assembled from an inverted tonal balance is not
evidence of convergence on contrast_and_exposure and is not credited as such.

THE ROAD IS BLUE AGAIN. The clearest single retraction. pass 3 recorded the road
hue as "nearly exact ... where pass 2 was blue-tilted at (113,105,122)". The road
on disk is (68,69,74) at seven taps from bumper to horizon — achromatic, blue-
tilted at every tap, and with no luminance ramp at all. Both halves of
material_and_texture_detail's rise are gone, and the axis returns to 1.

WHAT SURVIVED, stated as plainly as what did not: the car still casts a shadow
and it still falls consistently with an upper-right key (lighting_and_shadow
HELD at 2 — its pass-3 headline is the one that reproduces); the glazed rear
aperture is genuinely in the mesh; the aerial haze and the receding palm
colonnade still separate near from far (depth_and_separation HELD); and the
iteration-15 width finding reproduces exactly — the champion's body spans 0.306
of frame width against the reference's ~0.30, so the pulled-back rig still costs
scale_and_proportion nothing (HELD). Four axes were checked and left alone.

TWO AXES SHOULD HAVE BEEN LOWER BEFORE THE RE-BASELINE, independent of it.
detail_density has been held at 2 for four passes while the frame carries a palm
line, a guardrail, a shrub tuft and some distant boxes against a reference with
sea, beach, sailboats, stilt houses, parasols, headland, mountains, roofs, rocks
and surf. artifact_level was called "largely gone" while the BOOST row prints
across both steering pads, the hint row prints across them too AND is flush-
clipped to x=0, and the gear disc reads "R" at 148 km/h. Those are three
overlapping HUD defects, not one, and they are now in their sixth pass.

RIGHT-AXIS CHECK: composition_and_framing is CONFIRMED as the next flaw, but for
a different reason than the record gave. It is not winning a twelve-way tie among
equals at 2; eight axes are at 1 and the fixed order breaks the tie at its first
entry. The axis also acquired a second, structural divergence that no lens put
there: the course rework made the moment a right-hand sweeper with an off-centre
vanishing point and a guardrail wall, against the reference's dead-straight
symmetric avenue, and the horizon dropped from 0.384h to 0.484h.

ABSTRACTION GATE — a NEW failure mode, and the opposite of the last eight.
Iteration 16 sets `abstraction_introduced = true` and names none, which is the
defect 0005-0008 were each written to repair. This time the honest ruling is not
"missing record": the only spine commit in the pass-3 stack is a2f86d91, and its
own message says "No new API surface, no app change" — it moves the shadow
volume's centre from Vec3::ZERO onto the resolved view camera inside an existing
module. That is a DEFECT FIX, and the gate does not ask for a record for one. So
the flag is CEREMONIAL: it claims a gate event that did not happen. No
abstractions/0009.toml is written here, deliberately — filing a record for an
abstraction that does not exist would be the same ceremony in the other
direction, and would leave the next pass hunting a phantom. The correction is
this paragraph: iteration 16 introduced no abstraction.

PARITY LANGUAGE: clean. Nothing in this campaign claims the render matches, is
done, or has reached the reference; nothing on the recalibrated scorecard is
above 2. The word "fixed" appears in axis notes only against named engine
defects, never against the reference.

THE GUARDED ARM IS CLEAN. No score, axis note or ledger reason in this campaign
credits or blames canvas2d. campaign.toml scopes the guard correctly
("legibility, not parity"), the re-baseline re-captured canvas-guard.png at the
same moment, and the recalibration above touches the judged WebGL2 arm only.

BOOKKEEPING ALSO REPAIRED HERE: ffd4334e landed a new champion on main and added
no champions/INDEX.md row and no archive, breaking that file's "one row per
landing" invariant (rows 0004 and 0007 are the precedent for a no-pass
re-capture). Row 0011 and the archived PNG are added. contact-sheet.png is now
one image behind and is OWED — it is not regenerated here because that is a
render step and this lens does not render.
"""
next_attacked_axis = "composition_and_framing"
abstraction_introduced = false

[iteration.scorecard_before]
composition_and_framing = 2
subject_fidelity = 2
silhouette_readability = 2
material_and_texture_detail = 2
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 2
depth_and_separation = 2
atmosphere = 2
scale_and_proportion = 2
detail_density = 2
artifact_level = 2

[iteration.scorecard_after]
composition_and_framing = 1
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 2
detail_density = 1
artifact_level = 1

[[iteration]]
iteration = 18
attacked_axis = "none — scoring recalibration"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of the six-lens stacked pass. The champion on disk had NEVER BEEN
SCORED; it is scored here. ONE axis lowered (scale_and_proportion 2 -> 1).
final_score 1.08 on this image. The next-attack axis is CONFIRMED unchanged.

THE FINDING: A CHAMPION REPLACED TWICE WITH NO DECISION TAKEN.
champion.png was overwritten by 1213286b (promote the stacked pass) and again
by d317218a (re-capture at the arithmetic moment). Neither commit wrote a
scorecard.candidate.toml, neither appended an [[iteration]], and the loop's
Step-4 decision — keep_candidate / keep_candidate_mark_regression /
reject_candidate / start_new_candidate_branch — was never taken for the pass
that produced the frame the campaign is now carrying. What DID land was an
honest header block on the scorecard saying its numbers described the pre-pass
baseline instead. That is the right instinct and it is not a substitute: a
declared stale score is still a campaign with no measurement of its own render.
Iteration 17 caught the same class of failure one pass earlier and wrote that
"nothing in the loop makes the gate record a precondition of landing"; the same
sentence now applies to the SCORE. Two consecutive promotes have shipped
without one.

THE BEFORE/AFTER OF THIS PASS IS STILL CONTAMINATED, and d317218a's message
reads as though it is not. That commit fixed a real and serious recipe defect —
freeze-then-place-then-step-a-fixed-count, so the moment is arithmetic
(1966.118 m, 43.157 m/s, verified byte-identical over two runs) instead of
wall-clock — and re-captured the AFTER on it. But the BEFORE, champions/0011,
is still the old wall-clock capture at ~2039 m. Seventy-three metres of a
course whose geometry demonstrably changes over that distance sit inside every
comparison of the two, and cannot be separated out. The commit says "the
round-2 before/after differ by the lenses and nothing else", which is true of
the NEXT round and not of the one that just landed. Any reading of this pass as
clean lens-attributable movement is unsupported. Comparability starts now.

WHAT REPRODUCES — measured on the frame on disk, and stated first because four
of the six lens claims land and the record had not credited any of them:

  car bottom edge     0.709h ref | 0.621h before | 0.690h now   (art-director)
  road r-b @ .56h        +32 ref |    -12        |   +23        (lighting)
  zenith RGB      (1,144,229) ref | (108,184,255) | (10,175,233) (colorist)
  palm shadows on road   yes ref |     no         |   yes        (architect)

The lighting lens's key gel in particular RETRACTS THE RETRACTION: iteration
17's headline was "THE ROAD IS BLUE AGAIN" at (68,69,74) with b > r at every
tap. On this image the road runs r-b +19..+24 against the reference's +32. That
finding is withdrawn in the scorecard, named as withdrawn, and the credit is
given.

WHAT MOVED AWAY, which no commit message and no scorecard noted:

  mean luma          112.0 ref | 114.3 before | 96.6 now   (15.4 BELOW ref)
  saturation         0.523 ref | 0.341 before | 0.575 now  (crossed over)
  cloud fraction     18.0% ref |  1.2% before | 0.5% now
  max luma / >L=235  255 / 4.84% ref | -- | 249 / 2.02% now

The frame is 17.7 levels darker than the one the lenses were shown and now sits
below the reference where it sat above. 63cf22f9 is titled "exposure-neutral"
and holds flat-road luma to 0.006%, which is true of the term it solved and not
of the frame; the causes are confounded (the 73 m moment shift, the
art-director's added depression putting more dark road in shot, the re-gel) and
an unscored promote is precisely what makes them unresolvable after the fact.
f72edb92 names atmosphere in its title and the statistic that axis is scored on
halved. No axis is above 2, so the "non-attacked axis fell >= 2" reject rule
cannot fire — the floor is hiding the regression, not absolving it.

THE ONE LOWERING: scale_and_proportion 2 -> 1. Held at 2 for three passes on a
single measurement, the car's painted width (0.302w against ~0.298w), which
still reproduces exactly and is still true. Underneath it sat two uncounted
proportion errors on the largest objects in the scene. The nearest roadside
palm measures 293 px over an 862 px carriageway at its own base row — 0.34 road
widths — against the reference's 464 px over 690 px, 0.67. Same detector, both
images: the palms stand at about HALF the height they should relative to the
road they line, and the reference's two foreground palms leave the top of the
frame while none of the champion's comes within 0.30h of it. The carriageway is
also ~25% wider in frame at the comparable row, five dash columns against
three. One correct sub-measurement against two structural errors is a 1.

RIGHT-AXIS CHECK: composition_and_framing CONFIRMED. Nine axes at 1 and the
fixed order breaks the tie at its first entry. Worth recording that the axis
survives at 1 despite a genuine win inside it — the subject drop closed to
0.019h of the reference — because three divergences of KIND remain: a
right-hand sweeper with an off-centre vanishing point against a straight
symmetric avenue, a horizon 0.08h low, and HUD furniture that differs in kind
and has for six passes.

ABSTRACTION GATE: HONORED. No record is owed and none is filed, and this is the
first pass in this campaign where that is the true answer rather than a missed
one. Two commits were candidates and both fail the gate's trigger, not its
justification:
  * fda9639c (architect) touches modules/axiom-render-pipeline but adds no
    facade export and no frame-contract field — shadow_volume and ORIGIN_VOLUME
    are crate::shadow_view internals, the API-file diff is an import and a call
    inside an existing closure, and the change replaces a hard-coded 20 m
    half-extent with the view frustum's own bounding sphere. That is a DEFECT
    FIX at the lowest correct layer — the identical ruling iteration 17 gave
    a2f86d91 for the same file and the same reason.
  * b2587aaa (surfacing) adds apps/burnt-rubber/src/render/verge_texture.rs, a
    new procedural albedo. It is APP-TIER content authored through the existing
    MaterialTexture path — the generation/data rung of the ladder, not new
    engine surface. The precedent is explicit: 0002 and 0004 were filed for new
    axiom_host types and a changed instance layout, not for authored textures.
No abstraction_introduced flag was set this pass because no ledger entry was
written at all; recording it here is the correction.

PARITY LANGUAGE: clean. Nothing in the pass's six commits, the promote, the
re-capture or this scorecard claims the render matches, is done, or has reached
the reference. Nothing scored is above 2. d317218a's "the architect's fix IS
delivering" is a claim about an engine feature reaching the pixels, verified
here (palm shadows are on the road), not a parity claim.

THE GUARDED ARM IS CLEAN. canvas-guard.png was re-captured at the same moment;
it reads mean luma 58.6 over 9..246 with the subject present, so it is legible,
not black and not errored. No score, axis note or reason in this campaign
credits or blames canvas2d, and this recalibration touches the judged WebGL2
arm only.

STILL OWED, and now one pass older: champions/contact-sheet.png was last
regenerated at bcc79aaf and is missing row 0011. Iteration 17 declined it as "a
render step"; it is not — the skill's recipe is pure PIL tiling of PNGs already
on disk — and it should be cleared by whoever lands the next champion, along
with the 0012 row. No archive row is owed for 1213286b or d317218a: neither is
on main, and the archive rule is archive-on-landing.
"""
next_attacked_axis = "composition_and_framing"
abstraction_introduced = false

[iteration.scorecard_before]
composition_and_framing = 1
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 2
detail_density = 1
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 1
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

[[iteration]]
iteration = 19
attacked_axis = "none - scoring recalibration"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/abstractions/0009.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of the ROUND-2 seven-lens pass (ace25285, fcaaf9e4, 5aff1bea,
51c5d43e, bde66b27, a17c44ad, promoted by 7f33fca4). That pass wrote no
scorecard.candidate.toml and no [[iteration]]; the champion it promoted had
never been scored. It is scored here. ONE axis lowered
(lighting_and_shadow 2 -> 1). final_score 1.05 on this image. The next-attack
axis is CONFIRMED unchanged at composition_and_framing.

THE FINDING: A STALE SCORE THAT DECLARED ITSELF CURRENT.
Iteration 18's scorecard opened with a banner reading "THIS FILE NOW DESCRIBES
THE IMAGE ON DISK. The offset is discharged." One commit later 7f33fca4
overwrote champion.png with the round-2 render (829,972 -> 840,496 bytes) and
left that file untouched. 7f33fca4's own message then says "the scorecard
header it wrote already says so in its own words" - but the header said the
opposite. Nobody in this sequence stated anything they believed to be false and
the record still ended up asserting something untrue, which is the failure mode
worth naming: iteration 18's honest declared-stale header was recoverable,
because it told the next reader to go and check. A declared-CURRENT stale score
is not, because it tells the next reader not to. If the loop keeps scoring the
BEFORE - and iterations 17, 18 and now 19 all have, and the promote each time
called that the correct order - then the promote must clear the banner it
invalidates, or the offset must stop being described as discharged at all.

THE LOWERING: lighting_and_shadow 2 -> 1, because THE IMAGE CHANGED UNDER THE
SCORE. The 2 rested, in iteration 18's own words, on three sub-features, the
first of which was palm shadows raking the carriageway - "the reference's
defining lighting event". They are absent from the frame on disk. Verified
independently of the promote message by stacking the identical road band
(rows 0.50h..0.72h, same arithmetic moment) from both frames: four hard shadow
bars across the right lanes become none, and the only shadow left in the band
is the car's own contact blob. The sun disc IS now in frame, which is a real
reference feature and the deficit iteration 18 named - but it was cited there
as a CEILING argument ("the sun itself is not in frame", a reason the axis
could not reach 3), and removing a ceiling argument does not raise a floor. It
also renders as a hard-edged white circle clipped at the right frame edge with
no rays, against a reference sun that throws wide multi-ray flare. One contact
shadow plus a pasted disc is a 1.

THE REJECT RULE COULD NOT FIRE, AND THAT IS STRUCTURAL. The loop rejects when a
non-attacked axis falls >= 2. lighting_and_shadow fell by 1, from 2, because 1
is the floor for anything legible. Iteration 18 recorded the same hole one pass
ago. With eleven of twelve axes at or below 2, the rule is arithmetically
unreachable for ANY single-step regression on this campaign and will stay so
until several axes clear 3. "No reject fired" must not be read as "nothing
regressed" - and to 7f33fca4's credit, it did not read it that way: the promote
names the regression, explains the cause (the key moved from 28.8/20.2 to
16.2/23.8, and a higher, less lateral key drops the palm shadows onto the verge
behind them), and lands it as the human's judgement call. That is legitimate.
What was missing is the half the promote explicitly deferred - scoring it. A
regression named in a commit message and not carried into the scorecard is a
regression the next pass cannot see.

WHAT ROUND 2 GENUINELY WON, measured on one detector across all three frames
(reference | round-1 | on disk):
  scene-band median   139.8 | 72.3 | 95.2   (largest luminance move in era C)
  p95 luma              235 |  174 |  191
  pixels above L=235   5.03%| 2.10%| 3.01%  (a highlight end, for the first time)
  zenith green          101 |  139 |   90   (off by 38, now off by 11)
  hazy/bright sky     13.4% | 1.5% | 3.1%
  mean luma           114.3 |101.0 | 97.0   (the one that moved AWAY)
Plus the HUD bottom band: BOOST row and both legend rows no longer print across
the steering pads and no longer overflow a frame edge, verified on a crop.
Two of artifact_level's four standing defects are discharged; the white
lane-dash wedges through the left pad and the GAS ring, and the gear disc
reading "R" at 155 km/h (now eight passes old), are not.

DETECTOR NOTE. This pass measured with its own grid sampler, whose reference
readings differ from the previous three scorecards' by a few luma levels and
several points of cloud fraction. All columns above are from the SAME code in
the same run and are comparable to each other and to nothing else. Recorded so
that a detector difference is never read as a movement.

ABSTRACTION GATE: MISSING-RECORD, BACKFILLED as abstractions/0009.toml.
a17c44ad added FrameSky::with_haze_height (+229 lines in crates/axiom-host) and
its WGSL mirror (+27 in modules/axiom-gpu-backend) - new public surface on a
layer, mirrored into a module, inside a convergence pass, with no gate record.
Ninth consecutive. The change CLEARS the gate on the inexpressibility branch,
and cleanly: the app's zenith stop had become a slope hack whose ceiling is
arithmetic (the reference top-of-frame green needs a blend of 0.88, the camera
supplies 0.545, at which the required green is negative). It also scopes its
capability correctly - it rides the existing Sky bit, so the GPU arm evaluates
it and the Canvas 2D profile drops it by declaration. It is the RECORD that was
missing. The other six lens commits are app-tier and owe nothing: ace25285
(hud/touch/web), fcaaf9e4 (scenery), 5aff1bea (render/mod), 51c5d43e (a new
procedural foliage albedo through the existing MaterialTexture path - the
generation rung, per the 0002/0004 precedent), bde66b27 (render/mod, palette).

RIGHT-AXIS CHECK: composition_and_framing CONFIRMED. Ten axes now sit at 1 and
the fixed order breaks the tie at its first entry. Round 2's art-director commit
fixed the bottom-band collision, which is a HUD-overlap defect and is credited
at artifact_level where iteration 18 charged it; the three divergences of KIND
that hold this axis at 1 - sweeper against straight avenue with an off-centre
vanishing point, a horizon well below the reference's, and HUD furniture
different in kind for seven passes - are untouched.

PARITY LANGUAGE: clean. Nothing in round 2's seven commits, the promote, or the
recalibrated scorecard claims the render matches, is done, or has reached the
reference. a17c44ad reports one gradient stop landing one level off the
reference's; that is a single measured sub-metric generalised to nothing, and
0009.toml flags it under the same warning 0008.toml raised - a source-verified
fact is not a scoring measurement.

THE GUARDED ARM IS CLEAN AND UNSCORED. canvas-guard.png was re-captured by the
same promote; it reads mean luma 61.5 over 9..246 with the subject, road, palm
line, bollards and full HUD present - legible, not black, not errored. No
score, axis note or reason in this campaign credits or blames canvas2d, and
a17c44ad's "the guarded arm is untouched by declaration rather than by
omission" is a correct scoping statement, not a credit. This recalibration
touches the judged WebGL2 arm only.

STILL OWED: champions/contact-sheet.png is now two images behind (last
regenerated at bcc79aaf, missing 0011 and later). No archive row is owed for
7f33fca4 - it is not on main and the archive rule is archive-on-landing. And
no spine gate run (coverage / check-architecture / dylint) is recorded for any
of the nine spine commits on this line, a17c44ad included, which touches a
layer and a module at once.
"""
next_attacked_axis = "composition_and_framing"
abstraction_introduced = true

[iteration.scorecard_before]
composition_and_framing = 1
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 2
color_palette = 2
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 1
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

[[iteration]]
iteration = 20
attacked_axis = "none - scoring recalibration"
changed_files = [
  "visual_targets/burnt-rubber/scorecard.champion.toml",
  "visual_targets/burnt-rubber/abstractions/0010.toml",
  "visual_targets/burnt-rubber/abstractions/0011.toml",
  "visual_targets/burnt-rubber/ledger.toml",
]
champion_screenshot = "champion.png"
candidate_screenshot = "champion.png"
decision = "scoring_recalibration"
reason = """
Critic audit of the ROUND-3 six-lens pass (b3e4fbed, d6ae6a57, d97b8935,
440eac5b, a1190a20, promoted by f1d722b3; the modeler's 0bbc65b9 was dropped
by the promote). Like round 2, that pass wrote no scorecard.candidate.toml and
no [[iteration]]; the champion it promoted had never been scored. It is scored
here, on the image on disk (998,034 bytes, md5 c2ab203f...), measured with the
same detector iteration 19 used, re-run over all three images in one pass so
the reference column reproduces to the tenth of a level.

THREE AXES MOVE, ALL BECAUSE THE IMAGE CHANGED UNDER THE SCORE:
  lighting_and_shadow   1 -> 2   RESTORED
  color_palette         2 -> 1   LOWERED
  contrast_and_exposure 1 -> 2   RAISED
final_score 1.075 on this image, against 1.05 on the previous one. DIFFERENT
IMAGES. The 0.025 is not a movement and must not be quoted as progress.

THE STALE SCORE, FOURTH PASS RUNNING - and correctly declared this time.
f1d722b3's first paragraph is "THE SCORECARD IN THIS STACK DESCRIBES THE
PREVIOUS IMAGE, NOT THIS ONE", written because iteration 19 caught the round-2
promote asserting the opposite. That is the right way to leave an offset and it
is why this pass found it in one read. It is still four consecutive promotes
that have never scored the image they promoted in the same commit, and the
recalibration is therefore always one pass behind the render the lenses aim at.

THE RESTORATION, and why a critic handing points back is not inflation.
lighting_and_shadow was lowered to 1 exactly one pass ago for exactly one
reason: the palm shadows raking the carriageway - "the reference's defining
lighting event" - had vanished. They are back, measured on the identical band:
14.6% of rows .50-.72h sit below 0.65x the band median, against the reference's
20.5% and round 2's car-contact-blob-only. Refusing the 2 back would be the
same offence as inflating - scoring the record instead of the image. It is NOT
progress over round 2: it is one feature going away and coming back, and any
lens quoting "1 -> 2" across those passes as a gain is misreading the
scorecard. The 2's ceiling is now a different defect from the one that used to
cap it: the key is over-driven (KEY_INTENSITY 1.84 -> 5.9) and the road band's
luminance profile INVERTS against the reference's monotone ramp
(118.7/136.0/119.9/84.1 against 152.8/121.2/99.4/75.1).

THE LOWERING: color_palette 2 -> 1, on the largest surface in the frame. Same
patch, same rows, same arithmetic moment: the carriageway goes (79,63,56)
r-b +23.0 to (165,109,85) r-b +79.9, against a reference asphalt of (85,76,70)
r-b +15.8. Five times the warm bias, and round 2 was NEARER. The previous 2 was
justified in its own words on "hue FAMILIES are all right ... warm grey road";
an orange clay roadway under a third of the frame is a divergence of kind, not
a magnitude, and that justification is gone. Compounding: saturation unimproved
at 0.566 against 0.522, and the zenith moved AWAY, (7,90,187) -> (4,90,179)
against (1,99,204).

THE RAISE, AND ITS COUPLING - which is the finding, not the number.
contrast_and_exposure's two headline statistics both landed: mean luma
97.0 -> 119.0 against 114.3 (seventeen under becomes five over) and scene-band
median 95.2 -> 132.2 against 139.6 (forty-five under becomes seven under). It
is a 2 and not a 3 because the tone CURVE is wrong at both ends at once: p95
200 against 235 while the population above L=250 runs 2.35% against 1.44%. And
the exposure was produced BY the colour defect - a 3.2x warm-gelled key times a
sunlit() grade at wb_r 1.15 / exposure 1.08. The frame is bright because it is
orange. The lens that fixes the road hue will take some of this back, and that
must be read as the cost of the fix, not as a fresh regression to reject on.

A NINE-PASS PHANTOM DEFECT IS STRUCK. Every scorecard since era B has charged
artifact_level with "the gear disc still reads 'R' at 155 km/h", called it the
campaign's longest-standing unfixed item, and diagnosed it as a CAPTURE-RECIPE
fault - the probe placing the car without engaging a forward gear. It is none
of those things. burnt-rubber has no gear model and no gear readout: a grep for
"gear" across apps/burnt-rubber/ returns nothing. The top-right ring is the
touch RESET button - apps/burnt-rubber/src/touch.rs:83 is
PadButton::Reset => "R", and line 749 says "The reset button sits top-right".
It reads "R" at every speed and no capture recipe can change it. Nine passes of
lenses were pointed at a fix that could not exist, and an axis was held down on
it. The true fact underneath - the reference puts a gear numeral where this
game puts a reset button - is HUD furniture differing in kind and is already
charged once, at composition_and_framing. artifact_level stays at 1 on the
lane-dash wedges through the GAS ring alone.

THE SCALE SWAP, which is the "faked progress" check earning its keep.
scale_and_proportion does not move, and the reason is that round 3 traded one
defect for another. Discharged: the palms now reach the reference's height and
slightly past it (canopy occupancy 15.0% in rows .20-.35h against 13.3%).
Introduced by the same change: the stems were scaled in height without being
scaled in girth, so at row .46h the two foreground trunks measure 13 and 12 px
against the reference's 37 and 33 - a third the width at comparable height,
reading as poles rather than palms. Still standing: the carriageway is wider at
the comparable row, five dash columns against three.

THE REJECT RULE STILL CANNOT FIRE. color_palette fell by 1, from 2. The rule
rejects at >= 2. Iterations 18 and 19 each recorded this hole in the same
words; this is the third. With ten of twelve axes at or below 2 it is
arithmetically unreachable for any single-step regression and will stay so
until several axes clear 3. "No reject fired" is not "nothing regressed" - and
to f1d722b3's credit it did not read it that way: the promote names the orange
road, gives the two-lens cause, and lands it as the human's call. What was
missing is the half it deferred - scoring it.

ABSTRACTION GATE: MISSING-RECORD, BACKFILLED TWICE. Round 3 landed TWO spine
commits and recorded neither.
  0010.toml <- a1190a20 (axiom-host frame_depth_fog +213, a new
    RenderCapability::AerialPerspective, frame_submission_report +6, and
    mirrored read paths in BOTH backend modules). Clears the gate on
    inexpressibility: the old fog was a normalised CLIP-DEPTH window, so a
    single near/far pair could not separate a 322 m band from the horizon, and
    an app cannot convert metres to clip depth without re-deriving the camera's
    own transform. Capability correctly scoped - GPU evaluates, Canvas 2D drops
    by declaration. UNVERIFIED: never rendered, by the promote's own admission,
    and no axis credits it.
  0011.toml <- 440eac5b (a new public FramePostProcess::sunlit() preset on the
    host layer). Clears the gate on inexpressibility - FramePostProcess has NO
    public constructor, only five getters and three presets, so an app cannot
    author a grade at all. Recorded with a structural reservation the next
    architect should read: a grade type whose only entry points are named
    presets forces every app into adding its authored look to the engine layer,
    which is the wrong direction of dependency pressure. sunlit() is defensible
    under today's shape and indefensible as a pattern.
Tenth and eleventh consecutive spine changes on this line to land without a
gate record. The count is the finding.

THE COORDINATION FAILURE THE BOARD CANNOT SEE, named once so it is findable.
d6ae6a57 tripled the key and 440eac5b warmed the grade, each solved correctly
in isolation, each blind to the other, multiplicative on the same path. The
product is the orange road. f1d722b3 predicted it before rendering and landed
it anyway. This will recur on any axis pair sharing a multiplicative path; it
is not a lens error and no lens can fix it.

RIGHT-AXIS CHECK: composition_and_framing CONFIRMED. Nine axes sit at 1 and the
fixed order breaks the tie at its first entry. For the first time this era the
axis WAS attacked - b3e4fbed grew the palm avenue on an FOV-invariant ratio -
and it did not move, because none of the three divergences of KIND that hold it
at 1 is about palm height: a right-hand sweeper with an off-centre vanishing
point against a dead-straight symmetric avenue to a centred one; a horizon well
below the reference's, leaving empty sky where the reference has beach, sea,
village and headland; and HUD furniture different in kind for an eighth pass.
Logged as attempt 1 of the abstraction gate's three on this axis.

HUMAN OVERRIDE, RECORDED SO THE COUNTER DOES NOT DRIFT. The modeler lens was
DIRECTED by the human onto the player car's geometry (subject_fidelity) rather
than choosing its own flaw - a deliberate override of the board's no-steering
rule, and the human's call to make. Its commit 0bbc65b9 was then dropped by the
promote for a stated arithmetic reason (it grew the palms via PropKind unit
half-extents while the art-director grew them via rank scale; final height is
the product). Consequence for the record: work aimed at subject_fidelity by
instruction is NOT an attempt on composition_and_framing, and the gate's
">= 3 failed bounded attempts" count for the next-flaw axis stands at ONE.

PARITY LANGUAGE: clean. Nothing in round 3's six commits, the promote, or the
recalibrated scorecard claims the render matches, is done, or has reached the
reference. 440eac5b's haze constant "round-trips to the reference's measured
(157,204,210) exactly" - an arithmetic identity about one authored constant,
not a claim about the frame, generalised to nothing. f1d722b3's "the exposure
target was hit" is qualified in the same sentence with "by the wrong means".
Neither is struck.

THE GUARDED ARM IS LEGIBLE AND UNSCORED. canvas-guard.png reads mean luma 67.3
over a 0..246 range with the subject, road, palm line, bollards and full HUD
present - not black, not errored. No score, axis note or reason in this
campaign credits or blames canvas2d; its numbers appear in no scored column.
One thing for the next capture to check rather than assume: a1190a20 added 51
lines to canvas2d_backend_api.rs in the same commit as the new capability.
Keeping the guarded arm legible under a new capability is legitimate; TUNING it
would not be, and neither arm was rendered this pass.

STILL OWED, carried forward from iteration 19 and grown: champions/
contact-sheet.png is now three images behind; and no spine gate run (coverage
100% / check-architecture / dylint / ts-gate) is recorded for ANY of the eleven
spine commits now stacked on this line, a1190a20 and 440eac5b included, which
between them touch one layer and two modules.
"""
next_attacked_axis = "composition_and_framing"
abstraction_introduced = true

[iteration.scorecard_before]
composition_and_framing = 1
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 1
color_palette = 2
contrast_and_exposure = 1
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 1
detail_density = 1
artifact_level = 1

[iteration.scorecard_after]
composition_and_framing = 1
subject_fidelity = 1
silhouette_readability = 1
material_and_texture_detail = 1
lighting_and_shadow = 2
color_palette = 1
contrast_and_exposure = 2
depth_and_separation = 2
atmosphere = 1
scale_and_proportion = 1
detail_density = 1
artifact_level = 1
