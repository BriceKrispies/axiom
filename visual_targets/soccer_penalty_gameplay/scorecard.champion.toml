# Champion = the KICKER-PASS render (convergence/soccer-kicker: a 4-lens board —
# proportion db27633 + pose 182381d + kit 31fe1da + framing cb2ee93, assets
# regenerated) vs reference.png. Same harness (axiom-shot --app soccer-penalty
# --backend gpu --features offscreen, 960x600, tick 0; byte-deterministic).
# 0..5, harsh; a proxy = 1. final_score = lowest*0.7 + avg*0.3
#
# This pass was a FOCUSED team effort on the KICKER CHARACTER MODEL only (user
# request). Four lenses each landed one bounded, isolated commit; the integrator
# stacked, regenerated the kicker.figure/kick_right.clip binaries from the merged
# authoring.rs, rendered once, and scored. The two KICKER axes rose; every
# non-kicker axis HELD (none dropped). The render remains a stylized low-poly
# box-man proxy vs a clean low-poly mobile reference — harsh caps applied, no
# parity claimed.

composition_and_framing   = 3  # held. Near-eye-level over-the-shoulder now: kicker foreground-left / ball centre-low / goal + keeper upper-mid. The goal sits a touch lower/larger than the reference (less crowd headroom) but every essential relationship holds, and the FULL kicker now reads — net-neutral, kept at 3.
camera_lens_and_scale     = 4  # RAISED 2->4. The defining champion flaw — the kicker hard-cropped at the torso (legs/boots off the bottom) — is FIXED: the framing lens dropped the eye to ~head height (y 4.3->2.1), levelled the aim, dollied in and widened the lens (fov 18->19.5) so the WHOLE kicker (hair to boots) reads in the left third at reference scale while the far goal barely moves. Shot class now MATCHES the reference. Capped at 4 (not 5): goal a touch larger/lower, ball-x ~44% vs ref ~49%.
kicker_silhouette         = 3  # RAISED 2->3. Now clearly on-model: leaner athletic proportions (smaller head, longer/slimmer legs, broader shoulders, quieter sphere-joints), a dark-HAIR head (was bald/skin), a royal-blue jersey, white shorts, BLUE socks over black boots, and a planted stride-ish stance with a trailing arm. Capped at 3: still a blocky box-man with visible sphere joints, NO jersey "10" (the number IS authored in recipe_textures.rs::jersey() but does not land on the rear chest face — a UV/render-bridge gap, queued next), and from directly behind the run-up lean reads more upright than the reference.
goalkeeper_silhouette     = 1  # held. Small yellow box-man, arms held up in a non-keeper Y-stance; slightly larger/lower after the re-frame but still a thumbnail proxy. Untouched by this kicker pass.
ball_fidelity             = 3  # held. Black/white panelled ball, close and centred; still blocky low-res panels.
goal_and_net_fidelity     = 2  # held. White posts + hanging grid net; the re-frame makes it larger/lower but no better/worse in fidelity (coarse open grid, no cloth droop).
field_markings_and_turf   = 1  # held. Flat green, no penalty spot/box/arc/mow-stripes.
stadium_and_ad_boards     = 1  # held. Solid red boards (no "AXIOM SPORTS" text) + dark near-empty crowd void.
lighting_shadow_and_depth = 2  # held. Flat daylight Lambert + hard blob shadows.
gameplay_readability      = 2  # held. Reads as a penalty setup; still no reticle/HUD/power bar.
artifact_level            = 3  # held. Clean flat colour; blocky low-res edges + faint grass/crowd noise remain.

# lowest = 1, tied across goalkeeper_silhouette, field_markings_and_turf,
# stadium_and_ad_boards. By FIXED axis order the first lowest axis is
# goalkeeper_silhouette. THE KICKER AXES ARE NO LONGER THE CONSTRAINT — this
# pass moved camera_lens_and_scale (2->4) and kicker_silhouette (2->3) off the
# bottom, exactly the focused goal.
# sum = 3+4+3+1+3+2+1+1+2+2+3 = 25. avg = 25/11 = 2.273.
# final_score = 1*0.7 + 2.273*0.3 = 1.38 (was 1.30 for the pre-pass champion; the
# +0.08 tracks the real kicker gains, not inflation — the lowest axis is unchanged
# at 1, so final moves only via the average).
# Next axis (lowest, ties by fixed order): goalkeeper_silhouette — a non-kicker
# axis, so the kicker-focused campaign has achieved its brief.
