# Candidate scorecard — iter 6 (ball black/white panels). Parity bar.
composition_and_framing = 3   # unchanged
subject_fidelity        = 2   # kicker posed (iter4); goalie still boxy
silhouette_readability  = 2   # unchanged
pitch_detail            = 2   # pronounced mow stripes + crisp lines (iter5)
stadium_and_crowd       = 2   # unchanged
color_palette           = 3   # warmer, less minty grass (iter5)
contrast_and_exposure   = 2   # unchanged
lighting_and_shadow     = 2   # unchanged
ball_fidelity           = 2   # 6 dark panels proud of the surface -> reads as a black/white soccer ball, not a blank sphere
depth_and_separation    = 3   # unchanged
scale_and_proportion    = 2   # unchanged
artifact_level          = 4   # clean render
