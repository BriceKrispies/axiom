# Candidate scorecard — iter 7 (real low-poly meshes: capsule limbs, sphere heads). Parity bar.
composition_and_framing = 3   # unchanged
subject_fidelity        = 3   # kicker + goalie now rounded figures (capsule limbs, sphere heads), not cube stacks; still blobby proportions, no face/number
silhouette_readability  = 3   # rounded silhouettes read more human
pitch_detail            = 2   # unchanged
stadium_and_crowd       = 2   # unchanged (still flat cards)
color_palette           = 3   # unchanged
contrast_and_exposure   = 2   # unchanged
lighting_and_shadow     = 2   # unchanged
ball_fidelity           = 2   # smooth sphere + panels
depth_and_separation    = 3   # unchanged
scale_and_proportion    = 2   # rounded but capsule torso/shorts still a bit blobby
artifact_level          = 4   # clean render
